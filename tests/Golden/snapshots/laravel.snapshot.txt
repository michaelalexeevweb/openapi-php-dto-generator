# laravel mode — 47 files, 4640 lines
# spec: OpenApiExamples/test.yaml, namespace: GoldenCorpus
#
# Animal.php
# Animal2.php
# Animal2AnyOf4.php
# AnimalOneOf3.php
# ApiTest2403.php
# ApiTest2PatchRequest.php
# ApiTest2PatchRequestFormRequest.php
# ApiTest2PostRequest.php
# ApiTest2PostRequestFormRequest.php
# ApiTest403.php
# ApiTestCatsActionsGetQueryParams.php
# ApiTestCatsActionsGetQueryParamsFormRequest.php
# ApiTestCatsActionsPostQueryParams.php
# ApiTestCatsActionsPostQueryParamsFormRequest.php
# ApiTestDogsTagsGetQueryParams.php
# ApiTestDogsTagsGetQueryParamsFormRequest.php
# ApiTestDogsTagsPatchQueryParams.php
# ApiTestDogsTagsPatchQueryParamsFormRequest.php
# ApiTestFileDeleteQueryParams.php
# ApiTestFileDeleteQueryParamsFormRequest.php
# ApiTestFilePatchQueryParams.php
# ApiTestFilePatchQueryParamsFormRequest.php
# ApiTestFilePostRequest.php
# ApiTestFilePostRequestFormRequest.php
# Cat.php
# Discriminator.php
# Discriminator1.php
# Discriminator2.php
# DiscriminatorType.php
# Dog.php
# Dog2.php
# FileRequest.php
# FileRequestFormRequest.php
# Pet.php
# Tag.php
# Test.php
# Test2.php
# Test2Types.php
# Test2UserType.php
# TestPatchRequest.php
# TestPatchRequestFormRequest.php
# TestPostRequest.php
# TestPostRequestFormRequest.php
# TypeEnum.php
# User.php
# User2.php
# User2Pet.php

===== Animal.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 */
interface Animal
{
}

===== Animal2.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 */
interface Animal2
{
}

===== Animal2AnyOf4.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 *
 * Laravel mode: the class needs nothing installed — `rules()` is fed to the framework's own
 * validator, and `fromValidated()` builds this object from `$request->validated()`:
 *
 *     final class StoreRequest extends FormRequest
 *     {
 *         public function rules(): array
 *         {
 *             return Animal2AnyOf4::rules();
 *         }
 *     }
 *
 *     $dto = Animal2AnyOf4::fromValidated($request->validated());
 */
final class Animal2AnyOf4 implements Animal2
{
    public function __construct(
        private readonly string $test2,
    ) {
    }

    /**
     * Validation rules for the framework's validator, keyed by the OpenAPI property names — which is
     * what the request payload carries, not the PHP property names.
     *
     * @return array<string, array<int, mixed>>
     */
    public static function rules(): array
    {
        return [
            'test2' => ['present', 'string'],
        ];
    }

    /**
     * @param array<string, mixed> $data the output of `$request->validated()`
     */
    public static function fromValidated(array $data): self
    {
        $dto = new self(
            test2: $data['test2'],
        );

        return $dto;
    }

    public function getTest2(): string
    {
        return $this->test2;
    }

    /**
     * @return array<string, mixed>
     */
    public function toArray(): array
    {
        $result = [];
        $result['test2'] = $this->test2;

        return $result;
    }
}

===== AnimalOneOf3.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 *
 * Laravel mode: the class needs nothing installed — `rules()` is fed to the framework's own
 * validator, and `fromValidated()` builds this object from `$request->validated()`:
 *
 *     final class StoreRequest extends FormRequest
 *     {
 *         public function rules(): array
 *         {
 *             return AnimalOneOf3::rules();
 *         }
 *     }
 *
 *     $dto = AnimalOneOf3::fromValidated($request->validated());
 */
final class AnimalOneOf3 implements Animal
{
    public function __construct(
        private readonly string $test,
    ) {
    }

    /**
     * Validation rules for the framework's validator, keyed by the OpenAPI property names — which is
     * what the request payload carries, not the PHP property names.
     *
     * @return array<string, array<int, mixed>>
     */
    public static function rules(): array
    {
        return [
            'test' => ['present', 'string'],
        ];
    }

    /**
     * @param array<string, mixed> $data the output of `$request->validated()`
     */
    public static function fromValidated(array $data): self
    {
        $dto = new self(
            test: $data['test'],
        );

        return $dto;
    }

    public function getTest(): string
    {
        return $this->test;
    }

    /**
     * @return array<string, mixed>
     */
    public function toArray(): array
    {
        $result = [];
        $result['test'] = $this->test;

        return $result;
    }
}

===== ApiTest2403.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 *
 * Route: GET /api/test2
 *
 * Laravel mode: the class needs nothing installed — `rules()` is fed to the framework's own
 * validator, and `fromValidated()` builds this object from `$request->validated()`:
 *
 *     final class StoreRequest extends FormRequest
 *     {
 *         public function rules(): array
 *         {
 *             return ApiTest2403::rules();
 *         }
 *     }
 *
 *     $dto = ApiTest2403::fromValidated($request->validated());
 */
final class ApiTest2403
{
    public function __construct(
        private readonly string $message,
    ) {
    }

    /**
     * Validation rules for the framework's validator, keyed by the OpenAPI property names — which is
     * what the request payload carries, not the PHP property names.
     *
     * @return array<string, array<int, mixed>>
     */
    public static function rules(): array
    {
        return [
            'message' => ['present', 'string'],
        ];
    }

    /**
     * @param array<string, mixed> $data the output of `$request->validated()`
     */
    public static function fromValidated(array $data): self
    {
        $dto = new self(
            message: $data['message'],
        );

        return $dto;
    }

    public function getMessage(): string
    {
        return $this->message;
    }

    /**
     * @return array<string, mixed>
     */
    public function toArray(): array
    {
        $result = [];
        $result['message'] = $this->message;

        return $result;
    }
}

===== ApiTest2PatchRequest.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use function floor;
use function is_finite;
use function is_float;
/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 *
 * Route: PATCH /api/test2
 *
 * Laravel mode: the class needs nothing installed — `rules()` is fed to the framework's own
 * validator, and `fromValidated()` builds this object from `$request->validated()`:
 *
 *     final class StoreRequest extends FormRequest
 *     {
 *         public function rules(): array
 *         {
 *             return ApiTest2PatchRequest::rules();
 *         }
 *     }
 *
 *     $dto = ApiTest2PatchRequest::fromValidated($request->validated());
 */
final class ApiTest2PatchRequest
{
    public function __construct(
        private readonly int $idPatchTest2,
        private readonly string $namePatchTest2,
    ) {
    }

    /**
     * Validation rules for the framework's validator, keyed by the OpenAPI property names — which is
     * what the request payload carries, not the PHP property names.
     *
     * @return array<string, array<int, mixed>>
     */
    public static function rules(): array
    {
        return [
            'idPatchTest2' => ['present', 'integer'],
            'namePatchTest2' => ['present', 'string'],
        ];
    }

    /**
     * @param array<string, mixed> $data the output of `$request->validated()`
     */
    public static function fromValidated(array $data): self
    {
        $dto = new self(
            idPatchTest2: self::toIntIfIntegral($data['idPatchTest2']),
            namePatchTest2: $data['namePatchTest2'],
        );

        return $dto;
    }

    public function getIdPatchTest2(): int
    {
        return $this->idPatchTest2;
    }

    public function getNamePatchTest2(): string
    {
        return $this->namePatchTest2;
    }

    /**
     * JSON Schema 2020-12 §6.1.1 counts a number with a zero fractional part as an integer, so `42.0` is
     * a valid `integer` payload — and PHP decodes it to a float an `int` property cannot hold. A real
     * fractional part is left alone: validation has already rejected it.
     */
    private static function toIntIfIntegral(mixed $value): mixed
    {
        return is_float($value) && is_finite($value) && floor($value) === $value
            ? (int)$value
            : $value;
    }

    /**
     * @return array<string, mixed>
     */
    public function toArray(): array
    {
        $result = [];
        $result['idPatchTest2'] = $this->idPatchTest2;
        $result['namePatchTest2'] = $this->namePatchTest2;

        return $result;
    }
}

===== ApiTest2PatchRequestFormRequest.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use Illuminate\Foundation\Http\FormRequest;

/**
 * NOTE: This FormRequest is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 *
 * Route: PATCH /api/test2
 *
 * Type-hint it in the controller and Laravel resolves and validates it BEFORE the action body runs; a
 * failure comes out as the framework's own 422 with its error bag:
 *
 *     public function store(ApiTest2PatchRequestFormRequest $request)
 *     {
 *         $dto = $request->toDto();
 *     }
 *
 * **Authorization is not generated.** An OpenAPI document describes payload shape, not policy, so this
 * class defines no `authorize()` — add one if the route needs it.
 */
final class ApiTest2PatchRequestFormRequest extends FormRequest
{
    /**
     * @return array<string, array<int, mixed>>
     */
    public function rules(): array
    {
        return ApiTest2PatchRequest::rules();
    }

    /**
     * The validated payload as the generated DTO. `validated()` returns only the keys that were sent,
     * which is what the DTO's `isXxxProvided()` reads.
     */
    public function toDto(): ApiTest2PatchRequest
    {
        /** @var array<string, mixed> $validated */
        $validated = $this->validated();

        return ApiTest2PatchRequest::fromValidated($validated);
    }
}

===== ApiTest2PostRequest.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use function floor;
use function is_finite;
use function is_float;
/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 *
 * Route: POST /api/test2
 *
 * Laravel mode: the class needs nothing installed — `rules()` is fed to the framework's own
 * validator, and `fromValidated()` builds this object from `$request->validated()`:
 *
 *     final class StoreRequest extends FormRequest
 *     {
 *         public function rules(): array
 *         {
 *             return ApiTest2PostRequest::rules();
 *         }
 *     }
 *
 *     $dto = ApiTest2PostRequest::fromValidated($request->validated());
 */
final class ApiTest2PostRequest
{
    public function __construct(
        private readonly int $idPostTest2,
        private readonly string $namePostTest2,
        private readonly string $surnamePostTest2,
    ) {
    }

    /**
     * Validation rules for the framework's validator, keyed by the OpenAPI property names — which is
     * what the request payload carries, not the PHP property names.
     *
     * @return array<string, array<int, mixed>>
     */
    public static function rules(): array
    {
        return [
            'idPostTest2' => ['present', 'integer'],
            'namePostTest2' => ['present', 'string'],
            'surnamePostTest2' => ['present', 'string'],
        ];
    }

    /**
     * @param array<string, mixed> $data the output of `$request->validated()`
     */
    public static function fromValidated(array $data): self
    {
        $dto = new self(
            idPostTest2: self::toIntIfIntegral($data['idPostTest2']),
            namePostTest2: $data['namePostTest2'],
            surnamePostTest2: $data['surnamePostTest2'],
        );

        return $dto;
    }

    public function getIdPostTest2(): int
    {
        return $this->idPostTest2;
    }

    public function getNamePostTest2(): string
    {
        return $this->namePostTest2;
    }

    public function getSurnamePostTest2(): string
    {
        return $this->surnamePostTest2;
    }

    /**
     * JSON Schema 2020-12 §6.1.1 counts a number with a zero fractional part as an integer, so `42.0` is
     * a valid `integer` payload — and PHP decodes it to a float an `int` property cannot hold. A real
     * fractional part is left alone: validation has already rejected it.
     */
    private static function toIntIfIntegral(mixed $value): mixed
    {
        return is_float($value) && is_finite($value) && floor($value) === $value
            ? (int)$value
            : $value;
    }

    /**
     * @return array<string, mixed>
     */
    public function toArray(): array
    {
        $result = [];
        $result['idPostTest2'] = $this->idPostTest2;
        $result['namePostTest2'] = $this->namePostTest2;
        $result['surnamePostTest2'] = $this->surnamePostTest2;

        return $result;
    }
}

===== ApiTest2PostRequestFormRequest.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use Illuminate\Foundation\Http\FormRequest;

/**
 * NOTE: This FormRequest is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 *
 * Route: POST /api/test2
 *
 * Type-hint it in the controller and Laravel resolves and validates it BEFORE the action body runs; a
 * failure comes out as the framework's own 422 with its error bag:
 *
 *     public function store(ApiTest2PostRequestFormRequest $request)
 *     {
 *         $dto = $request->toDto();
 *     }
 *
 * **Authorization is not generated.** An OpenAPI document describes payload shape, not policy, so this
 * class defines no `authorize()` — add one if the route needs it.
 */
final class ApiTest2PostRequestFormRequest extends FormRequest
{
    /**
     * @return array<string, array<int, mixed>>
     */
    public function rules(): array
    {
        return ApiTest2PostRequest::rules();
    }

    /**
     * The validated payload as the generated DTO. `validated()` returns only the keys that were sent,
     * which is what the DTO's `isXxxProvided()` reads.
     */
    public function toDto(): ApiTest2PostRequest
    {
        /** @var array<string, mixed> $validated */
        $validated = $this->validated();

        return ApiTest2PostRequest::fromValidated($validated);
    }
}

===== ApiTest403.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 *
 * Route: GET /api/test
 *
 * Laravel mode: the class needs nothing installed — `rules()` is fed to the framework's own
 * validator, and `fromValidated()` builds this object from `$request->validated()`:
 *
 *     final class StoreRequest extends FormRequest
 *     {
 *         public function rules(): array
 *         {
 *             return ApiTest403::rules();
 *         }
 *     }
 *
 *     $dto = ApiTest403::fromValidated($request->validated());
 */
final class ApiTest403
{
    public function __construct(
        private readonly string $message,
    ) {
    }

    /**
     * Validation rules for the framework's validator, keyed by the OpenAPI property names — which is
     * what the request payload carries, not the PHP property names.
     *
     * @return array<string, array<int, mixed>>
     */
    public static function rules(): array
    {
        return [
            'message' => ['present', 'string'],
        ];
    }

    /**
     * @param array<string, mixed> $data the output of `$request->validated()`
     */
    public static function fromValidated(array $data): self
    {
        $dto = new self(
            message: $data['message'],
        );

        return $dto;
    }

    public function getMessage(): string
    {
        return $this->message;
    }

    /**
     * @return array<string, mixed>
     */
    public function toArray(): array
    {
        $result = [];
        $result['message'] = $this->message;

        return $result;
    }
}

===== ApiTestCatsActionsGetQueryParams.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use function array_keys;
use function floor;
use function in_array;
use function is_finite;
use function is_float;
/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 *
 * Route: GET /api/test/cats/{id}/actions/{actionId}
 *
 * Laravel mode: the class needs nothing installed — `rules()` is fed to the framework's own
 * validator, and `fromValidated()` builds this object from `$request->validated()`:
 *
 *     final class StoreRequest extends FormRequest
 *     {
 *         public function rules(): array
 *         {
 *             return ApiTestCatsActionsGetQueryParams::rules();
 *         }
 *     }
 *
 *     $dto = ApiTestCatsActionsGetQueryParams::fromValidated($request->validated());
 *
 * `validated()` returns only the keys the payload carried, which is what `isXxxProvided()` reads —
 * so an absent optional field and one sent as `null` stay distinguishable (PATCH semantics).
 */
final class ApiTestCatsActionsGetQueryParams
{
    /**
     * The payload keys that were actually sent, which is what `isXxxProvided()` reads. Not a constructor
     * parameter: the constructor takes the schema's properties and nothing else. `fromValidated()` is the
     * only hydrator and sets this itself; a hand-built instance simply reports nothing as provided.
     *
     * @var array<int, string>
     */
    private array $providedOpenApiKeys = [];

    /**
     * @param string $id Example: 00000000-1111-2222-3333-444455556666
     */
    public function __construct(
        private readonly string $id,
        private readonly int $actionId,
        private readonly ?int $page = null,
        private readonly ?int $limit = null,
    ) {
    }

    /**
     * Validation rules for the framework's validator, keyed by the OpenAPI property names — which is
     * what the request payload carries, not the PHP property names.
     *
     * @return array<string, array<int, mixed>>
     */
    public static function rules(): array
    {
        return [
            'id' => ['present', 'string', 'uuid'],
            'actionId' => ['present', 'integer'],
            'page' => ['sometimes', 'integer', 'min:1'],
            'limit' => ['sometimes', 'integer', 'min:1', 'max:100'],
        ];
    }

    /**
     * @param array<string, mixed> $data the output of `$request->validated()`
     */
    public static function fromValidated(array $data): self
    {
        $dto = new self(
            id: $data['id'],
            actionId: self::toIntIfIntegral($data['actionId']),
            page: ($data['page'] ?? null) === null ? null : self::toIntIfIntegral($data['page']),
            limit: ($data['limit'] ?? null) === null ? null : self::toIntIfIntegral($data['limit']),
        );
        $dto->providedOpenApiKeys = array_keys($data);

        return $dto;
    }

    public function getId(): string
    {
        return $this->id;
    }

    public function getActionId(): int
    {
        return $this->actionId;
    }

    public function getPage(): ?int
    {
        return $this->page;
    }

    public function isPageProvided(): bool
    {
        return in_array('page', $this->providedOpenApiKeys, true);
    }

    public function getLimit(): ?int
    {
        return $this->limit;
    }

    public function isLimitProvided(): bool
    {
        return in_array('limit', $this->providedOpenApiKeys, true);
    }

    /**
     * JSON Schema 2020-12 §6.1.1 counts a number with a zero fractional part as an integer, so `42.0` is
     * a valid `integer` payload — and PHP decodes it to a float an `int` property cannot hold. A real
     * fractional part is left alone: validation has already rejected it.
     */
    private static function toIntIfIntegral(mixed $value): mixed
    {
        return is_float($value) && is_finite($value) && floor($value) === $value
            ? (int)$value
            : $value;
    }

    /**
     * @return array<string, mixed>
     */
    public function toArray(): array
    {
        $result = [];
        $result['id'] = $this->id;
        $result['actionId'] = $this->actionId;

        if ($this->isPageProvided()) {
            $result['page'] = $this->page;
        }

        if ($this->isLimitProvided()) {
            $result['limit'] = $this->limit;
        }

        return $result;
    }
}

===== ApiTestCatsActionsGetQueryParamsFormRequest.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use Illuminate\Foundation\Http\FormRequest;

/**
 * NOTE: This FormRequest is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 *
 * Route: GET /api/test/cats/{id}/actions/{actionId}
 *
 * Type-hint it in the controller and Laravel resolves and validates it BEFORE the action body runs; a
 * failure comes out as the framework's own 422 with its error bag:
 *
 *     public function store(ApiTestCatsActionsGetQueryParamsFormRequest $request)
 *     {
 *         $dto = $request->toDto();
 *     }
 *
 * **Authorization is not generated.** An OpenAPI document describes payload shape, not policy, so this
 * class defines no `authorize()` — add one if the route needs it.
 */
final class ApiTestCatsActionsGetQueryParamsFormRequest extends FormRequest
{
    /**
     * @return array<string, array<int, mixed>>
     */
    public function rules(): array
    {
        return ApiTestCatsActionsGetQueryParams::rules();
    }

    /**
     * The validated payload as the generated DTO. `validated()` returns only the keys that were sent,
     * which is what the DTO's `isXxxProvided()` reads.
     */
    public function toDto(): ApiTestCatsActionsGetQueryParams
    {
        /** @var array<string, mixed> $validated */
        $validated = $this->validated();

        return ApiTestCatsActionsGetQueryParams::fromValidated($validated);
    }
}

===== ApiTestCatsActionsPostQueryParams.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use function array_keys;
use function floor;
use function in_array;
use function is_finite;
use function is_float;
/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 *
 * Route: POST /api/test/cats/{id}/actions/{actionId}
 *
 * Laravel mode: the class needs nothing installed — `rules()` is fed to the framework's own
 * validator, and `fromValidated()` builds this object from `$request->validated()`:
 *
 *     final class StoreRequest extends FormRequest
 *     {
 *         public function rules(): array
 *         {
 *             return ApiTestCatsActionsPostQueryParams::rules();
 *         }
 *     }
 *
 *     $dto = ApiTestCatsActionsPostQueryParams::fromValidated($request->validated());
 *
 * `validated()` returns only the keys the payload carried, which is what `isXxxProvided()` reads —
 * so an absent optional field and one sent as `null` stay distinguishable (PATCH semantics).
 */
final class ApiTestCatsActionsPostQueryParams
{
    /**
     * The payload keys that were actually sent, which is what `isXxxProvided()` reads. Not a constructor
     * parameter: the constructor takes the schema's properties and nothing else. `fromValidated()` is the
     * only hydrator and sets this itself; a hand-built instance simply reports nothing as provided.
     *
     * @var array<int, string>
     */
    private array $providedOpenApiKeys = [];

    /**
     * @param string $id Example: 00000000-1111-2222-3333-444455556666
     */
    public function __construct(
        private readonly string $id,
        private readonly int $actionId,
        private readonly ?int $page = null,
        private readonly ?int $limit = null,
    ) {
    }

    /**
     * Validation rules for the framework's validator, keyed by the OpenAPI property names — which is
     * what the request payload carries, not the PHP property names.
     *
     * @return array<string, array<int, mixed>>
     */
    public static function rules(): array
    {
        return [
            'id' => ['present', 'string', 'uuid'],
            'actionId' => ['present', 'integer'],
            'page' => ['sometimes', 'integer', 'min:1'],
            'limit' => ['sometimes', 'integer', 'min:1', 'max:100'],
        ];
    }

    /**
     * @param array<string, mixed> $data the output of `$request->validated()`
     */
    public static function fromValidated(array $data): self
    {
        $dto = new self(
            id: $data['id'],
            actionId: self::toIntIfIntegral($data['actionId']),
            page: ($data['page'] ?? null) === null ? null : self::toIntIfIntegral($data['page']),
            limit: ($data['limit'] ?? null) === null ? null : self::toIntIfIntegral($data['limit']),
        );
        $dto->providedOpenApiKeys = array_keys($data);

        return $dto;
    }

    public function getId(): string
    {
        return $this->id;
    }

    public function getActionId(): int
    {
        return $this->actionId;
    }

    public function getPage(): ?int
    {
        return $this->page;
    }

    public function isPageProvided(): bool
    {
        return in_array('page', $this->providedOpenApiKeys, true);
    }

    public function getLimit(): ?int
    {
        return $this->limit;
    }

    public function isLimitProvided(): bool
    {
        return in_array('limit', $this->providedOpenApiKeys, true);
    }

    /**
     * JSON Schema 2020-12 §6.1.1 counts a number with a zero fractional part as an integer, so `42.0` is
     * a valid `integer` payload — and PHP decodes it to a float an `int` property cannot hold. A real
     * fractional part is left alone: validation has already rejected it.
     */
    private static function toIntIfIntegral(mixed $value): mixed
    {
        return is_float($value) && is_finite($value) && floor($value) === $value
            ? (int)$value
            : $value;
    }

    /**
     * @return array<string, mixed>
     */
    public function toArray(): array
    {
        $result = [];
        $result['id'] = $this->id;
        $result['actionId'] = $this->actionId;

        if ($this->isPageProvided()) {
            $result['page'] = $this->page;
        }

        if ($this->isLimitProvided()) {
            $result['limit'] = $this->limit;
        }

        return $result;
    }
}

===== ApiTestCatsActionsPostQueryParamsFormRequest.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use Illuminate\Foundation\Http\FormRequest;

/**
 * NOTE: This FormRequest is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 *
 * Route: POST /api/test/cats/{id}/actions/{actionId}
 *
 * Type-hint it in the controller and Laravel resolves and validates it BEFORE the action body runs; a
 * failure comes out as the framework's own 422 with its error bag:
 *
 *     public function store(ApiTestCatsActionsPostQueryParamsFormRequest $request)
 *     {
 *         $dto = $request->toDto();
 *     }
 *
 * **Authorization is not generated.** An OpenAPI document describes payload shape, not policy, so this
 * class defines no `authorize()` — add one if the route needs it.
 */
final class ApiTestCatsActionsPostQueryParamsFormRequest extends FormRequest
{
    /**
     * @return array<string, array<int, mixed>>
     */
    public function rules(): array
    {
        return ApiTestCatsActionsPostQueryParams::rules();
    }

    /**
     * The validated payload as the generated DTO. `validated()` returns only the keys that were sent,
     * which is what the DTO's `isXxxProvided()` reads.
     */
    public function toDto(): ApiTestCatsActionsPostQueryParams
    {
        /** @var array<string, mixed> $validated */
        $validated = $this->validated();

        return ApiTestCatsActionsPostQueryParams::fromValidated($validated);
    }
}

===== ApiTestDogsTagsGetQueryParams.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use function array_keys;
use function floor;
use function in_array;
use function is_finite;
use function is_float;
/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 *
 * Route: GET /api/test/dogs/{id}/tags/{tagId}
 *
 * Laravel mode: the class needs nothing installed — `rules()` is fed to the framework's own
 * validator, and `fromValidated()` builds this object from `$request->validated()`:
 *
 *     final class StoreRequest extends FormRequest
 *     {
 *         public function rules(): array
 *         {
 *             return ApiTestDogsTagsGetQueryParams::rules();
 *         }
 *     }
 *
 *     $dto = ApiTestDogsTagsGetQueryParams::fromValidated($request->validated());
 *
 * `validated()` returns only the keys the payload carried, which is what `isXxxProvided()` reads —
 * so an absent optional field and one sent as `null` stay distinguishable (PATCH semantics).
 */
final class ApiTestDogsTagsGetQueryParams
{
    /**
     * The payload keys that were actually sent, which is what `isXxxProvided()` reads. Not a constructor
     * parameter: the constructor takes the schema's properties and nothing else. `fromValidated()` is the
     * only hydrator and sets this itself; a hand-built instance simply reports nothing as provided.
     *
     * @var array<int, string>
     */
    private array $providedOpenApiKeys = [];

    /**
     * @param string $id Example: 00000000-1111-2222-3333-444455556666
     */
    public function __construct(
        private readonly string $id,
        private readonly int $tagId,
        private readonly ?int $page = null,
        private readonly ?int $limit = null,
    ) {
    }

    /**
     * Validation rules for the framework's validator, keyed by the OpenAPI property names — which is
     * what the request payload carries, not the PHP property names.
     *
     * @return array<string, array<int, mixed>>
     */
    public static function rules(): array
    {
        return [
            'id' => ['present', 'string', 'uuid'],
            'tagId' => ['present', 'integer'],
            'page' => ['sometimes', 'integer', 'min:1'],
            'limit' => ['sometimes', 'integer', 'min:1', 'max:100'],
        ];
    }

    /**
     * @param array<string, mixed> $data the output of `$request->validated()`
     */
    public static function fromValidated(array $data): self
    {
        $dto = new self(
            id: $data['id'],
            tagId: self::toIntIfIntegral($data['tagId']),
            page: ($data['page'] ?? null) === null ? null : self::toIntIfIntegral($data['page']),
            limit: ($data['limit'] ?? null) === null ? null : self::toIntIfIntegral($data['limit']),
        );
        $dto->providedOpenApiKeys = array_keys($data);

        return $dto;
    }

    public function getId(): string
    {
        return $this->id;
    }

    public function getTagId(): int
    {
        return $this->tagId;
    }

    public function getPage(): ?int
    {
        return $this->page;
    }

    public function isPageProvided(): bool
    {
        return in_array('page', $this->providedOpenApiKeys, true);
    }

    public function getLimit(): ?int
    {
        return $this->limit;
    }

    public function isLimitProvided(): bool
    {
        return in_array('limit', $this->providedOpenApiKeys, true);
    }

    /**
     * JSON Schema 2020-12 §6.1.1 counts a number with a zero fractional part as an integer, so `42.0` is
     * a valid `integer` payload — and PHP decodes it to a float an `int` property cannot hold. A real
     * fractional part is left alone: validation has already rejected it.
     */
    private static function toIntIfIntegral(mixed $value): mixed
    {
        return is_float($value) && is_finite($value) && floor($value) === $value
            ? (int)$value
            : $value;
    }

    /**
     * @return array<string, mixed>
     */
    public function toArray(): array
    {
        $result = [];
        $result['id'] = $this->id;
        $result['tagId'] = $this->tagId;

        if ($this->isPageProvided()) {
            $result['page'] = $this->page;
        }

        if ($this->isLimitProvided()) {
            $result['limit'] = $this->limit;
        }

        return $result;
    }
}

===== ApiTestDogsTagsGetQueryParamsFormRequest.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use Illuminate\Foundation\Http\FormRequest;

/**
 * NOTE: This FormRequest is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 *
 * Route: GET /api/test/dogs/{id}/tags/{tagId}
 *
 * Type-hint it in the controller and Laravel resolves and validates it BEFORE the action body runs; a
 * failure comes out as the framework's own 422 with its error bag:
 *
 *     public function store(ApiTestDogsTagsGetQueryParamsFormRequest $request)
 *     {
 *         $dto = $request->toDto();
 *     }
 *
 * **Authorization is not generated.** An OpenAPI document describes payload shape, not policy, so this
 * class defines no `authorize()` — add one if the route needs it.
 */
final class ApiTestDogsTagsGetQueryParamsFormRequest extends FormRequest
{
    /**
     * @return array<string, array<int, mixed>>
     */
    public function rules(): array
    {
        return ApiTestDogsTagsGetQueryParams::rules();
    }

    /**
     * The validated payload as the generated DTO. `validated()` returns only the keys that were sent,
     * which is what the DTO's `isXxxProvided()` reads.
     */
    public function toDto(): ApiTestDogsTagsGetQueryParams
    {
        /** @var array<string, mixed> $validated */
        $validated = $this->validated();

        return ApiTestDogsTagsGetQueryParams::fromValidated($validated);
    }
}

===== ApiTestDogsTagsPatchQueryParams.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use function array_keys;
use function floor;
use function in_array;
use function is_finite;
use function is_float;
/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 *
 * Route: PATCH /api/test/dogs/{id}/tags/{tagId}
 *
 * Laravel mode: the class needs nothing installed — `rules()` is fed to the framework's own
 * validator, and `fromValidated()` builds this object from `$request->validated()`:
 *
 *     final class StoreRequest extends FormRequest
 *     {
 *         public function rules(): array
 *         {
 *             return ApiTestDogsTagsPatchQueryParams::rules();
 *         }
 *     }
 *
 *     $dto = ApiTestDogsTagsPatchQueryParams::fromValidated($request->validated());
 *
 * `validated()` returns only the keys the payload carried, which is what `isXxxProvided()` reads —
 * so an absent optional field and one sent as `null` stay distinguishable (PATCH semantics).
 */
final class ApiTestDogsTagsPatchQueryParams
{
    /**
     * The payload keys that were actually sent, which is what `isXxxProvided()` reads. Not a constructor
     * parameter: the constructor takes the schema's properties and nothing else. `fromValidated()` is the
     * only hydrator and sets this itself; a hand-built instance simply reports nothing as provided.
     *
     * @var array<int, string>
     */
    private array $providedOpenApiKeys = [];

    /**
     * @param string $id Example: 00000000-1111-2222-3333-444455556666
     */
    public function __construct(
        private readonly string $id,
        private readonly int $tagId,
        private readonly ?int $page = null,
        private readonly ?int $limit = null,
    ) {
    }

    /**
     * Validation rules for the framework's validator, keyed by the OpenAPI property names — which is
     * what the request payload carries, not the PHP property names.
     *
     * @return array<string, array<int, mixed>>
     */
    public static function rules(): array
    {
        return [
            'id' => ['present', 'string', 'uuid'],
            'tagId' => ['present', 'integer'],
            'page' => ['sometimes', 'integer', 'min:1'],
            'limit' => ['sometimes', 'integer', 'min:1', 'max:100'],
        ];
    }

    /**
     * @param array<string, mixed> $data the output of `$request->validated()`
     */
    public static function fromValidated(array $data): self
    {
        $dto = new self(
            id: $data['id'],
            tagId: self::toIntIfIntegral($data['tagId']),
            page: ($data['page'] ?? null) === null ? null : self::toIntIfIntegral($data['page']),
            limit: ($data['limit'] ?? null) === null ? null : self::toIntIfIntegral($data['limit']),
        );
        $dto->providedOpenApiKeys = array_keys($data);

        return $dto;
    }

    public function getId(): string
    {
        return $this->id;
    }

    public function getTagId(): int
    {
        return $this->tagId;
    }

    public function getPage(): ?int
    {
        return $this->page;
    }

    public function isPageProvided(): bool
    {
        return in_array('page', $this->providedOpenApiKeys, true);
    }

    public function getLimit(): ?int
    {
        return $this->limit;
    }

    public function isLimitProvided(): bool
    {
        return in_array('limit', $this->providedOpenApiKeys, true);
    }

    /**
     * JSON Schema 2020-12 §6.1.1 counts a number with a zero fractional part as an integer, so `42.0` is
     * a valid `integer` payload — and PHP decodes it to a float an `int` property cannot hold. A real
     * fractional part is left alone: validation has already rejected it.
     */
    private static function toIntIfIntegral(mixed $value): mixed
    {
        return is_float($value) && is_finite($value) && floor($value) === $value
            ? (int)$value
            : $value;
    }

    /**
     * @return array<string, mixed>
     */
    public function toArray(): array
    {
        $result = [];
        $result['id'] = $this->id;
        $result['tagId'] = $this->tagId;

        if ($this->isPageProvided()) {
            $result['page'] = $this->page;
        }

        if ($this->isLimitProvided()) {
            $result['limit'] = $this->limit;
        }

        return $result;
    }
}

===== ApiTestDogsTagsPatchQueryParamsFormRequest.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use Illuminate\Foundation\Http\FormRequest;

/**
 * NOTE: This FormRequest is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 *
 * Route: PATCH /api/test/dogs/{id}/tags/{tagId}
 *
 * Type-hint it in the controller and Laravel resolves and validates it BEFORE the action body runs; a
 * failure comes out as the framework's own 422 with its error bag:
 *
 *     public function store(ApiTestDogsTagsPatchQueryParamsFormRequest $request)
 *     {
 *         $dto = $request->toDto();
 *     }
 *
 * **Authorization is not generated.** An OpenAPI document describes payload shape, not policy, so this
 * class defines no `authorize()` — add one if the route needs it.
 */
final class ApiTestDogsTagsPatchQueryParamsFormRequest extends FormRequest
{
    /**
     * @return array<string, array<int, mixed>>
     */
    public function rules(): array
    {
        return ApiTestDogsTagsPatchQueryParams::rules();
    }

    /**
     * The validated payload as the generated DTO. `validated()` returns only the keys that were sent,
     * which is what the DTO's `isXxxProvided()` reads.
     */
    public function toDto(): ApiTestDogsTagsPatchQueryParams
    {
        /** @var array<string, mixed> $validated */
        $validated = $this->validated();

        return ApiTestDogsTagsPatchQueryParams::fromValidated($validated);
    }
}

===== ApiTestFileDeleteQueryParams.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 *
 * Route: DELETE /api/test/file/{id}
 *
 * Laravel mode: the class needs nothing installed — `rules()` is fed to the framework's own
 * validator, and `fromValidated()` builds this object from `$request->validated()`:
 *
 *     final class StoreRequest extends FormRequest
 *     {
 *         public function rules(): array
 *         {
 *             return ApiTestFileDeleteQueryParams::rules();
 *         }
 *     }
 *
 *     $dto = ApiTestFileDeleteQueryParams::fromValidated($request->validated());
 */
final class ApiTestFileDeleteQueryParams
{
    /**
     * @param string $id Example: 00000000-1111-2222-3333-444455556666
     */
    public function __construct(
        private readonly string $id,
    ) {
    }

    /**
     * Validation rules for the framework's validator, keyed by the OpenAPI property names — which is
     * what the request payload carries, not the PHP property names.
     *
     * @return array<string, array<int, mixed>>
     */
    public static function rules(): array
    {
        return [
            'id' => ['present', 'string', 'uuid'],
        ];
    }

    /**
     * @param array<string, mixed> $data the output of `$request->validated()`
     */
    public static function fromValidated(array $data): self
    {
        $dto = new self(
            id: $data['id'],
        );

        return $dto;
    }

    public function getId(): string
    {
        return $this->id;
    }

    /**
     * @return array<string, mixed>
     */
    public function toArray(): array
    {
        $result = [];
        $result['id'] = $this->id;

        return $result;
    }
}

===== ApiTestFileDeleteQueryParamsFormRequest.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use Illuminate\Foundation\Http\FormRequest;

/**
 * NOTE: This FormRequest is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 *
 * Route: DELETE /api/test/file/{id}
 *
 * Type-hint it in the controller and Laravel resolves and validates it BEFORE the action body runs; a
 * failure comes out as the framework's own 422 with its error bag:
 *
 *     public function store(ApiTestFileDeleteQueryParamsFormRequest $request)
 *     {
 *         $dto = $request->toDto();
 *     }
 *
 * **Authorization is not generated.** An OpenAPI document describes payload shape, not policy, so this
 * class defines no `authorize()` — add one if the route needs it.
 */
final class ApiTestFileDeleteQueryParamsFormRequest extends FormRequest
{
    /**
     * @return array<string, array<int, mixed>>
     */
    public function rules(): array
    {
        return ApiTestFileDeleteQueryParams::rules();
    }

    /**
     * The validated payload as the generated DTO. `validated()` returns only the keys that were sent,
     * which is what the DTO's `isXxxProvided()` reads.
     */
    public function toDto(): ApiTestFileDeleteQueryParams
    {
        /** @var array<string, mixed> $validated */
        $validated = $this->validated();

        return ApiTestFileDeleteQueryParams::fromValidated($validated);
    }
}

===== ApiTestFilePatchQueryParams.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 *
 * Route: PATCH /api/test/file/{id}
 *
 * Laravel mode: the class needs nothing installed — `rules()` is fed to the framework's own
 * validator, and `fromValidated()` builds this object from `$request->validated()`:
 *
 *     final class StoreRequest extends FormRequest
 *     {
 *         public function rules(): array
 *         {
 *             return ApiTestFilePatchQueryParams::rules();
 *         }
 *     }
 *
 *     $dto = ApiTestFilePatchQueryParams::fromValidated($request->validated());
 */
final class ApiTestFilePatchQueryParams
{
    /**
     * @param string $id Example: 00000000-1111-2222-3333-444455556666
     */
    public function __construct(
        private readonly string $id,
    ) {
    }

    /**
     * Validation rules for the framework's validator, keyed by the OpenAPI property names — which is
     * what the request payload carries, not the PHP property names.
     *
     * @return array<string, array<int, mixed>>
     */
    public static function rules(): array
    {
        return [
            'id' => ['present', 'string', 'uuid'],
        ];
    }

    /**
     * @param array<string, mixed> $data the output of `$request->validated()`
     */
    public static function fromValidated(array $data): self
    {
        $dto = new self(
            id: $data['id'],
        );

        return $dto;
    }

    public function getId(): string
    {
        return $this->id;
    }

    /**
     * @return array<string, mixed>
     */
    public function toArray(): array
    {
        $result = [];
        $result['id'] = $this->id;

        return $result;
    }
}

===== ApiTestFilePatchQueryParamsFormRequest.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use Illuminate\Foundation\Http\FormRequest;

/**
 * NOTE: This FormRequest is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 *
 * Route: PATCH /api/test/file/{id}
 *
 * Type-hint it in the controller and Laravel resolves and validates it BEFORE the action body runs; a
 * failure comes out as the framework's own 422 with its error bag:
 *
 *     public function store(ApiTestFilePatchQueryParamsFormRequest $request)
 *     {
 *         $dto = $request->toDto();
 *     }
 *
 * **Authorization is not generated.** An OpenAPI document describes payload shape, not policy, so this
 * class defines no `authorize()` — add one if the route needs it.
 */
final class ApiTestFilePatchQueryParamsFormRequest extends FormRequest
{
    /**
     * @return array<string, array<int, mixed>>
     */
    public function rules(): array
    {
        return ApiTestFilePatchQueryParams::rules();
    }

    /**
     * The validated payload as the generated DTO. `validated()` returns only the keys that were sent,
     * which is what the DTO's `isXxxProvided()` reads.
     */
    public function toDto(): ApiTestFilePatchQueryParams
    {
        /** @var array<string, mixed> $validated */
        $validated = $this->validated();

        return ApiTestFilePatchQueryParams::fromValidated($validated);
    }
}

===== ApiTestFilePostRequest.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use Symfony\Component\HttpFoundation\File\UploadedFile;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 *
 * Route: POST /api/test/file
 *
 * Laravel mode: the class needs nothing installed — `rules()` is fed to the framework's own
 * validator, and `fromValidated()` builds this object from `$request->validated()`:
 *
 *     final class StoreRequest extends FormRequest
 *     {
 *         public function rules(): array
 *         {
 *             return ApiTestFilePostRequest::rules();
 *         }
 *     }
 *
 *     $dto = ApiTestFilePostRequest::fromValidated($request->validated());
 */
final class ApiTestFilePostRequest
{
    public function __construct(
        private readonly UploadedFile $file,
    ) {
    }

    /**
     * Validation rules for the framework's validator, keyed by the OpenAPI property names — which is
     * what the request payload carries, not the PHP property names.
     *
     * @return array<string, array<int, mixed>>
     */
    public static function rules(): array
    {
        return [
            'file' => ['present', 'file'],
        ];
    }

    /**
     * @param array<string, mixed> $data the output of `$request->validated()`
     */
    public static function fromValidated(array $data): self
    {
        $dto = new self(
            file: $data['file'],
        );

        return $dto;
    }

    public function getFile(): UploadedFile
    {
        return $this->file;
    }

    /**
     * @return array<string, mixed>
     */
    public function toArray(): array
    {
        $result = [];
        $result['file'] = $this->file;

        return $result;
    }
}

===== ApiTestFilePostRequestFormRequest.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use Illuminate\Foundation\Http\FormRequest;

/**
 * NOTE: This FormRequest is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 *
 * Route: POST /api/test/file
 *
 * Type-hint it in the controller and Laravel resolves and validates it BEFORE the action body runs; a
 * failure comes out as the framework's own 422 with its error bag:
 *
 *     public function store(ApiTestFilePostRequestFormRequest $request)
 *     {
 *         $dto = $request->toDto();
 *     }
 *
 * **Authorization is not generated.** An OpenAPI document describes payload shape, not policy, so this
 * class defines no `authorize()` — add one if the route needs it.
 */
final class ApiTestFilePostRequestFormRequest extends FormRequest
{
    /**
     * @return array<string, array<int, mixed>>
     */
    public function rules(): array
    {
        return ApiTestFilePostRequest::rules();
    }

    /**
     * The validated payload as the generated DTO. `validated()` returns only the keys that were sent,
     * which is what the DTO's `isXxxProvided()` reads.
     */
    public function toDto(): ApiTestFilePostRequest
    {
        /** @var array<string, mixed> $validated */
        $validated = $this->validated();

        return ApiTestFilePostRequest::fromValidated($validated);
    }
}

===== Cat.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 *
 * Laravel mode: the class needs nothing installed — `rules()` is fed to the framework's own
 * validator, and `fromValidated()` builds this object from `$request->validated()`:
 *
 *     final class StoreRequest extends FormRequest
 *     {
 *         public function rules(): array
 *         {
 *             return Cat::rules();
 *         }
 *     }
 *
 *     $dto = Cat::fromValidated($request->validated());
 */
final class Cat implements Animal, Animal2
{
    public function __construct(
        private readonly string $name,
        private readonly string $meow,
    ) {
    }

    /**
     * Validation rules for the framework's validator, keyed by the OpenAPI property names — which is
     * what the request payload carries, not the PHP property names.
     *
     * @return array<string, array<int, mixed>>
     */
    public static function rules(): array
    {
        return [
            'name' => ['present', 'string'],
            'meow' => ['present', 'string'],
        ];
    }

    /**
     * @param array<string, mixed> $data the output of `$request->validated()`
     */
    public static function fromValidated(array $data): self
    {
        $dto = new self(
            name: $data['name'],
            meow: $data['meow'],
        );

        return $dto;
    }

    public function getName(): string
    {
        return $this->name;
    }

    public function getMeow(): string
    {
        return $this->meow;
    }

    /**
     * @return array<string, mixed>
     */
    public function toArray(): array
    {
        $result = [];
        $result['name'] = $this->name;
        $result['meow'] = $this->meow;

        return $result;
    }
}

===== Discriminator.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use Illuminate\Validation\Rule;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 *
 * Laravel mode: the class needs nothing installed — `rules()` is fed to the framework's own
 * validator, and `fromValidated()` builds this object from `$request->validated()`:
 *
 *     final class StoreRequest extends FormRequest
 *     {
 *         public function rules(): array
 *         {
 *             return Discriminator::rules();
 *         }
 *     }
 *
 *     $dto = Discriminator::fromValidated($request->validated());
 */
final class Discriminator
{
    public function __construct(
        private readonly DiscriminatorType $type,
        private readonly ?string $name,
    ) {
    }

    /**
     * Validation rules for the framework's validator, keyed by the OpenAPI property names — which is
     * what the request payload carries, not the PHP property names.
     *
     * @return array<string, array<int, mixed>>
     */
    public static function rules(): array
    {
        return [
            'type' => ['present', Rule::enum(DiscriminatorType::class)],
            'name' => ['present', 'nullable', 'string'],
        ];
    }

    /**
     * @param array<string, mixed> $data the output of `$request->validated()`
     */
    public static function fromValidated(array $data): self
    {
        $dto = new self(
            type: DiscriminatorType::from($data['type']),
            name: ($data['name'] ?? null) === null ? null : $data['name'],
        );

        return $dto;
    }

    public function getType(): DiscriminatorType
    {
        return $this->type;
    }

    public function getName(): ?string
    {
        return $this->name;
    }

    /**
     * @return array<string, mixed>
     */
    public function toArray(): array
    {
        $result = [];
        $result['type'] = $this->type->value;
        $result['name'] = $this->name;

        return $result;
    }
}

===== Discriminator1.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use Illuminate\Validation\Rule;

use function floor;
use function is_finite;
use function is_float;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 *
 * Laravel mode: the class needs nothing installed — `rules()` is fed to the framework's own
 * validator, and `fromValidated()` builds this object from `$request->validated()`:
 *
 *     final class StoreRequest extends FormRequest
 *     {
 *         public function rules(): array
 *         {
 *             return Discriminator1::rules();
 *         }
 *     }
 *
 *     $dto = Discriminator1::fromValidated($request->validated());
 */
final class Discriminator1
{
    public function __construct(
        private readonly DiscriminatorType $type,
        private readonly ?string $name,
        private readonly int $id,
    ) {
    }

    /**
     * Validation rules for the framework's validator, keyed by the OpenAPI property names — which is
     * what the request payload carries, not the PHP property names.
     *
     * @return array<string, array<int, mixed>>
     */
    public static function rules(): array
    {
        return [
            'type' => ['present', Rule::enum(DiscriminatorType::class)],
            'name' => ['present', 'nullable', 'string'],
            'id' => ['present', 'integer'],
        ];
    }

    /**
     * @param array<string, mixed> $data the output of `$request->validated()`
     */
    public static function fromValidated(array $data): self
    {
        $dto = new self(
            type: DiscriminatorType::from($data['type']),
            name: ($data['name'] ?? null) === null ? null : $data['name'],
            id: self::toIntIfIntegral($data['id']),
        );

        return $dto;
    }

    public function getType(): DiscriminatorType
    {
        return $this->type;
    }

    public function getName(): ?string
    {
        return $this->name;
    }

    public function getId(): int
    {
        return $this->id;
    }

    /**
     * JSON Schema 2020-12 §6.1.1 counts a number with a zero fractional part as an integer, so `42.0` is
     * a valid `integer` payload — and PHP decodes it to a float an `int` property cannot hold. A real
     * fractional part is left alone: validation has already rejected it.
     */
    private static function toIntIfIntegral(mixed $value): mixed
    {
        return is_float($value) && is_finite($value) && floor($value) === $value
            ? (int)$value
            : $value;
    }

    /**
     * @return array<string, mixed>
     */
    public function toArray(): array
    {
        $result = [];
        $result['type'] = $this->type->value;
        $result['name'] = $this->name;
        $result['id'] = $this->id;

        return $result;
    }
}

===== Discriminator2.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use Illuminate\Validation\Rule;

use function floor;
use function is_finite;
use function is_float;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 *
 * Laravel mode: the class needs nothing installed — `rules()` is fed to the framework's own
 * validator, and `fromValidated()` builds this object from `$request->validated()`:
 *
 *     final class StoreRequest extends FormRequest
 *     {
 *         public function rules(): array
 *         {
 *             return Discriminator2::rules();
 *         }
 *     }
 *
 *     $dto = Discriminator2::fromValidated($request->validated());
 */
final class Discriminator2
{
    public function __construct(
        private readonly DiscriminatorType $type,
        private readonly ?string $name,
        private readonly int $id,
        private readonly string $nAme2,
    ) {
    }

    /**
     * Validation rules for the framework's validator, keyed by the OpenAPI property names — which is
     * what the request payload carries, not the PHP property names.
     *
     * @return array<string, array<int, mixed>>
     */
    public static function rules(): array
    {
        return [
            'type' => ['present', Rule::enum(DiscriminatorType::class)],
            'name' => ['present', 'nullable', 'string'],
            'id' => ['present', 'integer'],
            'NAme' => ['present', 'string'],
        ];
    }

    /**
     * @param array<string, mixed> $data the output of `$request->validated()`
     */
    public static function fromValidated(array $data): self
    {
        $dto = new self(
            type: DiscriminatorType::from($data['type']),
            name: ($data['name'] ?? null) === null ? null : $data['name'],
            id: self::toIntIfIntegral($data['id']),
            nAme2: $data['NAme'],
        );

        return $dto;
    }

    public function getType(): DiscriminatorType
    {
        return $this->type;
    }

    public function getName(): ?string
    {
        return $this->name;
    }

    public function getId(): int
    {
        return $this->id;
    }

    public function getNAme2(): string
    {
        return $this->nAme2;
    }

    /**
     * JSON Schema 2020-12 §6.1.1 counts a number with a zero fractional part as an integer, so `42.0` is
     * a valid `integer` payload — and PHP decodes it to a float an `int` property cannot hold. A real
     * fractional part is left alone: validation has already rejected it.
     */
    private static function toIntIfIntegral(mixed $value): mixed
    {
        return is_float($value) && is_finite($value) && floor($value) === $value
            ? (int)$value
            : $value;
    }

    /**
     * @return array<string, mixed>
     */
    public function toArray(): array
    {
        $result = [];
        $result['type'] = $this->type->value;
        $result['name'] = $this->name;
        $result['id'] = $this->id;
        $result['NAme'] = $this->nAme2;

        return $result;
    }
}

===== DiscriminatorType.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

/**
 * NOTE: This enum is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 *
 * From: Discriminator.type
 */
enum DiscriminatorType: string
{
    case DISCRIMINATOR1 = 'discriminator1';
    case DISCRIMINATOR2 = 'discriminator2';
}

===== Dog.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 *
 * Laravel mode: the class needs nothing installed — `rules()` is fed to the framework's own
 * validator, and `fromValidated()` builds this object from `$request->validated()`:
 *
 *     final class StoreRequest extends FormRequest
 *     {
 *         public function rules(): array
 *         {
 *             return Dog::rules();
 *         }
 *     }
 *
 *     $dto = Dog::fromValidated($request->validated());
 */
final class Dog implements Animal, Animal2
{
    public function __construct(
        private readonly string $name,
        private readonly string $bark,
    ) {
    }

    /**
     * Validation rules for the framework's validator, keyed by the OpenAPI property names — which is
     * what the request payload carries, not the PHP property names.
     *
     * @return array<string, array<int, mixed>>
     */
    public static function rules(): array
    {
        return [
            'name' => ['present', 'string'],
            'bark' => ['present', 'string'],
        ];
    }

    /**
     * @param array<string, mixed> $data the output of `$request->validated()`
     */
    public static function fromValidated(array $data): self
    {
        $dto = new self(
            name: $data['name'],
            bark: $data['bark'],
        );

        return $dto;
    }

    public function getName(): string
    {
        return $this->name;
    }

    public function getBark(): string
    {
        return $this->bark;
    }

    /**
     * @return array<string, mixed>
     */
    public function toArray(): array
    {
        $result = [];
        $result['name'] = $this->name;
        $result['bark'] = $this->bark;

        return $result;
    }
}

===== Dog2.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 *
 * Laravel mode: the class needs nothing installed — `rules()` is fed to the framework's own
 * validator, and `fromValidated()` builds this object from `$request->validated()`:
 *
 *     final class StoreRequest extends FormRequest
 *     {
 *         public function rules(): array
 *         {
 *             return Dog2::rules();
 *         }
 *     }
 *
 *     $dto = Dog2::fromValidated($request->validated());
 */
final class Dog2 implements Animal2
{
    public function __construct(
        private readonly string $name,
        private readonly string $bark,
    ) {
    }

    /**
     * Validation rules for the framework's validator, keyed by the OpenAPI property names — which is
     * what the request payload carries, not the PHP property names.
     *
     * @return array<string, array<int, mixed>>
     */
    public static function rules(): array
    {
        return [
            'name' => ['present', 'string'],
            'bark' => ['present', 'string'],
        ];
    }

    /**
     * @param array<string, mixed> $data the output of `$request->validated()`
     */
    public static function fromValidated(array $data): self
    {
        $dto = new self(
            name: $data['name'],
            bark: $data['bark'],
        );

        return $dto;
    }

    public function getName(): string
    {
        return $this->name;
    }

    public function getBark(): string
    {
        return $this->bark;
    }

    /**
     * @return array<string, mixed>
     */
    public function toArray(): array
    {
        $result = [];
        $result['name'] = $this->name;
        $result['bark'] = $this->bark;

        return $result;
    }
}

===== FileRequest.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use Symfony\Component\HttpFoundation\File\UploadedFile;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 *
 * Laravel mode: the class needs nothing installed — `rules()` is fed to the framework's own
 * validator, and `fromValidated()` builds this object from `$request->validated()`:
 *
 *     final class StoreRequest extends FormRequest
 *     {
 *         public function rules(): array
 *         {
 *             return FileRequest::rules();
 *         }
 *     }
 *
 *     $dto = FileRequest::fromValidated($request->validated());
 */
final class FileRequest
{
    public function __construct(
        private readonly UploadedFile $file,
        private readonly string $name,
        private readonly string $surname,
    ) {
    }

    /**
     * Validation rules for the framework's validator, keyed by the OpenAPI property names — which is
     * what the request payload carries, not the PHP property names.
     *
     * @return array<string, array<int, mixed>>
     */
    public static function rules(): array
    {
        return [
            'file' => ['present', 'file'],
            'name' => ['present', 'string'],
            'surname' => ['present', 'string'],
        ];
    }

    /**
     * @param array<string, mixed> $data the output of `$request->validated()`
     */
    public static function fromValidated(array $data): self
    {
        $dto = new self(
            file: $data['file'],
            name: $data['name'],
            surname: $data['surname'],
        );

        return $dto;
    }

    public function getFile(): UploadedFile
    {
        return $this->file;
    }

    public function getName(): string
    {
        return $this->name;
    }

    public function getSurname(): string
    {
        return $this->surname;
    }

    /**
     * @return array<string, mixed>
     */
    public function toArray(): array
    {
        $result = [];
        $result['file'] = $this->file;
        $result['name'] = $this->name;
        $result['surname'] = $this->surname;

        return $result;
    }
}

===== FileRequestFormRequest.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use Illuminate\Foundation\Http\FormRequest;

/**
 * NOTE: This FormRequest is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 *
 * Type-hint it in the controller and Laravel resolves and validates it BEFORE the action body runs; a
 * failure comes out as the framework's own 422 with its error bag:
 *
 *     public function store(FileRequestFormRequest $request)
 *     {
 *         $dto = $request->toDto();
 *     }
 *
 * **Authorization is not generated.** An OpenAPI document describes payload shape, not policy, so this
 * class defines no `authorize()` — add one if the route needs it.
 */
final class FileRequestFormRequest extends FormRequest
{
    /**
     * @return array<string, array<int, mixed>>
     */
    public function rules(): array
    {
        return FileRequest::rules();
    }

    /**
     * The validated payload as the generated DTO. `validated()` returns only the keys that were sent,
     * which is what the DTO's `isXxxProvided()` reads.
     */
    public function toDto(): FileRequest
    {
        /** @var array<string, mixed> $validated */
        $validated = $this->validated();

        return FileRequest::fromValidated($validated);
    }
}

===== Pet.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 *
 * Laravel mode: the class needs nothing installed — `rules()` is fed to the framework's own
 * validator, and `fromValidated()` builds this object from `$request->validated()`:
 *
 *     final class StoreRequest extends FormRequest
 *     {
 *         public function rules(): array
 *         {
 *             return Pet::rules();
 *         }
 *     }
 *
 *     $dto = Pet::fromValidated($request->validated());
 */
final class Pet
{
    public function __construct(
        private readonly string $name,
    ) {
    }

    /**
     * Validation rules for the framework's validator, keyed by the OpenAPI property names — which is
     * what the request payload carries, not the PHP property names.
     *
     * @return array<string, array<int, mixed>>
     */
    public static function rules(): array
    {
        return [
            'name' => ['present', 'string'],
        ];
    }

    /**
     * @param array<string, mixed> $data the output of `$request->validated()`
     */
    public static function fromValidated(array $data): self
    {
        $dto = new self(
            name: $data['name'],
        );

        return $dto;
    }

    public function getName(): string
    {
        return $this->name;
    }

    /**
     * @return array<string, mixed>
     */
    public function toArray(): array
    {
        $result = [];
        $result['name'] = $this->name;

        return $result;
    }
}

===== Tag.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use function floor;
use function is_finite;
use function is_float;
/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 *
 * Laravel mode: the class needs nothing installed — `rules()` is fed to the framework's own
 * validator, and `fromValidated()` builds this object from `$request->validated()`:
 *
 *     final class StoreRequest extends FormRequest
 *     {
 *         public function rules(): array
 *         {
 *             return Tag::rules();
 *         }
 *     }
 *
 *     $dto = Tag::fromValidated($request->validated());
 */
final class Tag
{
    public function __construct(
        private readonly int $id,
        private readonly string $name,
    ) {
    }

    /**
     * Validation rules for the framework's validator, keyed by the OpenAPI property names — which is
     * what the request payload carries, not the PHP property names.
     *
     * @return array<string, array<int, mixed>>
     */
    public static function rules(): array
    {
        return [
            'id' => ['present', 'integer'],
            'name' => ['present', 'string'],
        ];
    }

    /**
     * @param array<string, mixed> $data the output of `$request->validated()`
     */
    public static function fromValidated(array $data): self
    {
        $dto = new self(
            id: self::toIntIfIntegral($data['id']),
            name: $data['name'],
        );

        return $dto;
    }

    public function getId(): int
    {
        return $this->id;
    }

    public function getName(): string
    {
        return $this->name;
    }

    /**
     * JSON Schema 2020-12 §6.1.1 counts a number with a zero fractional part as an integer, so `42.0` is
     * a valid `integer` payload — and PHP decodes it to a float an `int` property cannot hold. A real
     * fractional part is left alone: validation has already rejected it.
     */
    private static function toIntIfIntegral(mixed $value): mixed
    {
        return is_float($value) && is_finite($value) && floor($value) === $value
            ? (int)$value
            : $value;
    }

    /**
     * @return array<string, mixed>
     */
    public function toArray(): array
    {
        $result = [];
        $result['id'] = $this->id;
        $result['name'] = $this->name;

        return $result;
    }
}

===== Test.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use function floor;
use function is_finite;
use function is_float;
/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 *
 * Laravel mode: the class needs nothing installed — `rules()` is fed to the framework's own
 * validator, and `fromValidated()` builds this object from `$request->validated()`:
 *
 *     final class StoreRequest extends FormRequest
 *     {
 *         public function rules(): array
 *         {
 *             return Test::rules();
 *         }
 *     }
 *
 *     $dto = Test::fromValidated($request->validated());
 */
final class Test
{
    public function __construct(
        private readonly int $id,
    ) {
    }

    /**
     * Validation rules for the framework's validator, keyed by the OpenAPI property names — which is
     * what the request payload carries, not the PHP property names.
     *
     * @return array<string, array<int, mixed>>
     */
    public static function rules(): array
    {
        return [
            'id' => ['present', 'integer'],
        ];
    }

    /**
     * @param array<string, mixed> $data the output of `$request->validated()`
     */
    public static function fromValidated(array $data): self
    {
        $dto = new self(
            id: self::toIntIfIntegral($data['id']),
        );

        return $dto;
    }

    public function getId(): int
    {
        return $this->id;
    }

    /**
     * JSON Schema 2020-12 §6.1.1 counts a number with a zero fractional part as an integer, so `42.0` is
     * a valid `integer` payload — and PHP decodes it to a float an `int` property cannot hold. A real
     * fractional part is left alone: validation has already rejected it.
     */
    private static function toIntIfIntegral(mixed $value): mixed
    {
        return is_float($value) && is_finite($value) && floor($value) === $value
            ? (int)$value
            : $value;
    }

    /**
     * @return array<string, mixed>
     */
    public function toArray(): array
    {
        $result = [];
        $result['id'] = $this->id;

        return $result;
    }
}

===== Test2.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use Illuminate\Validation\Rule;
use Illuminate\Validation\Validator;
use stdClass;

use function array_filter;
use function array_is_list;
use function array_key_exists;
use function array_keys;
use function array_map;
use function array_shift;
use function explode;
use function floor;
use function get_object_vars;
use function in_array;
use function is_array;
use function is_finite;
use function is_float;
use function is_object;
use function is_string;
use function json_decode;
use function property_exists;
use function sprintf;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 *
 * Laravel mode: the class needs nothing installed — `rules()` is fed to the framework's own
 * validator, and `fromValidated()` builds this object from `$request->validated()`:
 *
 *     final class StoreRequest extends FormRequest
 *     {
 *         public function rules(): array
 *         {
 *             return Test2::rules();
 *         }
 *     }
 *
 *     $dto = Test2::fromValidated($request->validated());
 *
 * `validated()` returns only the keys the payload carried, which is what `isXxxProvided()` reads —
 * so an absent optional field and one sent as `null` stay distinguishable (PATCH semantics).
 */
final class Test2
{
    private const array OPENAPI_VALIDATION_CONSTRAINTS = [
        'tags' => [
            'items' => [
                'required' => [
                    'id',
                    'name',
                ],
            ],
        ],
        'user' => [
            'required' => [
                'id',
                'name',
                'pet',
                'createdAt',
            ],
            'nullable' => true,
        ],
    ];

    private const int OPENAPI_MAX_VALIDATION_DEPTH = 256;

    /**
     * The payload keys that were actually sent, which is what `isXxxProvided()` reads. Not a constructor
     * parameter: the constructor takes the schema's properties and nothing else. `fromValidated()` is the
     * only hydrator and sets this itself; a hand-built instance simply reports nothing as provided.
     *
     * @var array<int, string>
     */
    private array $providedOpenApiKeys = [];

    /**
     * @param int $id this is id
     * @param string $name this is name
     * @param array<Tag> $tags
     */
    public function __construct(
        private readonly int $id,
        private readonly string $name,
        private readonly array $tags,
        private readonly Test2Types $types,
        private readonly ?User $user = null,
        private readonly ?Test2UserType $userType = null,
    ) {
    }

    /**
     * Validation rules for the framework's validator, keyed by the OpenAPI property names — which is
     * what the request payload carries, not the PHP property names.
     *
     * @return array<string, array<int, mixed>>
     */
    public static function rules(): array
    {
        return [
            'id' => ['present', 'integer'],
            'name' => ['present', 'string'],
            'tags' => ['present', 'array', 'list'],
            'tags.*.id' => ['integer'],
            'tags.*.name' => ['string'],
            'types' => ['present', Rule::enum(Test2Types::class)],
            'user' => ['sometimes'],
            'user.id' => ['integer'],
            'user.name' => ['string'],
            'user.pet' => ['nullable'],
            'user.createdAt' => ['date_format:Y-m-d\TH:i:sP,Y-m-d\TH:i:s.uP,Y-m-d H:i:s,Y-m-d\TH:i:s'],
            'userType' => ['sometimes', 'array'],
            'userType.region' => ['string'],
        ];
    }

    /**
     * Paths whose schema is `type: object`. A `*` segment matches every index of an array.
     *
     * @var array<int, string>
     */
    private const array OPENAPI_OBJECT_PATHS = [
        'tags.*',
        'user',
        'user.pet',
        'userType',
    ];

    /**
     * The half of the schema Laravel's rule vocabulary cannot express — composition, conditionals,
     * `contains`, `unevaluated*`, `content*`, `propertyNames`, `discriminator` — checked against the
     * raw payload the validator already holds.
     *
     * Hook it up once, in the FormRequest:
     *
     *     public function withValidator(Validator $validator): void
     *     {
     *         Test2::withValidator($validator, $this->getContent());
     *     }
     *
     * `$rawJson` is the UNDECODED body. `type: object` versus `type: array` is a property of the wire
     * shape, and PHP loses it — a JSON object keyed 0..n-1 and a JSON array become the same list — so
     * that one check reads the raw body. Omit the argument and it is skipped; everything else still runs.
     */
    public static function withValidator(Validator $validator, ?string $rawJson = null): void
    {
        $validator->after(static function (Validator $validator) use ($rawJson): void {
            /** @var array<string, mixed> $payload */
            $payload = $validator->getData();

            foreach (self::OPENAPI_VALIDATION_CONSTRAINTS as $property => $schema) {
                if (!array_key_exists($property, $payload)) {
                    continue;
                }

                foreach (self::validateOpenApiNode($payload[$property], $schema, $property, 0) as $error) {
                    $validator->errors()->add($property, $error);
                }
            }

            if ($rawJson !== null && $rawJson !== '') {
                self::assertRawObjectShapes($validator, $rawJson);
            }
        });
    }

    /**
     * Adds an error for every `type: object` path the raw body sent as a JSON array.
     */
    private static function assertRawObjectShapes(Validator $validator, string $rawJson): void
    {
        // Decoded WITHOUT the assoc flag on purpose: that is what keeps a JSON object apart from a
        // JSON array. Laravel's own `$validator->getData()` cannot answer this question.
        $decoded = json_decode($rawJson, false);
        if (!$decoded instanceof stdClass) {
            return;
        }

        foreach (self::OPENAPI_OBJECT_PATHS as $path) {
            foreach (self::rawNodesAt($decoded, explode('.', $path), '') as $foundPath => $node) {
                if (is_array($node)) {
                    $validator->errors()->add($foundPath, sprintf('%s expects object, got array', $foundPath));
                }
            }
        }
    }

    /**
     * Every node a (possibly wildcarded) path reaches, keyed by the concrete path that got there.
     *
     * @param array<int, string> $segments
     * @return array<string, mixed>
     */
    private static function rawNodesAt(mixed $node, array $segments, string $path): array
    {
        if ($segments === []) {
            return [$path => $node];
        }

        $segment = array_shift($segments);
        if ($segment === '*') {
            // `*` stands for both an array index and a map key, so a JSON object node is walked too.
            if ($node instanceof stdClass) {
                $node = get_object_vars($node);
            }
            if (!is_array($node)) {
                return [];
            }

            $found = [];
            foreach ($node as $index => $item) {
                $itemPath = $path === '' ? (string)$index : $path . '.' . $index;
                foreach (self::rawNodesAt($item, $segments, $itemPath) as $deepPath => $deepNode) {
                    $found[$deepPath] = $deepNode;
                }
            }

            return $found;
        }

        if (!$node instanceof stdClass || !property_exists($node, $segment)) {
            return [];
        }

        return self::rawNodesAt($node->{$segment}, $segments, $path === '' ? $segment : $path . '.' . $segment);
    }

    /**
     * @param array<string, mixed> $data the output of `$request->validated()`
     */
    public static function fromValidated(array $data): self
    {
        $dto = new self(
            id: self::toIntIfIntegral($data['id']),
            name: $data['name'],
            tags: array_map(static fn(array $item): Tag => Tag::fromValidated($item), $data['tags']),
            types: Test2Types::from($data['types']),
            user: ($data['user'] ?? null) === null ? null : User::fromValidated($data['user']),
            userType: ($data['userType'] ?? null) === null ? null : Test2UserType::fromValidated($data['userType']),
        );
        $dto->providedOpenApiKeys = array_keys($data);

        return $dto;
    }

    public function getId(): int
    {
        return $this->id;
    }

    public function getName(): string
    {
        return $this->name;
    }

    /**
     * @return array<Tag>
     */
    public function getTags(): array
    {
        return $this->tags;
    }

    public function getTypes(): Test2Types
    {
        return $this->types;
    }

    public function getUser(): ?User
    {
        return $this->user;
    }

    public function isUserProvided(): bool
    {
        return in_array('user', $this->providedOpenApiKeys, true);
    }

    public function getUserType(): ?Test2UserType
    {
        return $this->userType;
    }

    public function isUserTypeProvided(): bool
    {
        return in_array('userType', $this->providedOpenApiKeys, true);
    }

    /**
     * @param array<string, mixed> $schema
     * @return array<int, string>
     */
    private static function validateOpenApiNode(mixed $value, array $schema, string $path, int $depth): array
    {
        if ($depth >= self::OPENAPI_MAX_VALIDATION_DEPTH) {
            return [];
        }

        $errors = [];
        if ($value === null && ($schema['nullable'] ?? false) === true) {
            return [];
        }

        $structuredValue = self::normalizeOpenApiStructuralValue($value);

        if (is_array($structuredValue) && is_array($schema['required'] ?? null)) {
            foreach ($schema['required'] as $requiredProp) {
                if (is_string($requiredProp) && !array_key_exists($requiredProp, $structuredValue)) {
                    $errors[] = sprintf('%s is required', self::openApiChildPath($path, $requiredProp));
                }
            }
        }

        $listValue = is_array($value) && array_is_list($value);

        if ($listValue && is_array($schema['items'] ?? null)) {
            foreach ($value as $index => $itemValue) {
                $errors = [
                    ...$errors,
                    ...self::validateOpenApiNode($itemValue, $schema['items'], $path . '[' . $index . ']', $depth + 1),
                ];
            }
        }

        return $errors;
    }

    private static function normalizeOpenApiStructuralValue(mixed $value): mixed
    {
        if (is_array($value) || !is_object($value)) {
            return $value;
        }

        // Any other object: only its public state is visible, under PHP property names.
        return array_filter(
            get_object_vars($value),
            static fn(mixed $propertyValue): bool => $propertyValue !== null,
        );
    }

    private static function openApiChildPath(string $path, string $propertyName): string
    {
        // The first level names the field the way the runtime validator's caller does, so both
        // modes report the same subject: `field "tags" must ...`.
        return $path === 'payload'
            ? sprintf('field "%s"', $propertyName)
            : $path . '.' . $propertyName;
    }

    /**
     * JSON Schema 2020-12 §6.1.1 counts a number with a zero fractional part as an integer, so `42.0` is
     * a valid `integer` payload — and PHP decodes it to a float an `int` property cannot hold. A real
     * fractional part is left alone: validation has already rejected it.
     */
    private static function toIntIfIntegral(mixed $value): mixed
    {
        return is_float($value) && is_finite($value) && floor($value) === $value
            ? (int)$value
            : $value;
    }

    /**
     * @return array<string, mixed>
     */
    public function toArray(): array
    {
        $result = [];
        $result['id'] = $this->id;
        $result['name'] = $this->name;
        $result['tags'] = array_map(static fn(Tag $item): array => $item->toArray(), $this->tags);
        $result['types'] = $this->types->value;

        if ($this->isUserProvided()) {
            $result['user'] = $this->user?->toArray();
        }

        if ($this->isUserTypeProvided()) {
            $result['userType'] = $this->userType?->toArray();
        }

        return $result;
    }
}

===== Test2Types.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

/**
 * NOTE: This enum is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 *
 * From: Test2.types
 */
enum Test2Types: string
{
    case CAT = 'cat';
    case DOG = 'dog';
}

===== Test2UserType.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use function array_keys;
use function in_array;
/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 * From: Test2.userType
 *
 * Laravel mode: the class needs nothing installed — `rules()` is fed to the framework's own
 * validator, and `fromValidated()` builds this object from `$request->validated()`:
 *
 *     final class StoreRequest extends FormRequest
 *     {
 *         public function rules(): array
 *         {
 *             return Test2UserType::rules();
 *         }
 *     }
 *
 *     $dto = Test2UserType::fromValidated($request->validated());
 *
 * `validated()` returns only the keys the payload carried, which is what `isXxxProvided()` reads —
 * so an absent optional field and one sent as `null` stay distinguishable (PATCH semantics).
 */
final class Test2UserType
{
    /**
     * The payload keys that were actually sent, which is what `isXxxProvided()` reads. Not a constructor
     * parameter: the constructor takes the schema's properties and nothing else. `fromValidated()` is the
     * only hydrator and sets this itself; a hand-built instance simply reports nothing as provided.
     *
     * @var array<int, string>
     */
    private array $providedOpenApiKeys = [];

    public function __construct(
        private readonly ?string $region = null,
    ) {
    }

    /**
     * Validation rules for the framework's validator, keyed by the OpenAPI property names — which is
     * what the request payload carries, not the PHP property names.
     *
     * @return array<string, array<int, mixed>>
     */
    public static function rules(): array
    {
        return [
            'region' => ['sometimes', 'string'],
        ];
    }

    /**
     * @param array<string, mixed> $data the output of `$request->validated()`
     */
    public static function fromValidated(array $data): self
    {
        $dto = new self(
            region: ($data['region'] ?? null) === null ? null : $data['region'],
        );
        $dto->providedOpenApiKeys = array_keys($data);

        return $dto;
    }

    public function getRegion(): ?string
    {
        return $this->region;
    }

    public function isRegionProvided(): bool
    {
        return in_array('region', $this->providedOpenApiKeys, true);
    }

    /**
     * @return array<string, mixed>
     */
    public function toArray(): array
    {
        $result = [];

        if ($this->isRegionProvided()) {
            $result['region'] = $this->region;
        }

        return $result;
    }
}

===== TestPatchRequest.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use function floor;
use function is_finite;
use function is_float;
/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 *
 * Laravel mode: the class needs nothing installed — `rules()` is fed to the framework's own
 * validator, and `fromValidated()` builds this object from `$request->validated()`:
 *
 *     final class StoreRequest extends FormRequest
 *     {
 *         public function rules(): array
 *         {
 *             return TestPatchRequest::rules();
 *         }
 *     }
 *
 *     $dto = TestPatchRequest::fromValidated($request->validated());
 */
final class TestPatchRequest
{
    public function __construct(
        private readonly int $id,
        private readonly string $name,
        private readonly string $surname,
    ) {
    }

    /**
     * Validation rules for the framework's validator, keyed by the OpenAPI property names — which is
     * what the request payload carries, not the PHP property names.
     *
     * @return array<string, array<int, mixed>>
     */
    public static function rules(): array
    {
        return [
            'id' => ['present', 'integer'],
            'name' => ['present', 'string'],
            'surname' => ['present', 'string'],
        ];
    }

    /**
     * @param array<string, mixed> $data the output of `$request->validated()`
     */
    public static function fromValidated(array $data): self
    {
        $dto = new self(
            id: self::toIntIfIntegral($data['id']),
            name: $data['name'],
            surname: $data['surname'],
        );

        return $dto;
    }

    public function getId(): int
    {
        return $this->id;
    }

    public function getName(): string
    {
        return $this->name;
    }

    public function getSurname(): string
    {
        return $this->surname;
    }

    /**
     * JSON Schema 2020-12 §6.1.1 counts a number with a zero fractional part as an integer, so `42.0` is
     * a valid `integer` payload — and PHP decodes it to a float an `int` property cannot hold. A real
     * fractional part is left alone: validation has already rejected it.
     */
    private static function toIntIfIntegral(mixed $value): mixed
    {
        return is_float($value) && is_finite($value) && floor($value) === $value
            ? (int)$value
            : $value;
    }

    /**
     * @return array<string, mixed>
     */
    public function toArray(): array
    {
        $result = [];
        $result['id'] = $this->id;
        $result['name'] = $this->name;
        $result['surname'] = $this->surname;

        return $result;
    }
}

===== TestPatchRequestFormRequest.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use Illuminate\Foundation\Http\FormRequest;

/**
 * NOTE: This FormRequest is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 *
 * Type-hint it in the controller and Laravel resolves and validates it BEFORE the action body runs; a
 * failure comes out as the framework's own 422 with its error bag:
 *
 *     public function store(TestPatchRequestFormRequest $request)
 *     {
 *         $dto = $request->toDto();
 *     }
 *
 * **Authorization is not generated.** An OpenAPI document describes payload shape, not policy, so this
 * class defines no `authorize()` — add one if the route needs it.
 */
final class TestPatchRequestFormRequest extends FormRequest
{
    /**
     * @return array<string, array<int, mixed>>
     */
    public function rules(): array
    {
        return TestPatchRequest::rules();
    }

    /**
     * The validated payload as the generated DTO. `validated()` returns only the keys that were sent,
     * which is what the DTO's `isXxxProvided()` reads.
     */
    public function toDto(): TestPatchRequest
    {
        /** @var array<string, mixed> $validated */
        $validated = $this->validated();

        return TestPatchRequest::fromValidated($validated);
    }
}

===== TestPostRequest.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use Illuminate\Validation\Validator;

use function array_filter;
use function array_is_list;
use function array_key_exists;
use function array_keys;
use function array_pop;
use function array_unique;
use function array_values;
use function floor;
use function get_object_vars;
use function implode;
use function in_array;
use function is_array;
use function is_bool;
use function is_finite;
use function is_float;
use function is_int;
use function is_object;
use function is_scalar;
use function is_string;
use function json_encode;
use function ltrim;
use function preg_match;
use function preg_replace;
use function property_exists;
use function sprintf;
use function str_contains;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 *
 * Laravel mode: the class needs nothing installed — `rules()` is fed to the framework's own
 * validator, and `fromValidated()` builds this object from `$request->validated()`:
 *
 *     final class StoreRequest extends FormRequest
 *     {
 *         public function rules(): array
 *         {
 *             return TestPostRequest::rules();
 *         }
 *     }
 *
 *     $dto = TestPostRequest::fromValidated($request->validated());
 *
 * `validated()` returns only the keys the payload carried, which is what `isXxxProvided()` reads —
 * so an absent optional field and one sent as `null` stay distinguishable (PATCH semantics).
 */
final class TestPostRequest
{
    private const array OPENAPI_VALIDATION_CONSTRAINTS = [
        'code' => [
            'oneOf' => [
                [
                    'type' => 'integer',
                    'minimum' => 10,
                    'maximum' => 100,
                ],
                [
                    'type' => 'string',
                    'format' => 'uuid',
                ],
            ],
        ],
        'endpoint' => [
            'format' => 'uri-template',
        ],
    ];

    private const int OPENAPI_MAX_VALIDATION_DEPTH = 256;

    /**
     * The payload keys that were actually sent, which is what `isXxxProvided()` reads. Not a constructor
     * parameter: the constructor takes the schema's properties and nothing else. `fromValidated()` is the
     * only hydrator and sets this itself; a hand-built instance simply reports nothing as provided.
     *
     * @var array<int, string>
     */
    private array $providedOpenApiKeys = [];

    public function __construct(
        private readonly int $id,
        private readonly string $name,
        private readonly int|string $code,
        private readonly ?string $homepage = null,
        private readonly ?string $endpoint = null,
    ) {
    }

    /**
     * Validation rules for the framework's validator, keyed by the OpenAPI property names — which is
     * what the request payload carries, not the PHP property names.
     *
     * @return array<string, array<int, mixed>>
     */
    public static function rules(): array
    {
        return [
            'id' => ['present', 'integer'],
            'name' => ['present', 'string'],
            'code' => ['present'],
            'homepage' => ['sometimes', 'string', 'url'],
            'endpoint' => ['sometimes', 'string'],
        ];
    }

    /**
     * The half of the schema Laravel's rule vocabulary cannot express — composition, conditionals,
     * `contains`, `unevaluated*`, `content*`, `propertyNames`, `discriminator` — checked against the
     * raw payload the validator already holds.
     *
     * Hook it up once, in the FormRequest:
     *
     *     public function withValidator(Validator $validator): void
     *     {
     *         TestPostRequest::withValidator($validator, $this->getContent());
     *     }
     *
     */
    public static function withValidator(Validator $validator, ?string $rawJson = null): void
    {
        $validator->after(static function (Validator $validator) use ($rawJson): void {
            /** @var array<string, mixed> $payload */
            $payload = $validator->getData();

            foreach (self::OPENAPI_VALIDATION_CONSTRAINTS as $property => $schema) {
                if (!array_key_exists($property, $payload)) {
                    continue;
                }

                foreach (self::validateOpenApiNode($payload[$property], $schema, $property, 0) as $error) {
                    $validator->errors()->add($property, $error);
                }
            }
        });
    }

    /**
     * @param array<string, mixed> $data the output of `$request->validated()`
     */
    public static function fromValidated(array $data): self
    {
        $dto = new self(
            id: self::toIntIfIntegral($data['id']),
            name: $data['name'],
            code: self::toIntIfIntegral($data['code']),
            homepage: ($data['homepage'] ?? null) === null ? null : $data['homepage'],
            endpoint: ($data['endpoint'] ?? null) === null ? null : $data['endpoint'],
        );
        $dto->providedOpenApiKeys = array_keys($data);

        return $dto;
    }

    public function getId(): int
    {
        return $this->id;
    }

    public function getName(): string
    {
        return $this->name;
    }

    public function getCode(): int|string
    {
        return $this->code;
    }

    public function getHomepage(): ?string
    {
        return $this->homepage;
    }

    public function isHomepageProvided(): bool
    {
        return in_array('homepage', $this->providedOpenApiKeys, true);
    }

    public function getEndpoint(): ?string
    {
        return $this->endpoint;
    }

    public function isEndpointProvided(): bool
    {
        return in_array('endpoint', $this->providedOpenApiKeys, true);
    }

    /**
     * @param array<string, mixed> $schema
     * @return array<int, string>
     */
    private static function validateOpenApiNode(mixed $value, array $schema, string $path, int $depth): array
    {
        if ($depth >= self::OPENAPI_MAX_VALIDATION_DEPTH) {
            return [];
        }

        $errors = [];
        $normalizedValue = $value;
        $structuredValue = self::normalizeOpenApiStructuralValue($value);

        if (is_array($schema['oneOf'] ?? null)) {
            $matchingBranches = 0;
            $branchErrors = [];
            foreach ($schema['oneOf'] as $branch) {
                if (!is_array($branch)) {
                    continue;
                }

                if (is_string($branchClass = $branch['x-php-instanceof'] ?? null) && $branchClass !== '') {
                    $branchFqcn = __NAMESPACE__ . '\\' . ltrim($branchClass, '\\');
                    if (is_object($value) && $value instanceof $branchFqcn) {
                        $matchingBranches++;
                    }
                    continue;
                }

                if (array_key_exists('type', $branch) && !self::matchesOpenApiCallbackType($normalizedValue, $branch['type'])) {
                    continue;
                }
                // Keep why each branch failed: with no match at all those reasons are far more
                // useful than "does not match any oneOf branch".
                $errorsForBranch = self::validateOpenApiNode($value, $branch, $path, $depth + 1);
                if ($errorsForBranch === []) {
                    $matchingBranches++;
                    continue;
                }

                $branchErrors = [...$branchErrors, ...$errorsForBranch];
            }

            if ($matchingBranches > 1) {
                $errors[] = sprintf('%s matches more than one allowed oneOf branch', $path);
            } elseif ($matchingBranches === 0) {
                $errors = $branchErrors === []
                    ? [...$errors, self::describeOpenApiUnionMismatch($path, 'oneOf', $schema['oneOf'], $value)]
                    : [...$errors, ...array_values(array_unique($branchErrors))];
            }

            if ($matchingBranches === 1) {
                $discriminatorProperty = is_string($schema['x-discriminator-property'] ?? null)
                    ? $schema['x-discriminator-property']
                    : null;
                $discriminatorPhpProperty = is_string($schema['x-discriminator-php-property'] ?? null)
                    ? $schema['x-discriminator-php-property']
                    : null;
                $discriminatorMap = is_array($schema['x-discriminator-map'] ?? null)
                    ? $schema['x-discriminator-map']
                    : null;

                if ($discriminatorProperty !== null && $discriminatorMap !== null && is_object($value)) {
                    $phpProperty = $discriminatorPhpProperty ?? $discriminatorProperty;
                    // The DTO keeps its state private: read the discriminator from the payload it
                    // reports, keyed by the OpenAPI name, and fall back to a public property for a
                    // hand-written object.
                    $discriminatorPayload = self::normalizeOpenApiStructuralValue($value);
                    $discriminatorCarrier = is_array($discriminatorPayload) ? $discriminatorPayload : [];
                    if (!array_key_exists($discriminatorProperty, $discriminatorCarrier) && !property_exists($value, $phpProperty)) {
                        $errors[] = sprintf('%s discriminator property %s is required', $path, $discriminatorProperty);
                    } else {
                        $discriminatorValue = $discriminatorCarrier[$discriminatorProperty]
                            ?? (property_exists($value, $phpProperty) ? $value->{$phpProperty} : null);
                        $mapKey = is_scalar($discriminatorValue) ? (string)$discriminatorValue : null;
                        if ($mapKey === null || !array_key_exists($mapKey, $discriminatorMap)) {
                            $errors[] = sprintf(
                                '%s discriminator %s must be one of: %s',
                                $path,
                                $discriminatorProperty,
                                implode(', ', array_keys($discriminatorMap)),
                            );
                        } elseif (is_string($expectedClass = $discriminatorMap[$mapKey]) && $expectedClass !== '') {
                            $expectedFqcn = __NAMESPACE__ . '\\' . ltrim($expectedClass, '\\');
                            if (!$value instanceof $expectedFqcn) {
                                $errors[] = sprintf('%s discriminator %s must match concrete class', $path, $discriminatorProperty);
                            }
                        }
                    }
                }
            }
        }

        if (array_key_exists('type', $schema)) {
            $typeConstraint = $schema['type'];
            if (is_string($typeConstraint)) {
                if (!self::matchesOpenApiCallbackType($normalizedValue, $typeConstraint)) {
                    $errors[] = sprintf('%s must be of type %s', $path, $typeConstraint);
                }
            }
        }

        if ((is_int($normalizedValue) || is_float($normalizedValue)) && self::schemaHasAnyOpenApiKey($schema, ['minimum', 'maximum'])) {
            $minimum = self::toFloatConstraint($schema['minimum'] ?? null);
            if ($minimum !== null && !($normalizedValue >= $minimum)) {
                $errors[] = sprintf('%s must be greater than or equal to %s', $path, self::stringifyOpenApiNumber($minimum));
            }
            $maximum = self::toFloatConstraint($schema['maximum'] ?? null);
            if ($maximum !== null && !($normalizedValue <= $maximum)) {
                $errors[] = sprintf('%s must be less than or equal to %s', $path, self::stringifyOpenApiNumber($maximum));
            }
        }

        if (is_string($normalizedValue) && self::schemaHasAnyOpenApiKey($schema, ['format'])) {
            if (is_string($format = $schema['format'] ?? null) && !self::isValidOpenApiStringFormat($normalizedValue, $format)) {
                $errors[] = sprintf('%s must match format %s', $path, $format);
            }
        }

        return $errors;
    }

    /**
     * @param array<string, mixed> $schema
     */
    private static function matchesOpenApiNode(mixed $value, array $schema, int $depth): bool
    {
        return self::validateOpenApiNode($value, $schema, 'payload', $depth) === [];
    }

    /**
     * Every branch was gated out by its declared type, so no branch has a reason to report. Naming the
     * types the union does accept turns an unactionable sentence into one the caller can act on. Same
     * wording as the runtime validator, so a payload reads the same whichever mode answered it.
     *
     * @param array<int, mixed> $branches
     */
    private static function describeOpenApiUnionMismatch(string $path, string $kind, array $branches, mixed $value): string
    {
        $types = [];
        foreach ($branches as $branch) {
            if (!is_array($branch)) {
                continue;
            }

            $declared = is_array($branch['type'] ?? null) ? $branch['type'] : [$branch['type'] ?? null];
            foreach ($declared as $type) {
                if (is_string($type) && $type !== '' && !in_array($type, $types, true)) {
                    $types[] = $type;
                }
            }
        }

        if ($types === []) {
            return sprintf('%s does not match any %s branch', $path, $kind);
        }

        $last = array_pop($types);
        $expected = $types === [] ? $last : implode(', ', $types) . ' or ' . $last;

        return sprintf(
            '%s does not match any %s branch (expected %s, got %s)',
            $path,
            $kind,
            $expected,
            match (true) {
                $value === null => 'null',
                is_bool($value) => 'boolean',
                is_int($value) => 'integer',
                is_float($value) => 'number',
                is_string($value) => 'string',
                is_array($value) => array_is_list($value) ? 'array' : 'object',
                default => 'object',
            },
        );
    }

    private static function matchesOpenApiCallbackType(mixed $value, mixed $type): bool
    {
        if (!is_string($type) || $type === '') {
            return true;
        }

        return match ($type) {
            'integer' => is_int($value) || (is_float($value) && is_finite($value) && floor($value) === $value),
            'string' => is_string($value),
            default => true,
        };
    }

    private static function normalizeOpenApiStructuralValue(mixed $value): mixed
    {
        if (is_array($value) || !is_object($value)) {
            return $value;
        }

        // Any other object: only its public state is visible, under PHP property names.
        return array_filter(
            get_object_vars($value),
            static fn(mixed $propertyValue): bool => $propertyValue !== null,
        );
    }

    /**
     * @param array<int, string> $keys
     */
    private static function schemaHasAnyOpenApiKey(array $schema, array $keys): bool
    {
        foreach ($keys as $key) {
            if (array_key_exists($key, $schema)) {
                return true;
            }
        }

        return false;
    }

    private static function toFloatConstraint(mixed $value): ?float
    {
        return is_int($value) || is_float($value) ? (float)$value : null;
    }

    private static function stringifyOpenApiNumber(float $value): string
    {
        $rendered = json_encode($value);
        return is_string($rendered) ? $rendered : (string)$value;
    }

    private static function isValidOpenApiStringFormat(string $value, string $format): bool
    {
        return match ($format) {
            'uuid' => self::isValidOpenApiUuid($value),
            'uri-template' => self::isValidOpenApiUriTemplate($value),
            default => true,
        };
    }

    private static function isValidOpenApiUuid(string $value): bool
    {
        return preg_match('/^[0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}$/', $value) === 1;
    }

    private static function isValidOpenApiUriReference(string $value): bool
    {
        return preg_match('/[\s\x00-\x1F\x7F]/u', $value) !== 1;
    }

    private static function isValidOpenApiUriTemplate(string $value): bool
    {
        // RFC 6570: a URI-reference that may embed {expression} blocks. Every brace has to belong to a
        // well-formed expression — an optional operator followed by a comma-separated list of varspecs,
        // each a varname with an optional ":" prefix-length or "*" explode modifier. Strip the valid
        // expressions; a surviving brace means the template is malformed.
        if (!self::isValidOpenApiUriReference($value)) {
            return false;
        }

        if (!str_contains($value, '{')) {
            return !str_contains($value, '}');
        }

        $expression = '\{[+#./;?&=,!@|]?(?:[\p{L}\p{N}_%]+(?::[1-9][0-9]{0,3}|\*)?)'
            . '(?:,[\p{L}\p{N}_%]+(?::[1-9][0-9]{0,3}|\*)?)*\}';
        $stripped = preg_replace('~' . $expression . '~u', '', $value);

        return $stripped !== null && !str_contains($stripped, '{') && !str_contains($stripped, '}');
    }

    /**
     * JSON Schema 2020-12 §6.1.1 counts a number with a zero fractional part as an integer, so `42.0` is
     * a valid `integer` payload — and PHP decodes it to a float an `int` property cannot hold. A real
     * fractional part is left alone: validation has already rejected it.
     */
    private static function toIntIfIntegral(mixed $value): mixed
    {
        return is_float($value) && is_finite($value) && floor($value) === $value
            ? (int)$value
            : $value;
    }

    /**
     * @return array<string, mixed>
     */
    public function toArray(): array
    {
        $result = [];
        $result['id'] = $this->id;
        $result['name'] = $this->name;
        $result['code'] = $this->code;

        if ($this->isHomepageProvided()) {
            $result['homepage'] = $this->homepage;
        }

        if ($this->isEndpointProvided()) {
            $result['endpoint'] = $this->endpoint;
        }

        return $result;
    }
}

===== TestPostRequestFormRequest.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use Illuminate\Foundation\Http\FormRequest;
use Illuminate\Validation\Validator;

/**
 * NOTE: This FormRequest is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 *
 * Type-hint it in the controller and Laravel resolves and validates it BEFORE the action body runs; a
 * failure comes out as the framework's own 422 with its error bag:
 *
 *     public function store(TestPostRequestFormRequest $request)
 *     {
 *         $dto = $request->toDto();
 *     }
 *
 * **Authorization is not generated.** An OpenAPI document describes payload shape, not policy, so this
 * class defines no `authorize()` — add one if the route needs it.
 */
final class TestPostRequestFormRequest extends FormRequest
{
    /**
     * @return array<string, array<int, mixed>>
     */
    public function rules(): array
    {
        return TestPostRequest::rules();
    }

    /**
     * The half of the schema Laravel's rule vocabulary cannot express — composition, conditionals,
     * `contains`, `unevaluated*`, `content*`, `propertyNames`, `discriminator`. It runs against the raw
     * payload, so an undeclared key is still visible to `additionalProperties: false`.
     */
    public function withValidator(Validator $validator): void
    {
        // The raw body goes along: `type: object` versus `type: array` is decided by the wire shape,
        // and the decoded payload Laravel hands the validator no longer carries it.
        TestPostRequest::withValidator($validator, $this->getContent());
    }

    /**
     * The validated payload as the generated DTO. `validated()` returns only the keys that were sent,
     * which is what the DTO's `isXxxProvided()` reads.
     */
    public function toDto(): TestPostRequest
    {
        /** @var array<string, mixed> $validated */
        $validated = $this->validated();

        return TestPostRequest::fromValidated($validated);
    }
}

===== TypeEnum.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

/**
 * NOTE: This enum is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 */
enum TypeEnum: string
{
    case CAT_TYPE = 'catType';
    case DOG_TYPE = 'dogType';
}

===== User.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use DateTimeImmutable;
use Illuminate\Validation\Validator;
use stdClass;

use function array_shift;
use function explode;
use function floor;
use function get_object_vars;
use function is_array;
use function is_finite;
use function is_float;
use function json_decode;
use function property_exists;
use function sprintf;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 *
 * Laravel mode: the class needs nothing installed — `rules()` is fed to the framework's own
 * validator, and `fromValidated()` builds this object from `$request->validated()`:
 *
 *     final class StoreRequest extends FormRequest
 *     {
 *         public function rules(): array
 *         {
 *             return User::rules();
 *         }
 *     }
 *
 *     $dto = User::fromValidated($request->validated());
 */
final class User
{
    public function __construct(
        private readonly int $id,
        private readonly string $name,
        private readonly ?Animal2 $pet,
        private readonly DateTimeImmutable $createdAt,
    ) {
    }

    /**
     * Validation rules for the framework's validator, keyed by the OpenAPI property names — which is
     * what the request payload carries, not the PHP property names.
     *
     * @return array<string, array<int, mixed>>
     */
    public static function rules(): array
    {
        return [
            'id' => ['present', 'integer'],
            'name' => ['present', 'string'],
            'pet' => ['present', 'nullable'],
            'createdAt' => ['present', 'date_format:Y-m-d\TH:i:sP,Y-m-d\TH:i:s.uP,Y-m-d H:i:s,Y-m-d\TH:i:s'],
        ];
    }

    /**
     * Paths whose schema is `type: object`. A `*` segment matches every index of an array.
     *
     * @var array<int, string>
     */
    private const array OPENAPI_OBJECT_PATHS = [
        'pet',
    ];

    /**
     * The one shape Laravel's rules cannot check: `type: object` given a JSON array.
     *
     * Hook it up once, in the FormRequest:
     *
     *     public function withValidator(Validator $validator): void
     *     {
     *         User::withValidator($validator, $this->getContent());
     *     }
     *
     * `$rawJson` is the UNDECODED body. `type: object` versus `type: array` is a property of the wire
     * shape, and PHP loses it — a JSON object keyed 0..n-1 and a JSON array become the same list — so
     * that one check reads the raw body. Omit the argument and it is skipped; everything else still runs.
     */
    public static function withValidator(Validator $validator, ?string $rawJson = null): void
    {
        $validator->after(static function (Validator $validator) use ($rawJson): void {
            if ($rawJson !== null && $rawJson !== '') {
                self::assertRawObjectShapes($validator, $rawJson);
            }
        });
    }

    /**
     * Adds an error for every `type: object` path the raw body sent as a JSON array.
     */
    private static function assertRawObjectShapes(Validator $validator, string $rawJson): void
    {
        // Decoded WITHOUT the assoc flag on purpose: that is what keeps a JSON object apart from a
        // JSON array. Laravel's own `$validator->getData()` cannot answer this question.
        $decoded = json_decode($rawJson, false);
        if (!$decoded instanceof stdClass) {
            return;
        }

        foreach (self::OPENAPI_OBJECT_PATHS as $path) {
            foreach (self::rawNodesAt($decoded, explode('.', $path), '') as $foundPath => $node) {
                if (is_array($node)) {
                    $validator->errors()->add($foundPath, sprintf('%s expects object, got array', $foundPath));
                }
            }
        }
    }

    /**
     * Every node a (possibly wildcarded) path reaches, keyed by the concrete path that got there.
     *
     * @param array<int, string> $segments
     * @return array<string, mixed>
     */
    private static function rawNodesAt(mixed $node, array $segments, string $path): array
    {
        if ($segments === []) {
            return [$path => $node];
        }

        $segment = array_shift($segments);
        if ($segment === '*') {
            // `*` stands for both an array index and a map key, so a JSON object node is walked too.
            if ($node instanceof stdClass) {
                $node = get_object_vars($node);
            }
            if (!is_array($node)) {
                return [];
            }

            $found = [];
            foreach ($node as $index => $item) {
                $itemPath = $path === '' ? (string)$index : $path . '.' . $index;
                foreach (self::rawNodesAt($item, $segments, $itemPath) as $deepPath => $deepNode) {
                    $found[$deepPath] = $deepNode;
                }
            }

            return $found;
        }

        if (!$node instanceof stdClass || !property_exists($node, $segment)) {
            return [];
        }

        return self::rawNodesAt($node->{$segment}, $segments, $path === '' ? $segment : $path . '.' . $segment);
    }

    /**
     * @param array<string, mixed> $data the output of `$request->validated()`
     */
    public static function fromValidated(array $data): self
    {
        $dto = new self(
            id: self::toIntIfIntegral($data['id']),
            name: $data['name'],
            pet: ($data['pet'] ?? null) === null ? null : Animal2::fromValidated($data['pet']),
            createdAt: new DateTimeImmutable($data['createdAt']),
        );

        return $dto;
    }

    public function getId(): int
    {
        return $this->id;
    }

    public function getName(): string
    {
        return $this->name;
    }

    public function getPet(): ?Animal2
    {
        return $this->pet;
    }

    /**
     * Formatted as the schema declares it — a date stays a date, and a date-time keeps the
     * sub-second precision the payload carried.
     */
    public function getCreatedAt(): string
    {
        return ($this->createdAt->format('u') === '000000'
            ? $this->createdAt->format('c')
            : $this->createdAt->format('Y-m-d\TH:i:s.uP'));
    }

    public function getCreatedAtAsDateTime(): DateTimeImmutable
    {
        return $this->createdAt;
    }

    /**
     * JSON Schema 2020-12 §6.1.1 counts a number with a zero fractional part as an integer, so `42.0` is
     * a valid `integer` payload — and PHP decodes it to a float an `int` property cannot hold. A real
     * fractional part is left alone: validation has already rejected it.
     */
    private static function toIntIfIntegral(mixed $value): mixed
    {
        return is_float($value) && is_finite($value) && floor($value) === $value
            ? (int)$value
            : $value;
    }

    /**
     * @return array<string, mixed>
     */
    public function toArray(): array
    {
        $result = [];
        $result['id'] = $this->id;
        $result['name'] = $this->name;
        $result['pet'] = $this->pet?->toArray();
        $result['createdAt'] = $this->getCreatedAt();

        return $result;
    }
}

===== User2.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use DateTimeImmutable;
use Illuminate\Validation\Validator;
use stdClass;

use function array_filter;
use function array_key_exists;
use function array_shift;
use function explode;
use function floor;
use function get_object_vars;
use function is_array;
use function is_finite;
use function is_float;
use function is_object;
use function is_string;
use function json_decode;
use function property_exists;
use function sprintf;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 *
 * Laravel mode: the class needs nothing installed — `rules()` is fed to the framework's own
 * validator, and `fromValidated()` builds this object from `$request->validated()`:
 *
 *     final class StoreRequest extends FormRequest
 *     {
 *         public function rules(): array
 *         {
 *             return User2::rules();
 *         }
 *     }
 *
 *     $dto = User2::fromValidated($request->validated());
 */
final class User2
{
    private const array OPENAPI_VALIDATION_CONSTRAINTS = [
        'pet' => [
            'allOf' => [
                [
                    'type' => 'object',
                    'required' => [
                        'test',
                    ],
                    'properties' => [
                        'test' => [
                            'type' => 'string',
                        ],
                    ],
                ],
                [
                    'type' => 'object',
                    'required' => [
                        'test',
                    ],
                    'properties' => [
                        'test' => [
                            'type' => 'string',
                        ],
                    ],
                ],
            ],
            'required' => [
                'name',
                'meow',
                'name',
                'bark',
                'test',
                'test',
            ],
            'nullable' => true,
        ],
    ];

    private const int OPENAPI_MAX_VALIDATION_DEPTH = 256;

    public function __construct(
        private readonly int $id,
        private readonly string $name,
        private readonly ?User2Pet $pet,
        private readonly DateTimeImmutable $createdAt,
    ) {
    }

    /**
     * Validation rules for the framework's validator, keyed by the OpenAPI property names — which is
     * what the request payload carries, not the PHP property names.
     *
     * @return array<string, array<int, mixed>>
     */
    public static function rules(): array
    {
        return [
            'id' => ['present', 'integer'],
            'name' => ['present', 'string'],
            'pet' => ['present', 'nullable'],
            'pet.name' => ['string'],
            'pet.meow' => ['string'],
            'pet.bark' => ['string'],
            'pet.test' => ['string'],
            'createdAt' => ['present', 'date_format:Y-m-d'],
        ];
    }

    /**
     * Paths whose schema is `type: object`. A `*` segment matches every index of an array.
     *
     * @var array<int, string>
     */
    private const array OPENAPI_OBJECT_PATHS = [
        'pet',
    ];

    /**
     * The half of the schema Laravel's rule vocabulary cannot express — composition, conditionals,
     * `contains`, `unevaluated*`, `content*`, `propertyNames`, `discriminator` — checked against the
     * raw payload the validator already holds.
     *
     * Hook it up once, in the FormRequest:
     *
     *     public function withValidator(Validator $validator): void
     *     {
     *         User2::withValidator($validator, $this->getContent());
     *     }
     *
     * `$rawJson` is the UNDECODED body. `type: object` versus `type: array` is a property of the wire
     * shape, and PHP loses it — a JSON object keyed 0..n-1 and a JSON array become the same list — so
     * that one check reads the raw body. Omit the argument and it is skipped; everything else still runs.
     */
    public static function withValidator(Validator $validator, ?string $rawJson = null): void
    {
        $validator->after(static function (Validator $validator) use ($rawJson): void {
            /** @var array<string, mixed> $payload */
            $payload = $validator->getData();

            foreach (self::OPENAPI_VALIDATION_CONSTRAINTS as $property => $schema) {
                if (!array_key_exists($property, $payload)) {
                    continue;
                }

                foreach (self::validateOpenApiNode($payload[$property], $schema, $property, 0) as $error) {
                    $validator->errors()->add($property, $error);
                }
            }

            if ($rawJson !== null && $rawJson !== '') {
                self::assertRawObjectShapes($validator, $rawJson);
            }
        });
    }

    /**
     * Adds an error for every `type: object` path the raw body sent as a JSON array.
     */
    private static function assertRawObjectShapes(Validator $validator, string $rawJson): void
    {
        // Decoded WITHOUT the assoc flag on purpose: that is what keeps a JSON object apart from a
        // JSON array. Laravel's own `$validator->getData()` cannot answer this question.
        $decoded = json_decode($rawJson, false);
        if (!$decoded instanceof stdClass) {
            return;
        }

        foreach (self::OPENAPI_OBJECT_PATHS as $path) {
            foreach (self::rawNodesAt($decoded, explode('.', $path), '') as $foundPath => $node) {
                if (is_array($node)) {
                    $validator->errors()->add($foundPath, sprintf('%s expects object, got array', $foundPath));
                }
            }
        }
    }

    /**
     * Every node a (possibly wildcarded) path reaches, keyed by the concrete path that got there.
     *
     * @param array<int, string> $segments
     * @return array<string, mixed>
     */
    private static function rawNodesAt(mixed $node, array $segments, string $path): array
    {
        if ($segments === []) {
            return [$path => $node];
        }

        $segment = array_shift($segments);
        if ($segment === '*') {
            // `*` stands for both an array index and a map key, so a JSON object node is walked too.
            if ($node instanceof stdClass) {
                $node = get_object_vars($node);
            }
            if (!is_array($node)) {
                return [];
            }

            $found = [];
            foreach ($node as $index => $item) {
                $itemPath = $path === '' ? (string)$index : $path . '.' . $index;
                foreach (self::rawNodesAt($item, $segments, $itemPath) as $deepPath => $deepNode) {
                    $found[$deepPath] = $deepNode;
                }
            }

            return $found;
        }

        if (!$node instanceof stdClass || !property_exists($node, $segment)) {
            return [];
        }

        return self::rawNodesAt($node->{$segment}, $segments, $path === '' ? $segment : $path . '.' . $segment);
    }

    /**
     * @param array<string, mixed> $data the output of `$request->validated()`
     */
    public static function fromValidated(array $data): self
    {
        $dto = new self(
            id: self::toIntIfIntegral($data['id']),
            name: $data['name'],
            pet: ($data['pet'] ?? null) === null ? null : User2Pet::fromValidated($data['pet']),
            createdAt: new DateTimeImmutable($data['createdAt']),
        );

        return $dto;
    }

    public function getId(): int
    {
        return $this->id;
    }

    public function getName(): string
    {
        return $this->name;
    }

    public function getPet(): ?User2Pet
    {
        return $this->pet;
    }

    /**
     * Formatted as the schema declares it — a date stays a date, and a date-time keeps the
     * sub-second precision the payload carried.
     */
    public function getCreatedAt(): string
    {
        return $this->createdAt->format('Y-m-d');
    }

    public function getCreatedAtAsDateTime(): DateTimeImmutable
    {
        return $this->createdAt;
    }

    /**
     * @param array<string, mixed> $schema
     * @return array<int, string>
     */
    private static function validateOpenApiNode(mixed $value, array $schema, string $path, int $depth): array
    {
        if ($depth >= self::OPENAPI_MAX_VALIDATION_DEPTH) {
            return [];
        }

        $errors = [];
        if ($value === null && ($schema['nullable'] ?? false) === true) {
            return [];
        }

        $normalizedValue = $value;
        $structuredValue = self::normalizeOpenApiStructuralValue($value);

        if (array_key_exists('type', $schema)) {
            $typeConstraint = $schema['type'];
            if (is_string($typeConstraint)) {
                if (!self::matchesOpenApiCallbackType($normalizedValue, $typeConstraint)) {
                    $errors[] = sprintf('%s must be of type %s', $path, $typeConstraint);
                }
            }
        }

        if (is_array($structuredValue) && is_array($schema['required'] ?? null)) {
            foreach ($schema['required'] as $requiredProp) {
                if (is_string($requiredProp) && !array_key_exists($requiredProp, $structuredValue)) {
                    $errors[] = sprintf('%s is required', self::openApiChildPath($path, $requiredProp));
                }
            }
        }

        if (is_array($schema['properties'] ?? null) && is_array($structuredValue)) {
            foreach ($schema['properties'] as $propertyName => $propertySchema) {
                if (!is_string($propertyName) || !is_array($propertySchema)) {
                    continue;
                }
                if (!array_key_exists($propertyName, $structuredValue)) {
                    continue;
                }
                $errors = [
                    ...$errors,
                    ...self::validateOpenApiNode($structuredValue[$propertyName], $propertySchema, self::openApiChildPath($path, $propertyName), $depth + 1),
                ];
            }
        }

        return $errors;
    }

    private static function matchesOpenApiCallbackType(mixed $value, mixed $type): bool
    {
        if (!is_string($type) || $type === '') {
            return true;
        }

        return match ($type) {
            'string' => is_string($value),
            'object' => is_array($value) || is_object($value),
            default => true,
        };
    }

    private static function normalizeOpenApiStructuralValue(mixed $value): mixed
    {
        if (is_array($value) || !is_object($value)) {
            return $value;
        }

        // Any other object: only its public state is visible, under PHP property names.
        return array_filter(
            get_object_vars($value),
            static fn(mixed $propertyValue): bool => $propertyValue !== null,
        );
    }

    private static function openApiChildPath(string $path, string $propertyName): string
    {
        // The first level names the field the way the runtime validator's caller does, so both
        // modes report the same subject: `field "tags" must ...`.
        return $path === 'payload'
            ? sprintf('field "%s"', $propertyName)
            : $path . '.' . $propertyName;
    }

    /**
     * JSON Schema 2020-12 §6.1.1 counts a number with a zero fractional part as an integer, so `42.0` is
     * a valid `integer` payload — and PHP decodes it to a float an `int` property cannot hold. A real
     * fractional part is left alone: validation has already rejected it.
     */
    private static function toIntIfIntegral(mixed $value): mixed
    {
        return is_float($value) && is_finite($value) && floor($value) === $value
            ? (int)$value
            : $value;
    }

    /**
     * @return array<string, mixed>
     */
    public function toArray(): array
    {
        $result = [];
        $result['id'] = $this->id;
        $result['name'] = $this->name;
        $result['pet'] = $this->pet?->toArray();
        $result['createdAt'] = $this->getCreatedAt();

        return $result;
    }
}

===== User2Pet.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 * From: User2.pet
 *
 * Laravel mode: the class needs nothing installed — `rules()` is fed to the framework's own
 * validator, and `fromValidated()` builds this object from `$request->validated()`:
 *
 *     final class StoreRequest extends FormRequest
 *     {
 *         public function rules(): array
 *         {
 *             return User2Pet::rules();
 *         }
 *     }
 *
 *     $dto = User2Pet::fromValidated($request->validated());
 */
final class User2Pet
{
    public function __construct(
        private readonly string $name,
        private readonly string $meow,
        private readonly string $bark,
        private readonly string $test,
    ) {
    }

    /**
     * Validation rules for the framework's validator, keyed by the OpenAPI property names — which is
     * what the request payload carries, not the PHP property names.
     *
     * @return array<string, array<int, mixed>>
     */
    public static function rules(): array
    {
        return [
            'name' => ['present', 'string'],
            'meow' => ['present', 'string'],
            'bark' => ['present', 'string'],
            'test' => ['present', 'string'],
        ];
    }

    /**
     * @param array<string, mixed> $data the output of `$request->validated()`
     */
    public static function fromValidated(array $data): self
    {
        $dto = new self(
            name: $data['name'],
            meow: $data['meow'],
            bark: $data['bark'],
            test: $data['test'],
        );

        return $dto;
    }

    public function getName(): string
    {
        return $this->name;
    }

    public function getMeow(): string
    {
        return $this->meow;
    }

    public function getBark(): string
    {
        return $this->bark;
    }

    public function getTest(): string
    {
        return $this->test;
    }

    /**
     * @return array<string, mixed>
     */
    public function toArray(): array
    {
        $result = [];
        $result['name'] = $this->name;
        $result['meow'] = $this->meow;
        $result['bark'] = $this->bark;
        $result['test'] = $this->test;

        return $result;
    }
}
