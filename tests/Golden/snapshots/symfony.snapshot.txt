# symfony mode — 50 files, 5644 lines
# spec: OpenApiExamples/test.yaml, namespace: GoldenCorpus
#
# Animal.php
# Animal2.php
# Animal2AnyOf4.php
# AnimalOneOf3.php
# ApiTest2403.php
# ApiTest2PatchRequest.php
# ApiTest2PostRequest.php
# ApiTest403.php
# ApiTestCatsActionsGetQueryParams.php
# ApiTestCatsActionsPostQueryParams.php
# ApiTestDogsTagsGetQueryParams.php
# ApiTestDogsTagsPatchQueryParams.php
# ApiTestFileDeleteQueryParams.php
# ApiTestFilePatchQueryParams.php
# ApiTestFilePostRequest.php
# AwkwardNames.php
# Cat.php
# ChainLink.php
# ChainMiddle.php
# ChainTag.php
# Circle.php
# ContainerAliases.php
# Containers.php
# CountMap.php
# Discriminator.php
# Discriminator1.php
# Discriminator2.php
# DiscriminatorType.php
# Dog.php
# Dog2.php
# FileRequest.php
# ItemBounds.php
# NestedContainers.php
# NullableContainerValues.php
# Pet.php
# Shape.php
# Square.php
# Tag.php
# Test.php
# Test2.php
# Test2Types.php
# Test2UserType.php
# TestPatchRequest.php
# TestPostRequest.php
# TreeNode.php
# TypeEnum.php
# UnionUnderContainer.php
# User.php
# User2.php
# User2Pet.php

===== Animal.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 *
 * Members: Cat|Dog|AnimalOneOf3
 */
interface Animal
{
}

===== Animal2.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 *
 * Members: Cat|Dog|Dog2|Animal2AnyOf4
 */
interface Animal2
{
}

===== Animal2AnyOf4.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use Symfony\Component\Validator\Constraints as Assert;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 */
final class Animal2AnyOf4 implements Animal2
{
    public function __construct(
        #[Assert\NotNull]
        private readonly string $test2,
    ) {
    }

    public function getTest2(): string
    {
        return $this->test2;
    }

    /**
     * This DTO as an OpenAPI-named payload: what it received, under the names the schema uses.
     * Machinery for the generated validation, not an API to call — an enclosing DTO reads a nested
     * one through here, because the properties themselves are private. For output, use the Symfony
     * serializer.
     *
     * @internal
     *
     * @return array<string, mixed>
     */
    public function toOpenApiValidationPayload(): array
    {
        $payload = [];
        $payload['test2'] = $this->test2;

        return $payload;
    }
}

===== AnimalOneOf3.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use Symfony\Component\Validator\Constraints as Assert;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 */
final class AnimalOneOf3 implements Animal
{
    public function __construct(
        #[Assert\NotNull]
        private readonly string $test,
    ) {
    }

    public function getTest(): string
    {
        return $this->test;
    }
}

===== ApiTest2403.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use Symfony\Component\Validator\Constraints as Assert;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 *
 * Route: GET /api/test2
 */
final class ApiTest2403
{
    public function __construct(
        #[Assert\NotNull]
        private readonly string $message,
    ) {
    }

    public function getMessage(): string
    {
        return $this->message;
    }
}

===== ApiTest2PatchRequest.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use Symfony\Component\Validator\Constraints as Assert;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 *
 * Route: PATCH /api/test2
 */
final class ApiTest2PatchRequest
{
    public function __construct(
        #[Assert\NotNull]
        private readonly int $idPatchTest2,
        #[Assert\NotNull]
        private readonly string $namePatchTest2,
    ) {
    }

    public function getIdPatchTest2(): int
    {
        return $this->idPatchTest2;
    }

    public function getNamePatchTest2(): string
    {
        return $this->namePatchTest2;
    }
}

===== ApiTest2PostRequest.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use Symfony\Component\Validator\Constraints as Assert;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 *
 * Route: POST /api/test2
 */
final class ApiTest2PostRequest
{
    public function __construct(
        #[Assert\NotNull]
        private readonly int $idPostTest2,
        #[Assert\NotNull]
        private readonly string $namePostTest2,
        #[Assert\NotNull]
        private readonly string $surnamePostTest2,
    ) {
    }

    public function getIdPostTest2(): int
    {
        return $this->idPostTest2;
    }

    public function getNamePostTest2(): string
    {
        return $this->namePostTest2;
    }

    public function getSurnamePostTest2(): string
    {
        return $this->surnamePostTest2;
    }
}

===== ApiTest403.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use Symfony\Component\Validator\Constraints as Assert;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 *
 * Route: GET /api/test
 */
final class ApiTest403
{
    public function __construct(
        #[Assert\NotNull]
        private readonly string $message,
    ) {
    }

    public function getMessage(): string
    {
        return $this->message;
    }
}

===== ApiTestCatsActionsGetQueryParams.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use Symfony\Component\Serializer\Attribute\Ignore;
use Symfony\Component\Validator\Constraints as Assert;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 *
 * Route: GET /api/test/cats/{id}/actions/{actionId}
 */
final class ApiTestCatsActionsGetQueryParams
{
    #[Assert\Range(min: 1)]
    private ?int $page = null;

    #[Ignore]
    private bool $pageProvided = false;

    #[Assert\Range(min: 1, max: 100)]
    private ?int $limit = null;

    #[Ignore]
    private bool $limitProvided = false;

    /**
     * @param string $id Example: 00000000-1111-2222-3333-444455556666
     */
    public function __construct(
        #[Assert\NotNull]
        #[Assert\Uuid]
        private readonly string $id,
        #[Assert\NotNull]
        private readonly int $actionId,
    ) {
    }

    public function getId(): string
    {
        return $this->id;
    }

    public function getActionId(): int
    {
        return $this->actionId;
    }

    public function getPage(): ?int
    {
        return $this->page;
    }

    public function setPage(?int $page): void
    {
        $this->page = $page;
        $this->pageProvided = true;
    }

    /**
     * Whether the payload carried this key — true even when its value was null. Set by the setter,
     * so it answers for a request the serializer built and for a DTO assembled by hand alike.
     */
    #[Ignore]
    public function isPageProvided(): bool
    {
        return $this->pageProvided;
    }

    public function getLimit(): ?int
    {
        return $this->limit;
    }

    public function setLimit(?int $limit): void
    {
        $this->limit = $limit;
        $this->limitProvided = true;
    }

    /**
     * Whether the payload carried this key — true even when its value was null. Set by the setter,
     * so it answers for a request the serializer built and for a DTO assembled by hand alike.
     */
    #[Ignore]
    public function isLimitProvided(): bool
    {
        return $this->limitProvided;
    }
}

===== ApiTestCatsActionsPostQueryParams.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use Symfony\Component\Serializer\Attribute\Ignore;
use Symfony\Component\Validator\Constraints as Assert;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 *
 * Route: POST /api/test/cats/{id}/actions/{actionId}
 */
final class ApiTestCatsActionsPostQueryParams
{
    #[Assert\Range(min: 1)]
    private ?int $page = null;

    #[Ignore]
    private bool $pageProvided = false;

    #[Assert\Range(min: 1, max: 100)]
    private ?int $limit = null;

    #[Ignore]
    private bool $limitProvided = false;

    /**
     * @param string $id Example: 00000000-1111-2222-3333-444455556666
     */
    public function __construct(
        #[Assert\NotNull]
        #[Assert\Uuid]
        private readonly string $id,
        #[Assert\NotNull]
        private readonly int $actionId,
    ) {
    }

    public function getId(): string
    {
        return $this->id;
    }

    public function getActionId(): int
    {
        return $this->actionId;
    }

    public function getPage(): ?int
    {
        return $this->page;
    }

    public function setPage(?int $page): void
    {
        $this->page = $page;
        $this->pageProvided = true;
    }

    /**
     * Whether the payload carried this key — true even when its value was null. Set by the setter,
     * so it answers for a request the serializer built and for a DTO assembled by hand alike.
     */
    #[Ignore]
    public function isPageProvided(): bool
    {
        return $this->pageProvided;
    }

    public function getLimit(): ?int
    {
        return $this->limit;
    }

    public function setLimit(?int $limit): void
    {
        $this->limit = $limit;
        $this->limitProvided = true;
    }

    /**
     * Whether the payload carried this key — true even when its value was null. Set by the setter,
     * so it answers for a request the serializer built and for a DTO assembled by hand alike.
     */
    #[Ignore]
    public function isLimitProvided(): bool
    {
        return $this->limitProvided;
    }
}

===== ApiTestDogsTagsGetQueryParams.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use Symfony\Component\Serializer\Attribute\Ignore;
use Symfony\Component\Validator\Constraints as Assert;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 *
 * Route: GET /api/test/dogs/{id}/tags/{tagId}
 */
final class ApiTestDogsTagsGetQueryParams
{
    #[Assert\Range(min: 1)]
    private ?int $page = null;

    #[Ignore]
    private bool $pageProvided = false;

    #[Assert\Range(min: 1, max: 100)]
    private ?int $limit = null;

    #[Ignore]
    private bool $limitProvided = false;

    /**
     * @param string $id Example: 00000000-1111-2222-3333-444455556666
     */
    public function __construct(
        #[Assert\NotNull]
        #[Assert\Uuid]
        private readonly string $id,
        #[Assert\NotNull]
        private readonly int $tagId,
    ) {
    }

    public function getId(): string
    {
        return $this->id;
    }

    public function getTagId(): int
    {
        return $this->tagId;
    }

    public function getPage(): ?int
    {
        return $this->page;
    }

    public function setPage(?int $page): void
    {
        $this->page = $page;
        $this->pageProvided = true;
    }

    /**
     * Whether the payload carried this key — true even when its value was null. Set by the setter,
     * so it answers for a request the serializer built and for a DTO assembled by hand alike.
     */
    #[Ignore]
    public function isPageProvided(): bool
    {
        return $this->pageProvided;
    }

    public function getLimit(): ?int
    {
        return $this->limit;
    }

    public function setLimit(?int $limit): void
    {
        $this->limit = $limit;
        $this->limitProvided = true;
    }

    /**
     * Whether the payload carried this key — true even when its value was null. Set by the setter,
     * so it answers for a request the serializer built and for a DTO assembled by hand alike.
     */
    #[Ignore]
    public function isLimitProvided(): bool
    {
        return $this->limitProvided;
    }
}

===== ApiTestDogsTagsPatchQueryParams.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use Symfony\Component\Serializer\Attribute\Ignore;
use Symfony\Component\Validator\Constraints as Assert;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 *
 * Route: PATCH /api/test/dogs/{id}/tags/{tagId}
 */
final class ApiTestDogsTagsPatchQueryParams
{
    #[Assert\Range(min: 1)]
    private ?int $page = null;

    #[Ignore]
    private bool $pageProvided = false;

    #[Assert\Range(min: 1, max: 100)]
    private ?int $limit = null;

    #[Ignore]
    private bool $limitProvided = false;

    /**
     * @param string $id Example: 00000000-1111-2222-3333-444455556666
     */
    public function __construct(
        #[Assert\NotNull]
        #[Assert\Uuid]
        private readonly string $id,
        #[Assert\NotNull]
        private readonly int $tagId,
    ) {
    }

    public function getId(): string
    {
        return $this->id;
    }

    public function getTagId(): int
    {
        return $this->tagId;
    }

    public function getPage(): ?int
    {
        return $this->page;
    }

    public function setPage(?int $page): void
    {
        $this->page = $page;
        $this->pageProvided = true;
    }

    /**
     * Whether the payload carried this key — true even when its value was null. Set by the setter,
     * so it answers for a request the serializer built and for a DTO assembled by hand alike.
     */
    #[Ignore]
    public function isPageProvided(): bool
    {
        return $this->pageProvided;
    }

    public function getLimit(): ?int
    {
        return $this->limit;
    }

    public function setLimit(?int $limit): void
    {
        $this->limit = $limit;
        $this->limitProvided = true;
    }

    /**
     * Whether the payload carried this key — true even when its value was null. Set by the setter,
     * so it answers for a request the serializer built and for a DTO assembled by hand alike.
     */
    #[Ignore]
    public function isLimitProvided(): bool
    {
        return $this->limitProvided;
    }
}

===== ApiTestFileDeleteQueryParams.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use Symfony\Component\Validator\Constraints as Assert;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 *
 * Route: DELETE /api/test/file/{id}
 */
final class ApiTestFileDeleteQueryParams
{
    /**
     * @param string $id Example: 00000000-1111-2222-3333-444455556666
     */
    public function __construct(
        #[Assert\NotNull]
        #[Assert\Uuid]
        private readonly string $id,
    ) {
    }

    public function getId(): string
    {
        return $this->id;
    }
}

===== ApiTestFilePatchQueryParams.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use Symfony\Component\Validator\Constraints as Assert;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 *
 * Route: PATCH /api/test/file/{id}
 */
final class ApiTestFilePatchQueryParams
{
    /**
     * @param string $id Example: 00000000-1111-2222-3333-444455556666
     */
    public function __construct(
        #[Assert\NotNull]
        #[Assert\Uuid]
        private readonly string $id,
    ) {
    }

    public function getId(): string
    {
        return $this->id;
    }
}

===== ApiTestFilePostRequest.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use Symfony\Component\HttpFoundation\File\UploadedFile;
use Symfony\Component\Validator\Constraints as Assert;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 *
 * Route: POST /api/test/file
 */
final class ApiTestFilePostRequest
{
    public function __construct(
        #[Assert\NotNull]
        private readonly UploadedFile $file,
    ) {
    }

    public function getFile(): UploadedFile
    {
        return $this->file;
    }
}

===== AwkwardNames.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use Symfony\Component\Serializer\Attribute\Ignore;
use Symfony\Component\Serializer\Attribute\SerializedName;
use Symfony\Component\Validator\Constraints as Assert;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 */
final class AwkwardNames
{
    #[SerializedName('back\slash')]
    private ?string $backSlash = null;

    #[Ignore]
    private bool $backSlashProvided = false;

    #[Assert\Range(min: 1)]
    #[SerializedName('both\\\'s')]
    private ?int $bothS = null;

    #[Ignore]
    private bool $bothSProvided = false;

    public function __construct(
        #[Assert\NotNull]
        private readonly string $plain,
        #[Assert\NotNull]
        #[Assert\Length(min: 2)]
        #[SerializedName('it\'s')]
        private readonly string $itS,
    ) {
    }

    public function getPlain(): string
    {
        return $this->plain;
    }

    public function getItS(): string
    {
        return $this->itS;
    }

    public function getBackSlash(): ?string
    {
        return $this->backSlash;
    }

    public function setBackSlash(?string $backSlash): void
    {
        $this->backSlash = $backSlash;
        $this->backSlashProvided = true;
    }

    /**
     * Whether the payload carried this key — true even when its value was null. Set by the setter,
     * so it answers for a request the serializer built and for a DTO assembled by hand alike.
     */
    #[Ignore]
    public function isBackSlashProvided(): bool
    {
        return $this->backSlashProvided;
    }

    public function getBothS(): ?int
    {
        return $this->bothS;
    }

    public function setBothS(?int $bothS): void
    {
        $this->bothS = $bothS;
        $this->bothSProvided = true;
    }

    /**
     * Whether the payload carried this key — true even when its value was null. Set by the setter,
     * so it answers for a request the serializer built and for a DTO assembled by hand alike.
     */
    #[Ignore]
    public function isBothSProvided(): bool
    {
        return $this->bothSProvided;
    }
}

===== Cat.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use Symfony\Component\Validator\Constraints as Assert;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 */
final class Cat implements Animal, Animal2
{
    public function __construct(
        #[Assert\NotNull]
        private readonly string $name,
        #[Assert\NotNull]
        private readonly string $meow,
    ) {
    }

    public function getName(): string
    {
        return $this->name;
    }

    public function getMeow(): string
    {
        return $this->meow;
    }

    /**
     * This DTO as an OpenAPI-named payload: what it received, under the names the schema uses.
     * Machinery for the generated validation, not an API to call — an enclosing DTO reads a nested
     * one through here, because the properties themselves are private. For output, use the Symfony
     * serializer.
     *
     * @internal
     *
     * @return array<string, mixed>
     */
    public function toOpenApiValidationPayload(): array
    {
        $payload = [];
        $payload['name'] = $this->name;
        $payload['meow'] = $this->meow;

        return $payload;
    }
}

===== ChainLink.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use Symfony\Component\Serializer\Attribute\Ignore;
use Symfony\Component\Validator\Constraints as Assert;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 */
final class ChainLink
{
    /**
     * @var ?array<ChainTag>
     */
    #[Assert\Valid]
    private ?array $tags = null;

    #[Ignore]
    private bool $tagsProvided = false;

    public function __construct(
        #[Assert\NotNull]
        #[Assert\Length(min: 3)]
        private readonly string $code,
    ) {
    }

    public function getCode(): string
    {
        return $this->code;
    }

    /**
     * @return ?array<ChainTag>
     */
    public function getTags(): ?array
    {
        return $this->tags;
    }

    /**
     * @param ?array<ChainTag> $tags
     */
    public function setTags(?array $tags): void
    {
        $this->tags = $tags;
        $this->tagsProvided = true;
    }

    /**
     * Whether the payload carried this key — true even when its value was null. Set by the setter,
     * so it answers for a request the serializer built and for a DTO assembled by hand alike.
     */
    #[Ignore]
    public function isTagsProvided(): bool
    {
        return $this->tagsProvided;
    }
}

===== ChainMiddle.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use Symfony\Component\Validator\Constraints as Assert;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 */
final class ChainMiddle
{
    /**
     * @param array<ChainLink> $links
     */
    public function __construct(
        #[Assert\NotNull]
        #[Assert\Valid]
        private readonly array $links,
    ) {
    }

    /**
     * @return array<ChainLink>
     */
    public function getLinks(): array
    {
        return $this->links;
    }
}

===== ChainTag.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use Symfony\Component\Validator\Constraints as Assert;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 */
final class ChainTag
{
    public function __construct(
        #[Assert\NotNull]
        #[Assert\Length(min: 2)]
        private readonly string $label,
    ) {
    }

    public function getLabel(): string
    {
        return $this->label;
    }
}

===== Circle.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use Symfony\Component\Validator\Constraints as Assert;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 */
final class Circle implements Shape
{
    public function __construct(
        #[Assert\NotNull]
        private readonly string $kind,
        #[Assert\NotNull]
        private readonly int $radius,
    ) {
    }

    public function getKind(): string
    {
        return $this->kind;
    }

    public function getRadius(): int
    {
        return $this->radius;
    }
}

===== ContainerAliases.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use Symfony\Component\Serializer\Attribute\Ignore;
use Symfony\Component\Validator\Constraints as Assert;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 */
final class ContainerAliases
{
    /**
     * @var ?array<array<string, mixed>>
     */
    private ?array $listOfMaps = null;

    #[Ignore]
    private bool $listOfMapsProvided = false;

    /**
     * @var ?array<string, array<string>>
     */
    private ?array $mapOfLists = null;

    #[Ignore]
    private bool $mapOfListsProvided = false;

    /**
     * @var ?array<string>
     */
    private ?array $plain = null;

    #[Ignore]
    private bool $plainProvided = false;

    /**
     * @param array<array<string>> $listOfLists
     */
    public function __construct(
        #[Assert\NotNull]
        private readonly array $listOfLists,
    ) {
    }

    /**
     * @return array<array<string>>
     */
    public function getListOfLists(): array
    {
        return $this->listOfLists;
    }

    /**
     * @return ?array<array<string, mixed>>
     */
    public function getListOfMaps(): ?array
    {
        return $this->listOfMaps;
    }

    /**
     * @param ?array<array<string, mixed>> $listOfMaps
     */
    public function setListOfMaps(?array $listOfMaps): void
    {
        $this->listOfMaps = $listOfMaps;
        $this->listOfMapsProvided = true;
    }

    /**
     * Whether the payload carried this key — true even when its value was null. Set by the setter,
     * so it answers for a request the serializer built and for a DTO assembled by hand alike.
     */
    #[Ignore]
    public function isListOfMapsProvided(): bool
    {
        return $this->listOfMapsProvided;
    }

    /**
     * @return ?array<string, array<string>>
     */
    public function getMapOfLists(): ?array
    {
        return $this->mapOfLists;
    }

    /**
     * @param ?array<string, array<string>> $mapOfLists
     */
    public function setMapOfLists(?array $mapOfLists): void
    {
        $this->mapOfLists = $mapOfLists;
        $this->mapOfListsProvided = true;
    }

    /**
     * Whether the payload carried this key — true even when its value was null. Set by the setter,
     * so it answers for a request the serializer built and for a DTO assembled by hand alike.
     */
    #[Ignore]
    public function isMapOfListsProvided(): bool
    {
        return $this->mapOfListsProvided;
    }

    /**
     * @return ?array<string>
     */
    public function getPlain(): ?array
    {
        return $this->plain;
    }

    /**
     * @param ?array<string> $plain
     */
    public function setPlain(?array $plain): void
    {
        $this->plain = $plain;
        $this->plainProvided = true;
    }

    /**
     * Whether the payload carried this key — true even when its value was null. Set by the setter,
     * so it answers for a request the serializer built and for a DTO assembled by hand alike.
     */
    #[Ignore]
    public function isPlainProvided(): bool
    {
        return $this->plainProvided;
    }
}

===== Containers.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use DateTimeImmutable;
use DateTimeInterface;
use Symfony\Component\Serializer\Attribute\Ignore;
use Symfony\Component\Validator\Constraints as Assert;
use Symfony\Component\Validator\Context\ExecutionContextInterface;

use function array_filter;
use function array_is_list;
use function array_key_exists;
use function array_map;
use function get_object_vars;
use function is_array;
use function is_object;
use function is_string;
use function method_exists;
use function preg_match;
use function sprintf;
use function str_ends_with;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 */
final class Containers
{
    private const array OPENAPI_VALIDATION_CONSTRAINTS = [
        'properties' => [
            'dates' => [
                'items' => [
                    'type' => 'string',
                    'format' => 'date',
                ],
            ],
            'datesByDay' => [
                'additionalProperties' => [
                    'type' => 'string',
                    'format' => 'date',
                ],
            ],
            'timestamps' => [
                'items' => [
                    'type' => 'string',
                    'format' => 'date-time',
                ],
            ],
            'shapes' => [],
            'tagsByKey' => [],
        ],
    ];

    private const array OPENAPI_PHP_TO_NAME_MAP = [
        'dates' => 'dates',
        'shapes' => 'shapes',
        'datesByDay' => 'datesByDay',
        'timestamps' => 'timestamps',
        'tagsByKey' => 'tagsByKey',
    ];

    private const array OPENAPI_PROVIDED_FLAGS = [
        'datesByDay' => 'datesByDayProvided',
        'timestamps' => 'timestampsProvided',
        'tagsByKey' => 'tagsByKeyProvided',
    ];

    private const int OPENAPI_MAX_VALIDATION_DEPTH = 256;

    /**
     * @var ?array<string, DateTimeImmutable>
     */
    #[Assert\All([new Assert\Type(DateTimeImmutable::class)])]
    private ?array $datesByDay = null;

    #[Ignore]
    private bool $datesByDayProvided = false;

    /**
     * @var ?array<DateTimeImmutable>
     */
    #[Assert\All([new Assert\Type(DateTimeImmutable::class)])]
    private ?array $timestamps = null;

    #[Ignore]
    private bool $timestampsProvided = false;

    /**
     * @var ?array<string, Tag>
     */
    private ?array $tagsByKey = null;

    #[Ignore]
    private bool $tagsByKeyProvided = false;

    /**
     * @param array<DateTimeImmutable> $dates
     * @param array<Shape> $shapes
     */
    public function __construct(
        #[Assert\NotNull]
        #[Assert\All([new Assert\Type(DateTimeImmutable::class)])]
        private readonly array $dates,
        #[Assert\NotNull]
        #[Assert\Valid]
        private readonly array $shapes,
    ) {
    }

    /**
     * Formatted as the schema declares it — a date stays a date, and a date-time keeps the
     * sub-second precision the payload carried.
     *
     * @return array<string>
     */
    public function getDates(): array
    {
        return array_map(
            static fn (mixed $item): mixed => $item instanceof DateTimeImmutable
                ? $item->format('Y-m-d')
                : $item,
            $this->dates,
        );
    }

    /**
     * @return array<DateTimeImmutable>
     */
    #[Ignore]
    public function getDatesAsDateTime(): array
    {
        return $this->dates;
    }

    /**
     * @return array<Shape>
     */
    public function getShapes(): array
    {
        return $this->shapes;
    }

    /**
     * Formatted as the schema declares it — a date stays a date, and a date-time keeps the
     * sub-second precision the payload carried.
     *
     * @return ?array<string, string>
     */
    public function getDatesByDay(): ?array
    {
        return $this->datesByDay === null ? null : array_map(
            static fn (mixed $item): mixed => $item instanceof DateTimeImmutable
                ? $item->format('Y-m-d')
                : $item,
            $this->datesByDay,
        );
    }

    /**
     * @return ?array<string, DateTimeImmutable>
     */
    #[Ignore]
    public function getDatesByDayAsDateTime(): ?array
    {
        return $this->datesByDay;
    }

    /**
     * @param ?array<string, DateTimeImmutable> $datesByDay
     */
    public function setDatesByDay(?array $datesByDay): void
    {
        $this->datesByDay = $datesByDay;
        $this->datesByDayProvided = true;
    }

    /**
     * Whether the payload carried this key — true even when its value was null. Set by the setter,
     * so it answers for a request the serializer built and for a DTO assembled by hand alike.
     */
    #[Ignore]
    public function isDatesByDayProvided(): bool
    {
        return $this->datesByDayProvided;
    }

    /**
     * Formatted as the schema declares it — a date stays a date, and a date-time keeps the
     * sub-second precision the payload carried.
     *
     * @return ?array<string>
     */
    public function getTimestamps(): ?array
    {
        return $this->timestamps === null ? null : array_map(
            static fn (mixed $item): mixed => $item instanceof DateTimeImmutable
                ? ($item->format('u') === '000000'
                    ? $item->format('c')
                    : $item->format('Y-m-d\TH:i:s.uP'))
                : $item,
            $this->timestamps,
        );
    }

    /**
     * @return ?array<DateTimeImmutable>
     */
    #[Ignore]
    public function getTimestampsAsDateTime(): ?array
    {
        return $this->timestamps;
    }

    /**
     * @param ?array<DateTimeImmutable> $timestamps
     */
    public function setTimestamps(?array $timestamps): void
    {
        $this->timestamps = $timestamps;
        $this->timestampsProvided = true;
    }

    /**
     * Whether the payload carried this key — true even when its value was null. Set by the setter,
     * so it answers for a request the serializer built and for a DTO assembled by hand alike.
     */
    #[Ignore]
    public function isTimestampsProvided(): bool
    {
        return $this->timestampsProvided;
    }

    /**
     * @return ?array<string, Tag>
     */
    public function getTagsByKey(): ?array
    {
        return $this->tagsByKey;
    }

    /**
     * @param ?array<string, Tag> $tagsByKey
     */
    public function setTagsByKey(?array $tagsByKey): void
    {
        $this->tagsByKey = $tagsByKey;
        $this->tagsByKeyProvided = true;
    }

    /**
     * Whether the payload carried this key — true even when its value was null. Set by the setter,
     * so it answers for a request the serializer built and for a DTO assembled by hand alike.
     */
    #[Ignore]
    public function isTagsByKeyProvided(): bool
    {
        return $this->tagsByKeyProvided;
    }

    #[Assert\Callback]
    public function validateOpenApiConstraints(ExecutionContextInterface $context): void
    {
        foreach ($this->validateOpenApiNode($this->toOpenApiValidationPayload(), self::OPENAPI_VALIDATION_CONSTRAINTS, 'payload', 0) as $error) {
            $context->buildViolation(str_ends_with($error, '.') ? $error : $error . '.')->addViolation();
        }
    }

    /**
     * This DTO as an OpenAPI-named payload — machinery for the generated validation, not an API to
     * call. It is public only because an enclosing DTO is a different class and needs this view of
     * a nested one. For output, use the Symfony serializer.
     *
     * @internal
     *
     * @return array<string, mixed>
     */
    public function toOpenApiValidationPayload(): array
    {
        $payload = [];
        foreach (self::OPENAPI_PHP_TO_NAME_MAP as $phpName => $openApiName) {
            // A required property always exists. An optional one belongs in the payload only when
            // it was actually provided — its presence flag says so, which is also what keeps the
            // flags themselves out of the payload (iterating the object's properties would put
            // them in and trip `additionalProperties: false`).
            $flag = self::OPENAPI_PROVIDED_FLAGS[$phpName] ?? null;
            if ($flag !== null && $this->{$flag} !== true) {
                continue;
            }
            $payload[$openApiName] = $this->{$phpName};
        }

        return $payload;
    }

    /**
     * @param array<string, mixed> $schema
     * @return array<int, string>
     */
    private function validateOpenApiNode(mixed $value, array $schema, string $path, int $depth): array
    {
        if ($depth >= self::OPENAPI_MAX_VALIDATION_DEPTH) {
            return [];
        }

        $errors = [];
        $normalizedValue = $this->normalizeOpenApiCallbackValue($value, $schema);
        $structuredValue = $this->normalizeOpenApiStructuralValue($value);

        if (array_key_exists('type', $schema)) {
            $typeConstraint = $schema['type'];
            if (is_string($typeConstraint)) {
                if (!$this->matchesOpenApiCallbackType($normalizedValue, $typeConstraint)) {
                    $errors[] = sprintf('%s must be of type %s', $path, $typeConstraint);
                }
            }
        }

        if (is_string($normalizedValue) && $this->schemaHasAnyOpenApiKey($schema, ['format'])) {
            if (is_string($format = $schema['format'] ?? null) && !$this->isValidOpenApiStringFormat($normalizedValue, $format)) {
                $errors[] = sprintf('%s must match format %s', $path, $format);
            }
        }

        $definedProperties = [];
        $patternMatchedProperties = [];

        if (is_array($schema['properties'] ?? null) && is_array($structuredValue)) {
            foreach ($schema['properties'] as $propertyName => $propertySchema) {
                if (!is_string($propertyName) || !is_array($propertySchema)) {
                    continue;
                }
                $definedProperties[$propertyName] = true;
                if (!array_key_exists($propertyName, $structuredValue)) {
                    continue;
                }
                if ($this->isGeneratedOpenApiDtoObject($structuredValue[$propertyName])) {
                    continue;
                }
                $errors = [
                    ...$errors,
                    ...$this->validateOpenApiNode($structuredValue[$propertyName], $propertySchema, $this->openApiChildPath($path, $propertyName), $depth + 1),
                ];
            }
        }

        $listValue = is_array($value) && array_is_list($value);

        if ($listValue && is_array($schema['items'] ?? null)) {
            foreach ($value as $index => $itemValue) {
                if ($this->isGeneratedOpenApiDtoObject($itemValue)) {
                    continue;
                }
                $errors = [
                    ...$errors,
                    ...$this->validateOpenApiNode($itemValue, $schema['items'], $path . '[' . $index . ']', $depth + 1),
                ];
            }
        }

        if (is_array($structuredValue)) {
            $extraPropertySchema = $schema['additionalProperties'] ?? null;
            $unevaluatedProperties = $schema['unevaluatedProperties'] ?? null;
            $evaluatedProperties = [...$definedProperties, ...$patternMatchedProperties];

            if ($extraPropertySchema === false || is_array($extraPropertySchema)) {
                foreach ($structuredValue as $propertyName => $propertyValue) {
                    if (!is_string($propertyName) || array_key_exists($propertyName, $evaluatedProperties)) {
                        continue;
                    }
                    if ($extraPropertySchema === false) {
                        $errors[] = sprintf('%s has additional property "%s" which is not allowed', $path, $propertyName);
                        continue;
                    }
                    if ($this->isGeneratedOpenApiDtoObject($propertyValue)) {
                        $evaluatedProperties[$propertyName] = true;
                        continue;
                    }
                    $errors = [
                        ...$errors,
                        ...$this->validateOpenApiNode($propertyValue, $extraPropertySchema, $this->openApiChildPath($path, $propertyName), $depth + 1),
                    ];
                    $evaluatedProperties[$propertyName] = true;
                }
            }

        }

        return $errors;
    }

    private function matchesOpenApiCallbackType(mixed $value, mixed $type): bool
    {
        if (!is_string($type) || $type === '') {
            return true;
        }

        return match ($type) {
            'string' => is_string($value),
            default => true,
        };
    }

    private function normalizeOpenApiCallbackValue(mixed $value, array $schema): mixed
    {
        if (!$value instanceof DateTimeInterface) {
            return $value;
        }

        $format = $schema['format'] ?? null;
        if (!is_string($format)) {
            return $value->format(DateTimeInterface::ATOM);
        }

        return match ($format) {
            'date' => $value->format('Y-m-d'),
            default => $value->format(DateTimeInterface::ATOM),
        };
    }

    private function normalizeOpenApiStructuralValue(mixed $value): mixed
    {
        if (is_array($value) || !is_object($value)) {
            return $value;
        }

        // Generated DTOs expose their OpenAPI-named payload themselves.
        if (method_exists($value, 'toOpenApiValidationPayload')) {
            return $value->toOpenApiValidationPayload();
        }
        // Any other object: only its public state is visible, under PHP property names.
        return array_filter(
            get_object_vars($value),
            static fn(mixed $propertyValue): bool => $propertyValue !== null,
        );
    }

    private function isGeneratedOpenApiDtoObject(mixed $value): bool
    {
        // Such a DTO validates its own schema through its own #[Assert\Callback], reached by the
        // #[Assert\Valid] cascade — the enclosing DTO must not report the same errors again.
        return is_object($value)
            && method_exists($value, 'validateOpenApiConstraints')
            && method_exists($value, 'toOpenApiValidationPayload');
    }

    private function openApiChildPath(string $path, string $propertyName): string
    {
        // The first level names the field the way the runtime validator's caller does, so both
        // modes report the same subject: `field "tags" must ...`.
        return $path === 'payload'
            ? sprintf('field "%s"', $propertyName)
            : $path . '.' . $propertyName;
    }

    /**
     * @param array<int, string> $keys
     */
    private function schemaHasAnyOpenApiKey(array $schema, array $keys): bool
    {
        foreach ($keys as $key) {
            if (array_key_exists($key, $schema)) {
                return true;
            }
        }

        return false;
    }

    private function isValidOpenApiStringFormat(string $value, string $format): bool
    {
        return match ($format) {
            'date' => $this->isValidOpenApiDateFormat($value),
            'date-time' => $this->isValidOpenApiDateTimeFormat($value),
            default => true,
        };
    }

    private function isValidOpenApiDateFormat(string $value): bool
    {
        if (!preg_match('/^\d{4}-\d{2}-\d{2}$/', $value)) {
            return false;
        }
        $date = DateTimeImmutable::createFromFormat('Y-m-d', $value);
        return $date !== false && $date->format('Y-m-d') === $value;
    }

    private function isValidOpenApiDateTimeFormat(string $value): bool
    {
        // The four shapes every mode agrees on, and a WARNING check per shape. `createFromFormat()`
        // alone is not a validator: it overflows silently, so `2026-13-45T99:99:99+00:00` parsed
        // clean and a payload the schema forbids was accepted. Only ATOM was tried before, which
        // also meant a sub-second value passed on leftover input rather than on being valid.
        foreach (['Y-m-d\TH:i:sp', 'Y-m-d\TH:i:s.up', 'Y-m-d H:i:s', 'Y-m-d\TH:i:s'] as $format) {
            if (DateTimeImmutable::createFromFormat($format, $value) === false) {
                continue;
            }
            $errors = DateTimeImmutable::getLastErrors();
            if ($errors === false) {
                return true;
            }
            if ((int)($errors['warning_count'] ?? 0) === 0 && (int)($errors['error_count'] ?? 0) === 0) {
                return true;
            }
        }

        return false;
    }
}

===== CountMap.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use Symfony\Component\Validator\Constraints as Assert;
use Symfony\Component\Validator\Context\ExecutionContextInterface;

use function array_filter;
use function array_key_exists;
use function floor;
use function get_object_vars;
use function is_array;
use function is_finite;
use function is_float;
use function is_int;
use function is_object;
use function is_string;
use function json_encode;
use function method_exists;
use function sprintf;
use function str_ends_with;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 */
final class CountMap
{
    private const array OPENAPI_VALIDATION_CONSTRAINTS = [
        'additionalProperties' => [
            'type' => 'integer',
            'minimum' => 0,
        ],
    ];

    private const array OPENAPI_PHP_TO_NAME_MAP = [];

    private const array OPENAPI_PROVIDED_FLAGS = [];

    private const int OPENAPI_MAX_VALIDATION_DEPTH = 256;

    public function __construct()
    {
    }

    #[Assert\Callback]
    public function validateOpenApiConstraints(ExecutionContextInterface $context): void
    {
        foreach ($this->validateOpenApiNode($this->toOpenApiValidationPayload(), self::OPENAPI_VALIDATION_CONSTRAINTS, 'payload', 0) as $error) {
            $context->buildViolation(str_ends_with($error, '.') ? $error : $error . '.')->addViolation();
        }
    }

    /**
     * This DTO as an OpenAPI-named payload — machinery for the generated validation, not an API to
     * call. It is public only because an enclosing DTO is a different class and needs this view of
     * a nested one. For output, use the Symfony serializer.
     *
     * @internal
     *
     * @return array<string, mixed>
     */
    public function toOpenApiValidationPayload(): array
    {
        $payload = [];
        foreach (self::OPENAPI_PHP_TO_NAME_MAP as $phpName => $openApiName) {
            // A required property always exists. An optional one belongs in the payload only when
            // it was actually provided — its presence flag says so, which is also what keeps the
            // flags themselves out of the payload (iterating the object's properties would put
            // them in and trip `additionalProperties: false`).
            $flag = self::OPENAPI_PROVIDED_FLAGS[$phpName] ?? null;
            if ($flag !== null && $this->{$flag} !== true) {
                continue;
            }
            $payload[$openApiName] = $this->{$phpName};
        }

        return $payload;
    }

    /**
     * @param array<string, mixed> $schema
     * @return array<int, string>
     */
    private function validateOpenApiNode(mixed $value, array $schema, string $path, int $depth): array
    {
        if ($depth >= self::OPENAPI_MAX_VALIDATION_DEPTH) {
            return [];
        }

        $errors = [];
        $normalizedValue = $value;
        $structuredValue = $this->normalizeOpenApiStructuralValue($value);

        if (array_key_exists('type', $schema)) {
            $typeConstraint = $schema['type'];
            if (is_string($typeConstraint)) {
                if (!$this->matchesOpenApiCallbackType($normalizedValue, $typeConstraint)) {
                    $errors[] = sprintf('%s must be of type %s', $path, $typeConstraint);
                }
            }
        }

        if ((is_int($normalizedValue) || is_float($normalizedValue)) && $this->schemaHasAnyOpenApiKey($schema, ['minimum'])) {
            $minimum = $this->toFloatConstraint($schema['minimum'] ?? null);
            if ($minimum !== null && !($normalizedValue >= $minimum)) {
                $errors[] = sprintf('%s must be greater than or equal to %s', $path, $this->stringifyOpenApiNumber($minimum));
            }
        }

        $definedProperties = [];
        $patternMatchedProperties = [];

        if (is_array($structuredValue)) {
            $extraPropertySchema = $schema['additionalProperties'] ?? null;
            $unevaluatedProperties = $schema['unevaluatedProperties'] ?? null;
            $evaluatedProperties = [...$definedProperties, ...$patternMatchedProperties];

            if ($extraPropertySchema === false || is_array($extraPropertySchema)) {
                foreach ($structuredValue as $propertyName => $propertyValue) {
                    if (!is_string($propertyName) || array_key_exists($propertyName, $evaluatedProperties)) {
                        continue;
                    }
                    if ($extraPropertySchema === false) {
                        $errors[] = sprintf('%s has additional property "%s" which is not allowed', $path, $propertyName);
                        continue;
                    }
                    if ($this->isGeneratedOpenApiDtoObject($propertyValue)) {
                        $evaluatedProperties[$propertyName] = true;
                        continue;
                    }
                    $errors = [
                        ...$errors,
                        ...$this->validateOpenApiNode($propertyValue, $extraPropertySchema, $this->openApiChildPath($path, $propertyName), $depth + 1),
                    ];
                    $evaluatedProperties[$propertyName] = true;
                }
            }

        }

        return $errors;
    }

    private function matchesOpenApiCallbackType(mixed $value, mixed $type): bool
    {
        if (!is_string($type) || $type === '') {
            return true;
        }

        return match ($type) {
            'integer' => is_int($value) || (is_float($value) && is_finite($value) && floor($value) === $value),
            default => true,
        };
    }

    private function normalizeOpenApiStructuralValue(mixed $value): mixed
    {
        if (is_array($value) || !is_object($value)) {
            return $value;
        }

        // Generated DTOs expose their OpenAPI-named payload themselves.
        if (method_exists($value, 'toOpenApiValidationPayload')) {
            return $value->toOpenApiValidationPayload();
        }
        // Any other object: only its public state is visible, under PHP property names.
        return array_filter(
            get_object_vars($value),
            static fn(mixed $propertyValue): bool => $propertyValue !== null,
        );
    }

    private function isGeneratedOpenApiDtoObject(mixed $value): bool
    {
        // Such a DTO validates its own schema through its own #[Assert\Callback], reached by the
        // #[Assert\Valid] cascade — the enclosing DTO must not report the same errors again.
        return is_object($value)
            && method_exists($value, 'validateOpenApiConstraints')
            && method_exists($value, 'toOpenApiValidationPayload');
    }

    private function openApiChildPath(string $path, string $propertyName): string
    {
        // The first level names the field the way the runtime validator's caller does, so both
        // modes report the same subject: `field "tags" must ...`.
        return $path === 'payload'
            ? sprintf('field "%s"', $propertyName)
            : $path . '.' . $propertyName;
    }

    /**
     * @param array<int, string> $keys
     */
    private function schemaHasAnyOpenApiKey(array $schema, array $keys): bool
    {
        foreach ($keys as $key) {
            if (array_key_exists($key, $schema)) {
                return true;
            }
        }

        return false;
    }

    private function toFloatConstraint(mixed $value): ?float
    {
        return is_int($value) || is_float($value) ? (float)$value : null;
    }

    private function stringifyOpenApiNumber(float $value): string
    {
        $rendered = json_encode($value);
        return is_string($rendered) ? $rendered : (string)$value;
    }
}

===== Discriminator.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use Symfony\Component\Validator\Constraints as Assert;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 */
final class Discriminator
{
    public function __construct(
        #[Assert\NotNull]
        private readonly DiscriminatorType $type,
        private readonly ?string $name,
    ) {
    }

    public function getType(): DiscriminatorType
    {
        return $this->type;
    }

    public function getName(): ?string
    {
        return $this->name;
    }
}

===== Discriminator1.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use Symfony\Component\Validator\Constraints as Assert;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 */
final class Discriminator1
{
    public function __construct(
        #[Assert\NotNull]
        private readonly DiscriminatorType $type,
        private readonly ?string $name,
        #[Assert\NotNull]
        private readonly int $id,
    ) {
    }

    public function getType(): DiscriminatorType
    {
        return $this->type;
    }

    public function getName(): ?string
    {
        return $this->name;
    }

    public function getId(): int
    {
        return $this->id;
    }
}

===== Discriminator2.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use Symfony\Component\Serializer\Attribute\SerializedName;
use Symfony\Component\Validator\Constraints as Assert;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 */
final class Discriminator2
{
    public function __construct(
        #[Assert\NotNull]
        private readonly DiscriminatorType $type,
        private readonly ?string $name,
        #[Assert\NotNull]
        private readonly int $id,
        #[Assert\NotNull]
        #[SerializedName('NAme')]
        private readonly string $nAme2,
    ) {
    }

    public function getType(): DiscriminatorType
    {
        return $this->type;
    }

    public function getName(): ?string
    {
        return $this->name;
    }

    public function getId(): int
    {
        return $this->id;
    }

    public function getNAme2(): string
    {
        return $this->nAme2;
    }
}

===== DiscriminatorType.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

/**
 * NOTE: This enum is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 *
 * From: Discriminator.type
 */
enum DiscriminatorType: string
{
    case DISCRIMINATOR1 = 'discriminator1';
    case DISCRIMINATOR2 = 'discriminator2';
}

===== Dog.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use Symfony\Component\Validator\Constraints as Assert;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 */
final class Dog implements Animal, Animal2
{
    public function __construct(
        #[Assert\NotNull]
        private readonly string $name,
        #[Assert\NotNull]
        private readonly string $bark,
    ) {
    }

    public function getName(): string
    {
        return $this->name;
    }

    public function getBark(): string
    {
        return $this->bark;
    }

    /**
     * This DTO as an OpenAPI-named payload: what it received, under the names the schema uses.
     * Machinery for the generated validation, not an API to call — an enclosing DTO reads a nested
     * one through here, because the properties themselves are private. For output, use the Symfony
     * serializer.
     *
     * @internal
     *
     * @return array<string, mixed>
     */
    public function toOpenApiValidationPayload(): array
    {
        $payload = [];
        $payload['name'] = $this->name;
        $payload['bark'] = $this->bark;

        return $payload;
    }
}

===== Dog2.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use Symfony\Component\Validator\Constraints as Assert;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 */
final class Dog2 implements Animal2
{
    public function __construct(
        #[Assert\NotNull]
        private readonly string $name,
        #[Assert\NotNull]
        private readonly string $bark,
    ) {
    }

    public function getName(): string
    {
        return $this->name;
    }

    public function getBark(): string
    {
        return $this->bark;
    }

    /**
     * This DTO as an OpenAPI-named payload: what it received, under the names the schema uses.
     * Machinery for the generated validation, not an API to call — an enclosing DTO reads a nested
     * one through here, because the properties themselves are private. For output, use the Symfony
     * serializer.
     *
     * @internal
     *
     * @return array<string, mixed>
     */
    public function toOpenApiValidationPayload(): array
    {
        $payload = [];
        $payload['name'] = $this->name;
        $payload['bark'] = $this->bark;

        return $payload;
    }
}

===== FileRequest.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use Symfony\Component\HttpFoundation\File\UploadedFile;
use Symfony\Component\Validator\Constraints as Assert;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 */
final class FileRequest
{
    public function __construct(
        #[Assert\NotNull]
        private readonly UploadedFile $file,
        #[Assert\NotNull]
        private readonly string $name,
        #[Assert\NotNull]
        private readonly string $surname,
    ) {
    }

    public function getFile(): UploadedFile
    {
        return $this->file;
    }

    public function getName(): string
    {
        return $this->name;
    }

    public function getSurname(): string
    {
        return $this->surname;
    }
}

===== ItemBounds.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use Symfony\Component\Serializer\Attribute\Ignore;
use Symfony\Component\Validator\Constraints as Assert;
use Symfony\Component\Validator\Context\ExecutionContextInterface;

use function array_filter;
use function array_is_list;
use function array_key_exists;
use function floor;
use function get_object_vars;
use function is_array;
use function is_finite;
use function is_float;
use function is_int;
use function is_object;
use function is_string;
use function method_exists;
use function sprintf;
use function str_ends_with;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 */
final class ItemBounds
{
    private const array OPENAPI_VALIDATION_CONSTRAINTS = [
        'properties' => [
            'scores' => [
                'items' => [
                    'type' => 'integer',
                ],
            ],
            'scoresByKey' => [
                'additionalProperties' => [
                    'type' => 'integer',
                ],
            ],
            'codes' => [
                'items' => [
                    'type' => 'string',
                ],
            ],
        ],
    ];

    private const array OPENAPI_PHP_TO_NAME_MAP = [
        'scores' => 'scores',
        'scoresByKey' => 'scoresByKey',
        'codes' => 'codes',
    ];

    private const array OPENAPI_PROVIDED_FLAGS = [
        'scoresByKey' => 'scoresByKeyProvided',
        'codes' => 'codesProvided',
    ];

    private const int OPENAPI_MAX_VALIDATION_DEPTH = 256;

    /**
     * @var ?array<string, int>
     */
    #[Assert\All([new Assert\Range(min: 5)])]
    private ?array $scoresByKey = null;

    #[Ignore]
    private bool $scoresByKeyProvided = false;

    /**
     * @var ?array<string>
     */
    #[Assert\All([new Assert\Length(min: 2), new Assert\Regex('/^[a-z]+$/')])]
    private ?array $codes = null;

    #[Ignore]
    private bool $codesProvided = false;

    /**
     * @param array<int> $scores
     */
    public function __construct(
        #[Assert\NotNull]
        #[Assert\All([new Assert\Range(min: 5)])]
        private readonly array $scores,
    ) {
    }

    /**
     * @return array<int>
     */
    public function getScores(): array
    {
        return $this->scores;
    }

    /**
     * @return ?array<string, int>
     */
    public function getScoresByKey(): ?array
    {
        return $this->scoresByKey;
    }

    /**
     * @param ?array<string, int> $scoresByKey
     */
    public function setScoresByKey(?array $scoresByKey): void
    {
        $this->scoresByKey = $scoresByKey;
        $this->scoresByKeyProvided = true;
    }

    /**
     * Whether the payload carried this key — true even when its value was null. Set by the setter,
     * so it answers for a request the serializer built and for a DTO assembled by hand alike.
     */
    #[Ignore]
    public function isScoresByKeyProvided(): bool
    {
        return $this->scoresByKeyProvided;
    }

    /**
     * @return ?array<string>
     */
    public function getCodes(): ?array
    {
        return $this->codes;
    }

    /**
     * @param ?array<string> $codes
     */
    public function setCodes(?array $codes): void
    {
        $this->codes = $codes;
        $this->codesProvided = true;
    }

    /**
     * Whether the payload carried this key — true even when its value was null. Set by the setter,
     * so it answers for a request the serializer built and for a DTO assembled by hand alike.
     */
    #[Ignore]
    public function isCodesProvided(): bool
    {
        return $this->codesProvided;
    }

    #[Assert\Callback]
    public function validateOpenApiConstraints(ExecutionContextInterface $context): void
    {
        foreach ($this->validateOpenApiNode($this->toOpenApiValidationPayload(), self::OPENAPI_VALIDATION_CONSTRAINTS, 'payload', 0) as $error) {
            $context->buildViolation(str_ends_with($error, '.') ? $error : $error . '.')->addViolation();
        }
    }

    /**
     * This DTO as an OpenAPI-named payload — machinery for the generated validation, not an API to
     * call. It is public only because an enclosing DTO is a different class and needs this view of
     * a nested one. For output, use the Symfony serializer.
     *
     * @internal
     *
     * @return array<string, mixed>
     */
    public function toOpenApiValidationPayload(): array
    {
        $payload = [];
        foreach (self::OPENAPI_PHP_TO_NAME_MAP as $phpName => $openApiName) {
            // A required property always exists. An optional one belongs in the payload only when
            // it was actually provided — its presence flag says so, which is also what keeps the
            // flags themselves out of the payload (iterating the object's properties would put
            // them in and trip `additionalProperties: false`).
            $flag = self::OPENAPI_PROVIDED_FLAGS[$phpName] ?? null;
            if ($flag !== null && $this->{$flag} !== true) {
                continue;
            }
            $payload[$openApiName] = $this->{$phpName};
        }

        return $payload;
    }

    /**
     * @param array<string, mixed> $schema
     * @return array<int, string>
     */
    private function validateOpenApiNode(mixed $value, array $schema, string $path, int $depth): array
    {
        if ($depth >= self::OPENAPI_MAX_VALIDATION_DEPTH) {
            return [];
        }

        $errors = [];
        $normalizedValue = $value;
        $structuredValue = $this->normalizeOpenApiStructuralValue($value);

        if (array_key_exists('type', $schema)) {
            $typeConstraint = $schema['type'];
            if (is_string($typeConstraint)) {
                if (!$this->matchesOpenApiCallbackType($normalizedValue, $typeConstraint)) {
                    $errors[] = sprintf('%s must be of type %s', $path, $typeConstraint);
                }
            }
        }

        $definedProperties = [];
        $patternMatchedProperties = [];

        if (is_array($schema['properties'] ?? null) && is_array($structuredValue)) {
            foreach ($schema['properties'] as $propertyName => $propertySchema) {
                if (!is_string($propertyName) || !is_array($propertySchema)) {
                    continue;
                }
                $definedProperties[$propertyName] = true;
                if (!array_key_exists($propertyName, $structuredValue)) {
                    continue;
                }
                if ($this->isGeneratedOpenApiDtoObject($structuredValue[$propertyName])) {
                    continue;
                }
                $errors = [
                    ...$errors,
                    ...$this->validateOpenApiNode($structuredValue[$propertyName], $propertySchema, $this->openApiChildPath($path, $propertyName), $depth + 1),
                ];
            }
        }

        $listValue = is_array($value) && array_is_list($value);

        if ($listValue && is_array($schema['items'] ?? null)) {
            foreach ($value as $index => $itemValue) {
                if ($this->isGeneratedOpenApiDtoObject($itemValue)) {
                    continue;
                }
                $errors = [
                    ...$errors,
                    ...$this->validateOpenApiNode($itemValue, $schema['items'], $path . '[' . $index . ']', $depth + 1),
                ];
            }
        }

        if (is_array($structuredValue)) {
            $extraPropertySchema = $schema['additionalProperties'] ?? null;
            $unevaluatedProperties = $schema['unevaluatedProperties'] ?? null;
            $evaluatedProperties = [...$definedProperties, ...$patternMatchedProperties];

            if ($extraPropertySchema === false || is_array($extraPropertySchema)) {
                foreach ($structuredValue as $propertyName => $propertyValue) {
                    if (!is_string($propertyName) || array_key_exists($propertyName, $evaluatedProperties)) {
                        continue;
                    }
                    if ($extraPropertySchema === false) {
                        $errors[] = sprintf('%s has additional property "%s" which is not allowed', $path, $propertyName);
                        continue;
                    }
                    if ($this->isGeneratedOpenApiDtoObject($propertyValue)) {
                        $evaluatedProperties[$propertyName] = true;
                        continue;
                    }
                    $errors = [
                        ...$errors,
                        ...$this->validateOpenApiNode($propertyValue, $extraPropertySchema, $this->openApiChildPath($path, $propertyName), $depth + 1),
                    ];
                    $evaluatedProperties[$propertyName] = true;
                }
            }

        }

        return $errors;
    }

    private function matchesOpenApiCallbackType(mixed $value, mixed $type): bool
    {
        if (!is_string($type) || $type === '') {
            return true;
        }

        return match ($type) {
            'integer' => is_int($value) || (is_float($value) && is_finite($value) && floor($value) === $value),
            'string' => is_string($value),
            default => true,
        };
    }

    private function normalizeOpenApiStructuralValue(mixed $value): mixed
    {
        if (is_array($value) || !is_object($value)) {
            return $value;
        }

        // Generated DTOs expose their OpenAPI-named payload themselves.
        if (method_exists($value, 'toOpenApiValidationPayload')) {
            return $value->toOpenApiValidationPayload();
        }
        // Any other object: only its public state is visible, under PHP property names.
        return array_filter(
            get_object_vars($value),
            static fn(mixed $propertyValue): bool => $propertyValue !== null,
        );
    }

    private function isGeneratedOpenApiDtoObject(mixed $value): bool
    {
        // Such a DTO validates its own schema through its own #[Assert\Callback], reached by the
        // #[Assert\Valid] cascade — the enclosing DTO must not report the same errors again.
        return is_object($value)
            && method_exists($value, 'validateOpenApiConstraints')
            && method_exists($value, 'toOpenApiValidationPayload');
    }

    private function openApiChildPath(string $path, string $propertyName): string
    {
        // The first level names the field the way the runtime validator's caller does, so both
        // modes report the same subject: `field "tags" must ...`.
        return $path === 'payload'
            ? sprintf('field "%s"', $propertyName)
            : $path . '.' . $propertyName;
    }
}

===== NestedContainers.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use DateTimeImmutable;
use Symfony\Component\Serializer\Attribute\Ignore;
use Symfony\Component\Validator\Constraints as Assert;
use Symfony\Component\Validator\Context\ExecutionContextInterface;

use function array_filter;
use function array_is_list;
use function array_key_exists;
use function array_map;
use function floor;
use function get_object_vars;
use function implode;
use function in_array;
use function is_array;
use function is_finite;
use function is_float;
use function is_int;
use function is_object;
use function is_string;
use function json_encode;
use function method_exists;
use function preg_match;
use function sprintf;
use function str_ends_with;
use function var_export;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 */
final class NestedContainers
{
    private const array OPENAPI_VALIDATION_CONSTRAINTS = [
        'properties' => [
            'matrix' => [
                'items' => [
                    'type' => 'array',
                    'items' => [
                        'type' => 'integer',
                        'minimum' => 0,
                    ],
                ],
            ],
            'rowsOfMaps' => [
                'items' => [
                    'type' => 'object',
                    'additionalProperties' => [
                        'type' => 'string',
                    ],
                ],
            ],
            'listsByKey' => [
                'additionalProperties' => [
                    'type' => 'array',
                    'items' => [
                        'type' => 'string',
                    ],
                ],
            ],
            'mapsByKey' => [
                'additionalProperties' => [
                    'type' => 'object',
                    'additionalProperties' => [
                        'type' => 'integer',
                    ],
                ],
            ],
            'dateRows' => [
                'items' => [
                    'type' => 'array',
                    'items' => [
                        'type' => 'string',
                        'format' => 'date',
                    ],
                ],
            ],
            'numRows' => [
                'items' => [
                    'type' => 'array',
                    'items' => [
                        'type' => 'number',
                    ],
                ],
            ],
            'enumRows' => [
                'items' => [
                    'type' => 'array',
                    'items' => [
                        'type' => 'string',
                        'enum' => [
                            'catType',
                            'dogType',
                        ],
                    ],
                ],
            ],
            'tagRows' => [
                'items' => [
                    'type' => 'array',
                    'items' => [
                        'type' => 'object',
                        'required' => [
                            'id',
                            'name',
                        ],
                        'properties' => [
                            'id' => [
                                'type' => 'integer',
                            ],
                            'name' => [
                                'type' => 'string',
                            ],
                        ],
                    ],
                ],
            ],
        ],
    ];

    private const array OPENAPI_PHP_TO_NAME_MAP = [
        'matrix' => 'matrix',
        'rowsOfMaps' => 'rowsOfMaps',
        'listsByKey' => 'listsByKey',
        'mapsByKey' => 'mapsByKey',
        'dateRows' => 'dateRows',
        'numRows' => 'numRows',
        'enumRows' => 'enumRows',
        'tagRows' => 'tagRows',
    ];

    private const array OPENAPI_PROVIDED_FLAGS = [
        'listsByKey' => 'listsByKeyProvided',
        'mapsByKey' => 'mapsByKeyProvided',
        'dateRows' => 'dateRowsProvided',
        'numRows' => 'numRowsProvided',
        'enumRows' => 'enumRowsProvided',
        'tagRows' => 'tagRowsProvided',
    ];

    private const int OPENAPI_MAX_VALIDATION_DEPTH = 256;

    /**
     * @var ?array<string, array<string>>
     */
    private ?array $listsByKey = null;

    #[Ignore]
    private bool $listsByKeyProvided = false;

    /**
     * @var ?array<string, array<string, int>>
     */
    private ?array $mapsByKey = null;

    #[Ignore]
    private bool $mapsByKeyProvided = false;

    /**
     * @var ?array<array<string>>
     */
    private ?array $dateRows = null;

    #[Ignore]
    private bool $dateRowsProvided = false;

    /**
     * @var array<array<float|int>>|null
     */
    private ?array $numRows = null;

    #[Ignore]
    private bool $numRowsProvided = false;

    /**
     * @var ?array<array<string>>
     */
    private ?array $enumRows = null;

    #[Ignore]
    private bool $enumRowsProvided = false;

    /**
     * @var ?array<array<Tag>>
     */
    private ?array $tagRows = null;

    #[Ignore]
    private bool $tagRowsProvided = false;

    /**
     * @param array<array<int>> $matrix
     * @param array<array<string, string>> $rowsOfMaps
     */
    public function __construct(
        #[Assert\NotNull]
        private readonly array $matrix,
        #[Assert\NotNull]
        private readonly array $rowsOfMaps,
    ) {
    }

    /**
     * @return array<array<int>>
     */
    public function getMatrix(): array
    {
        return $this->matrix;
    }

    /**
     * @return array<array<string, string>>
     */
    public function getRowsOfMaps(): array
    {
        return $this->rowsOfMaps;
    }

    /**
     * @return ?array<string, array<string>>
     */
    public function getListsByKey(): ?array
    {
        return $this->listsByKey;
    }

    /**
     * @param ?array<string, array<string>> $listsByKey
     */
    public function setListsByKey(?array $listsByKey): void
    {
        $this->listsByKey = $listsByKey;
        $this->listsByKeyProvided = true;
    }

    /**
     * Whether the payload carried this key — true even when its value was null. Set by the setter,
     * so it answers for a request the serializer built and for a DTO assembled by hand alike.
     */
    #[Ignore]
    public function isListsByKeyProvided(): bool
    {
        return $this->listsByKeyProvided;
    }

    /**
     * @return ?array<string, array<string, int>>
     */
    public function getMapsByKey(): ?array
    {
        return $this->mapsByKey;
    }

    /**
     * @param ?array<string, array<string, int>> $mapsByKey
     */
    public function setMapsByKey(?array $mapsByKey): void
    {
        $this->mapsByKey = $mapsByKey;
        $this->mapsByKeyProvided = true;
    }

    /**
     * Whether the payload carried this key — true even when its value was null. Set by the setter,
     * so it answers for a request the serializer built and for a DTO assembled by hand alike.
     */
    #[Ignore]
    public function isMapsByKeyProvided(): bool
    {
        return $this->mapsByKeyProvided;
    }

    /**
     * @return ?array<array<string>>
     */
    public function getDateRows(): ?array
    {
        return $this->dateRows;
    }

    /**
     * @param ?array<array<string>> $dateRows
     */
    public function setDateRows(?array $dateRows): void
    {
        $this->dateRows = $dateRows;
        $this->dateRowsProvided = true;
    }

    /**
     * Whether the payload carried this key — true even when its value was null. Set by the setter,
     * so it answers for a request the serializer built and for a DTO assembled by hand alike.
     */
    #[Ignore]
    public function isDateRowsProvided(): bool
    {
        return $this->dateRowsProvided;
    }

    /**
     * @return array<array<float|int>>|null
     */
    public function getNumRows(): ?array
    {
        return $this->numRows;
    }

    /**
     * @param array<array<float|int>>|null $numRows
     */
    public function setNumRows(?array $numRows): void
    {
        $this->numRows = $numRows;
        $this->numRowsProvided = true;
    }

    /**
     * Whether the payload carried this key — true even when its value was null. Set by the setter,
     * so it answers for a request the serializer built and for a DTO assembled by hand alike.
     */
    #[Ignore]
    public function isNumRowsProvided(): bool
    {
        return $this->numRowsProvided;
    }

    /**
     * @return ?array<array<string>>
     */
    public function getEnumRows(): ?array
    {
        return $this->enumRows;
    }

    /**
     * @param ?array<array<string>> $enumRows
     */
    public function setEnumRows(?array $enumRows): void
    {
        $this->enumRows = $enumRows;
        $this->enumRowsProvided = true;
    }

    /**
     * Whether the payload carried this key — true even when its value was null. Set by the setter,
     * so it answers for a request the serializer built and for a DTO assembled by hand alike.
     */
    #[Ignore]
    public function isEnumRowsProvided(): bool
    {
        return $this->enumRowsProvided;
    }

    /**
     * @return ?array<array<Tag>>
     */
    public function getTagRows(): ?array
    {
        return $this->tagRows;
    }

    /**
     * @param ?array<array<Tag>> $tagRows
     */
    public function setTagRows(?array $tagRows): void
    {
        $this->tagRows = $tagRows;
        $this->tagRowsProvided = true;
    }

    /**
     * Whether the payload carried this key — true even when its value was null. Set by the setter,
     * so it answers for a request the serializer built and for a DTO assembled by hand alike.
     */
    #[Ignore]
    public function isTagRowsProvided(): bool
    {
        return $this->tagRowsProvided;
    }

    #[Assert\Callback]
    public function validateOpenApiConstraints(ExecutionContextInterface $context): void
    {
        foreach ($this->validateOpenApiNode($this->toOpenApiValidationPayload(), self::OPENAPI_VALIDATION_CONSTRAINTS, 'payload', 0) as $error) {
            $context->buildViolation(str_ends_with($error, '.') ? $error : $error . '.')->addViolation();
        }
    }

    /**
     * This DTO as an OpenAPI-named payload — machinery for the generated validation, not an API to
     * call. It is public only because an enclosing DTO is a different class and needs this view of
     * a nested one. For output, use the Symfony serializer.
     *
     * @internal
     *
     * @return array<string, mixed>
     */
    public function toOpenApiValidationPayload(): array
    {
        $payload = [];
        foreach (self::OPENAPI_PHP_TO_NAME_MAP as $phpName => $openApiName) {
            // A required property always exists. An optional one belongs in the payload only when
            // it was actually provided — its presence flag says so, which is also what keeps the
            // flags themselves out of the payload (iterating the object's properties would put
            // them in and trip `additionalProperties: false`).
            $flag = self::OPENAPI_PROVIDED_FLAGS[$phpName] ?? null;
            if ($flag !== null && $this->{$flag} !== true) {
                continue;
            }
            $payload[$openApiName] = $this->{$phpName};
        }

        return $payload;
    }

    /**
     * @param array<string, mixed> $schema
     * @return array<int, string>
     */
    private function validateOpenApiNode(mixed $value, array $schema, string $path, int $depth): array
    {
        if ($depth >= self::OPENAPI_MAX_VALIDATION_DEPTH) {
            return [];
        }

        $errors = [];
        $normalizedValue = $value;
        $structuredValue = $this->normalizeOpenApiStructuralValue($value);

        if (is_array($schema['enum'] ?? null) && !in_array($normalizedValue, $schema['enum'], true)) {
            $allowed = implode(', ', array_map(
                static function (mixed $allowedValue): string {
                    $json = json_encode($allowedValue);
                    return $json !== false ? $json : var_export($allowedValue, true);
                },
                $schema['enum'],
            ));
            $errors[] = sprintf('%s must be one of: %s', $path, $allowed);
        }

        if (array_key_exists('type', $schema)) {
            $typeConstraint = $schema['type'];
            if (is_string($typeConstraint)) {
                if (!$this->matchesOpenApiCallbackType($normalizedValue, $typeConstraint)) {
                    $errors[] = $typeConstraint === 'array' && is_array($normalizedValue)
                        ? sprintf('%s must be a JSON array (list with sequential keys), got an associative array', $path)
                        : sprintf('%s must be of type %s', $path, $typeConstraint);
                }
            }
        }

        if ((is_int($normalizedValue) || is_float($normalizedValue)) && $this->schemaHasAnyOpenApiKey($schema, ['minimum'])) {
            $minimum = $this->toFloatConstraint($schema['minimum'] ?? null);
            if ($minimum !== null && !($normalizedValue >= $minimum)) {
                $errors[] = sprintf('%s must be greater than or equal to %s', $path, $this->stringifyOpenApiNumber($minimum));
            }
        }

        if (is_string($normalizedValue) && $this->schemaHasAnyOpenApiKey($schema, ['format'])) {
            if (is_string($format = $schema['format'] ?? null) && !$this->isValidOpenApiStringFormat($normalizedValue, $format)) {
                $errors[] = sprintf('%s must match format %s', $path, $format);
            }
        }

        if (is_array($structuredValue) && is_array($schema['required'] ?? null)) {
            foreach ($schema['required'] as $requiredProp) {
                if (is_string($requiredProp) && !array_key_exists($requiredProp, $structuredValue)) {
                    $errors[] = sprintf('%s is required', $this->openApiChildPath($path, $requiredProp));
                }
            }
        }

        $definedProperties = [];
        $patternMatchedProperties = [];

        if (is_array($schema['properties'] ?? null) && is_array($structuredValue)) {
            foreach ($schema['properties'] as $propertyName => $propertySchema) {
                if (!is_string($propertyName) || !is_array($propertySchema)) {
                    continue;
                }
                $definedProperties[$propertyName] = true;
                if (!array_key_exists($propertyName, $structuredValue)) {
                    continue;
                }
                if ($this->isGeneratedOpenApiDtoObject($structuredValue[$propertyName])) {
                    continue;
                }
                $errors = [
                    ...$errors,
                    ...$this->validateOpenApiNode($structuredValue[$propertyName], $propertySchema, $this->openApiChildPath($path, $propertyName), $depth + 1),
                ];
            }
        }

        $listValue = is_array($value) && array_is_list($value);

        if ($listValue && is_array($schema['items'] ?? null)) {
            foreach ($value as $index => $itemValue) {
                if ($this->isGeneratedOpenApiDtoObject($itemValue)) {
                    continue;
                }
                $errors = [
                    ...$errors,
                    ...$this->validateOpenApiNode($itemValue, $schema['items'], $path . '[' . $index . ']', $depth + 1),
                ];
            }
        }

        if (is_array($structuredValue)) {
            $extraPropertySchema = $schema['additionalProperties'] ?? null;
            $unevaluatedProperties = $schema['unevaluatedProperties'] ?? null;
            $evaluatedProperties = [...$definedProperties, ...$patternMatchedProperties];

            if ($extraPropertySchema === false || is_array($extraPropertySchema)) {
                foreach ($structuredValue as $propertyName => $propertyValue) {
                    if (!is_string($propertyName) || array_key_exists($propertyName, $evaluatedProperties)) {
                        continue;
                    }
                    if ($extraPropertySchema === false) {
                        $errors[] = sprintf('%s has additional property "%s" which is not allowed', $path, $propertyName);
                        continue;
                    }
                    if ($this->isGeneratedOpenApiDtoObject($propertyValue)) {
                        $evaluatedProperties[$propertyName] = true;
                        continue;
                    }
                    $errors = [
                        ...$errors,
                        ...$this->validateOpenApiNode($propertyValue, $extraPropertySchema, $this->openApiChildPath($path, $propertyName), $depth + 1),
                    ];
                    $evaluatedProperties[$propertyName] = true;
                }
            }

        }

        return $errors;
    }

    private function matchesOpenApiCallbackType(mixed $value, mixed $type): bool
    {
        if (!is_string($type) || $type === '') {
            return true;
        }

        return match ($type) {
            'integer' => is_int($value) || (is_float($value) && is_finite($value) && floor($value) === $value),
            'number' => is_int($value) || is_float($value),
            'string' => is_string($value),
            'array' => is_array($value) && array_is_list($value),
            'object' => is_array($value) || is_object($value),
            default => true,
        };
    }

    private function normalizeOpenApiStructuralValue(mixed $value): mixed
    {
        if (is_array($value) || !is_object($value)) {
            return $value;
        }

        // Generated DTOs expose their OpenAPI-named payload themselves.
        if (method_exists($value, 'toOpenApiValidationPayload')) {
            return $value->toOpenApiValidationPayload();
        }
        // Any other object: only its public state is visible, under PHP property names.
        return array_filter(
            get_object_vars($value),
            static fn(mixed $propertyValue): bool => $propertyValue !== null,
        );
    }

    private function isGeneratedOpenApiDtoObject(mixed $value): bool
    {
        // Such a DTO validates its own schema through its own #[Assert\Callback], reached by the
        // #[Assert\Valid] cascade — the enclosing DTO must not report the same errors again.
        return is_object($value)
            && method_exists($value, 'validateOpenApiConstraints')
            && method_exists($value, 'toOpenApiValidationPayload');
    }

    private function openApiChildPath(string $path, string $propertyName): string
    {
        // The first level names the field the way the runtime validator's caller does, so both
        // modes report the same subject: `field "tags" must ...`.
        return $path === 'payload'
            ? sprintf('field "%s"', $propertyName)
            : $path . '.' . $propertyName;
    }

    /**
     * @param array<int, string> $keys
     */
    private function schemaHasAnyOpenApiKey(array $schema, array $keys): bool
    {
        foreach ($keys as $key) {
            if (array_key_exists($key, $schema)) {
                return true;
            }
        }

        return false;
    }

    private function toFloatConstraint(mixed $value): ?float
    {
        return is_int($value) || is_float($value) ? (float)$value : null;
    }

    private function stringifyOpenApiNumber(float $value): string
    {
        $rendered = json_encode($value);
        return is_string($rendered) ? $rendered : (string)$value;
    }

    private function isValidOpenApiStringFormat(string $value, string $format): bool
    {
        return match ($format) {
            'date' => $this->isValidOpenApiDateFormat($value),
            default => true,
        };
    }

    private function isValidOpenApiDateFormat(string $value): bool
    {
        if (!preg_match('/^\d{4}-\d{2}-\d{2}$/', $value)) {
            return false;
        }
        $date = DateTimeImmutable::createFromFormat('Y-m-d', $value);
        return $date !== false && $date->format('Y-m-d') === $value;
    }
}

===== NullableContainerValues.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use Symfony\Component\Serializer\Attribute\Ignore;
use Symfony\Component\Validator\Constraints as Assert;
use Symfony\Component\Validator\Context\ExecutionContextInterface;

use function array_filter;
use function array_is_list;
use function array_key_exists;
use function floor;
use function get_object_vars;
use function is_array;
use function is_finite;
use function is_float;
use function is_int;
use function is_object;
use function is_string;
use function method_exists;
use function sprintf;
use function str_ends_with;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 */
final class NullableContainerValues
{
    private const array OPENAPI_VALIDATION_CONSTRAINTS = [
        'properties' => [
            'listOfNullable' => [
                'items' => [
                    'type' => 'string',
                    'nullable' => true,
                ],
            ],
            'mapOfNullable' => [
                'additionalProperties' => [
                    'type' => 'integer',
                    'nullable' => true,
                ],
            ],
            'listOfNullable31' => [
                'items' => [
                    'type' => 'string',
                    'nullable' => true,
                ],
            ],
            'mapOfNullable31' => [
                'additionalProperties' => [
                    'type' => 'integer',
                    'nullable' => true,
                ],
            ],
        ],
    ];

    private const array OPENAPI_PHP_TO_NAME_MAP = [
        'listOfNullable' => 'listOfNullable',
        'mapOfNullable' => 'mapOfNullable',
        'listOfNullable31' => 'listOfNullable31',
        'mapOfNullable31' => 'mapOfNullable31',
    ];

    private const array OPENAPI_PROVIDED_FLAGS = [
        'mapOfNullable' => 'mapOfNullableProvided',
        'listOfNullable31' => 'listOfNullable31Provided',
        'mapOfNullable31' => 'mapOfNullable31Provided',
    ];

    private const int OPENAPI_MAX_VALIDATION_DEPTH = 256;

    /**
     * @var ?array<string, ?int>
     */
    #[Assert\All([new Assert\Range(min: 5)])]
    private ?array $mapOfNullable = null;

    #[Ignore]
    private bool $mapOfNullableProvided = false;

    /**
     * @var ?array<?string>
     */
    private ?array $listOfNullable31 = null;

    #[Ignore]
    private bool $listOfNullable31Provided = false;

    /**
     * @var ?array<string, ?int>
     */
    private ?array $mapOfNullable31 = null;

    #[Ignore]
    private bool $mapOfNullable31Provided = false;

    /**
     * @param array<?string> $listOfNullable
     */
    public function __construct(
        #[Assert\NotNull]
        #[Assert\All([new Assert\Length(min: 2)])]
        private readonly array $listOfNullable,
    ) {
    }

    /**
     * @return array<?string>
     */
    public function getListOfNullable(): array
    {
        return $this->listOfNullable;
    }

    /**
     * @return ?array<string, ?int>
     */
    public function getMapOfNullable(): ?array
    {
        return $this->mapOfNullable;
    }

    /**
     * @param ?array<string, ?int> $mapOfNullable
     */
    public function setMapOfNullable(?array $mapOfNullable): void
    {
        $this->mapOfNullable = $mapOfNullable;
        $this->mapOfNullableProvided = true;
    }

    /**
     * Whether the payload carried this key — true even when its value was null. Set by the setter,
     * so it answers for a request the serializer built and for a DTO assembled by hand alike.
     */
    #[Ignore]
    public function isMapOfNullableProvided(): bool
    {
        return $this->mapOfNullableProvided;
    }

    /**
     * @return ?array<?string>
     */
    public function getListOfNullable31(): ?array
    {
        return $this->listOfNullable31;
    }

    /**
     * @param ?array<?string> $listOfNullable31
     */
    public function setListOfNullable31(?array $listOfNullable31): void
    {
        $this->listOfNullable31 = $listOfNullable31;
        $this->listOfNullable31Provided = true;
    }

    /**
     * Whether the payload carried this key — true even when its value was null. Set by the setter,
     * so it answers for a request the serializer built and for a DTO assembled by hand alike.
     */
    #[Ignore]
    public function isListOfNullable31Provided(): bool
    {
        return $this->listOfNullable31Provided;
    }

    /**
     * @return ?array<string, ?int>
     */
    public function getMapOfNullable31(): ?array
    {
        return $this->mapOfNullable31;
    }

    /**
     * @param ?array<string, ?int> $mapOfNullable31
     */
    public function setMapOfNullable31(?array $mapOfNullable31): void
    {
        $this->mapOfNullable31 = $mapOfNullable31;
        $this->mapOfNullable31Provided = true;
    }

    /**
     * Whether the payload carried this key — true even when its value was null. Set by the setter,
     * so it answers for a request the serializer built and for a DTO assembled by hand alike.
     */
    #[Ignore]
    public function isMapOfNullable31Provided(): bool
    {
        return $this->mapOfNullable31Provided;
    }

    #[Assert\Callback]
    public function validateOpenApiConstraints(ExecutionContextInterface $context): void
    {
        foreach ($this->validateOpenApiNode($this->toOpenApiValidationPayload(), self::OPENAPI_VALIDATION_CONSTRAINTS, 'payload', 0) as $error) {
            $context->buildViolation(str_ends_with($error, '.') ? $error : $error . '.')->addViolation();
        }
    }

    /**
     * This DTO as an OpenAPI-named payload — machinery for the generated validation, not an API to
     * call. It is public only because an enclosing DTO is a different class and needs this view of
     * a nested one. For output, use the Symfony serializer.
     *
     * @internal
     *
     * @return array<string, mixed>
     */
    public function toOpenApiValidationPayload(): array
    {
        $payload = [];
        foreach (self::OPENAPI_PHP_TO_NAME_MAP as $phpName => $openApiName) {
            // A required property always exists. An optional one belongs in the payload only when
            // it was actually provided — its presence flag says so, which is also what keeps the
            // flags themselves out of the payload (iterating the object's properties would put
            // them in and trip `additionalProperties: false`).
            $flag = self::OPENAPI_PROVIDED_FLAGS[$phpName] ?? null;
            if ($flag !== null && $this->{$flag} !== true) {
                continue;
            }
            $payload[$openApiName] = $this->{$phpName};
        }

        return $payload;
    }

    /**
     * @param array<string, mixed> $schema
     * @return array<int, string>
     */
    private function validateOpenApiNode(mixed $value, array $schema, string $path, int $depth): array
    {
        if ($depth >= self::OPENAPI_MAX_VALIDATION_DEPTH) {
            return [];
        }

        $errors = [];
        if ($value === null && ($schema['nullable'] ?? false) === true) {
            return [];
        }

        $normalizedValue = $value;
        $structuredValue = $this->normalizeOpenApiStructuralValue($value);

        if (array_key_exists('type', $schema)) {
            $typeConstraint = $schema['type'];
            if (is_string($typeConstraint)) {
                if (!$this->matchesOpenApiCallbackType($normalizedValue, $typeConstraint)) {
                    $errors[] = sprintf('%s must be of type %s', $path, $typeConstraint);
                }
            }
        }

        $definedProperties = [];
        $patternMatchedProperties = [];

        if (is_array($schema['properties'] ?? null) && is_array($structuredValue)) {
            foreach ($schema['properties'] as $propertyName => $propertySchema) {
                if (!is_string($propertyName) || !is_array($propertySchema)) {
                    continue;
                }
                $definedProperties[$propertyName] = true;
                if (!array_key_exists($propertyName, $structuredValue)) {
                    continue;
                }
                if ($this->isGeneratedOpenApiDtoObject($structuredValue[$propertyName])) {
                    continue;
                }
                $errors = [
                    ...$errors,
                    ...$this->validateOpenApiNode($structuredValue[$propertyName], $propertySchema, $this->openApiChildPath($path, $propertyName), $depth + 1),
                ];
            }
        }

        $listValue = is_array($value) && array_is_list($value);

        if ($listValue && is_array($schema['items'] ?? null)) {
            foreach ($value as $index => $itemValue) {
                if ($this->isGeneratedOpenApiDtoObject($itemValue)) {
                    continue;
                }
                $errors = [
                    ...$errors,
                    ...$this->validateOpenApiNode($itemValue, $schema['items'], $path . '[' . $index . ']', $depth + 1),
                ];
            }
        }

        if (is_array($structuredValue)) {
            $extraPropertySchema = $schema['additionalProperties'] ?? null;
            $unevaluatedProperties = $schema['unevaluatedProperties'] ?? null;
            $evaluatedProperties = [...$definedProperties, ...$patternMatchedProperties];

            if ($extraPropertySchema === false || is_array($extraPropertySchema)) {
                foreach ($structuredValue as $propertyName => $propertyValue) {
                    if (!is_string($propertyName) || array_key_exists($propertyName, $evaluatedProperties)) {
                        continue;
                    }
                    if ($extraPropertySchema === false) {
                        $errors[] = sprintf('%s has additional property "%s" which is not allowed', $path, $propertyName);
                        continue;
                    }
                    if ($this->isGeneratedOpenApiDtoObject($propertyValue)) {
                        $evaluatedProperties[$propertyName] = true;
                        continue;
                    }
                    $errors = [
                        ...$errors,
                        ...$this->validateOpenApiNode($propertyValue, $extraPropertySchema, $this->openApiChildPath($path, $propertyName), $depth + 1),
                    ];
                    $evaluatedProperties[$propertyName] = true;
                }
            }

        }

        return $errors;
    }

    private function matchesOpenApiCallbackType(mixed $value, mixed $type): bool
    {
        if (!is_string($type) || $type === '') {
            return true;
        }

        return match ($type) {
            'integer' => is_int($value) || (is_float($value) && is_finite($value) && floor($value) === $value),
            'string' => is_string($value),
            default => true,
        };
    }

    private function normalizeOpenApiStructuralValue(mixed $value): mixed
    {
        if (is_array($value) || !is_object($value)) {
            return $value;
        }

        // Generated DTOs expose their OpenAPI-named payload themselves.
        if (method_exists($value, 'toOpenApiValidationPayload')) {
            return $value->toOpenApiValidationPayload();
        }
        // Any other object: only its public state is visible, under PHP property names.
        return array_filter(
            get_object_vars($value),
            static fn(mixed $propertyValue): bool => $propertyValue !== null,
        );
    }

    private function isGeneratedOpenApiDtoObject(mixed $value): bool
    {
        // Such a DTO validates its own schema through its own #[Assert\Callback], reached by the
        // #[Assert\Valid] cascade — the enclosing DTO must not report the same errors again.
        return is_object($value)
            && method_exists($value, 'validateOpenApiConstraints')
            && method_exists($value, 'toOpenApiValidationPayload');
    }

    private function openApiChildPath(string $path, string $propertyName): string
    {
        // The first level names the field the way the runtime validator's caller does, so both
        // modes report the same subject: `field "tags" must ...`.
        return $path === 'payload'
            ? sprintf('field "%s"', $propertyName)
            : $path . '.' . $propertyName;
    }
}

===== Pet.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use Symfony\Component\Validator\Constraints as Assert;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 */
final class Pet
{
    public function __construct(
        #[Assert\NotNull]
        private readonly string $name,
    ) {
    }

    public function getName(): string
    {
        return $this->name;
    }
}

===== Shape.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use Symfony\Component\Serializer\Attribute\DiscriminatorMap;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 *
 * Members: Circle|Square
 */
#[DiscriminatorMap(typeProperty: 'kind', mapping: ['circle' => Circle::class, 'square' => Square::class])]
interface Shape
{
}

===== Square.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use Symfony\Component\Validator\Constraints as Assert;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 */
final class Square implements Shape
{
    public function __construct(
        #[Assert\NotNull]
        private readonly string $kind,
        #[Assert\NotNull]
        private readonly int $side,
    ) {
    }

    public function getKind(): string
    {
        return $this->kind;
    }

    public function getSide(): int
    {
        return $this->side;
    }
}

===== Tag.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use Symfony\Component\Validator\Constraints as Assert;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 */
final class Tag
{
    public function __construct(
        #[Assert\NotNull]
        private readonly int $id,
        #[Assert\NotNull]
        private readonly string $name,
    ) {
    }

    public function getId(): int
    {
        return $this->id;
    }

    public function getName(): string
    {
        return $this->name;
    }

    /**
     * This DTO as an OpenAPI-named payload: what it received, under the names the schema uses.
     * Machinery for the generated validation, not an API to call — an enclosing DTO reads a nested
     * one through here, because the properties themselves are private. For output, use the Symfony
     * serializer.
     *
     * @internal
     *
     * @return array<string, mixed>
     */
    public function toOpenApiValidationPayload(): array
    {
        $payload = [];
        $payload['id'] = $this->id;
        $payload['name'] = $this->name;

        return $payload;
    }
}

===== Test.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use Symfony\Component\Validator\Constraints as Assert;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 */
final class Test
{
    public function __construct(
        #[Assert\NotNull]
        private readonly int $id,
    ) {
    }

    public function getId(): int
    {
        return $this->id;
    }
}

===== Test2.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use Symfony\Component\Serializer\Attribute\Ignore;
use Symfony\Component\Validator\Constraints as Assert;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 */
final class Test2
{
    #[Assert\Valid]
    private ?User $user = null;

    #[Ignore]
    private bool $userProvided = false;

    #[Assert\Valid]
    private ?Test2UserType $userType = null;

    #[Ignore]
    private bool $userTypeProvided = false;

    /**
     * @param int $id this is id
     * @param string $name this is name
     * @param array<Tag> $tags
     */
    public function __construct(
        #[Assert\NotNull]
        private readonly int $id,
        #[Assert\NotNull]
        private readonly string $name,
        #[Assert\NotNull]
        #[Assert\Valid]
        private readonly array $tags,
        #[Assert\NotNull]
        private readonly Test2Types $types,
    ) {
    }

    public function getId(): int
    {
        return $this->id;
    }

    public function getName(): string
    {
        return $this->name;
    }

    /**
     * @return array<Tag>
     */
    public function getTags(): array
    {
        return $this->tags;
    }

    public function getTypes(): Test2Types
    {
        return $this->types;
    }

    public function getUser(): ?User
    {
        return $this->user;
    }

    public function setUser(?User $user): void
    {
        $this->user = $user;
        $this->userProvided = true;
    }

    /**
     * Whether the payload carried this key — true even when its value was null. Set by the setter,
     * so it answers for a request the serializer built and for a DTO assembled by hand alike.
     */
    #[Ignore]
    public function isUserProvided(): bool
    {
        return $this->userProvided;
    }

    public function getUserType(): ?Test2UserType
    {
        return $this->userType;
    }

    public function setUserType(?Test2UserType $userType): void
    {
        $this->userType = $userType;
        $this->userTypeProvided = true;
    }

    /**
     * Whether the payload carried this key — true even when its value was null. Set by the setter,
     * so it answers for a request the serializer built and for a DTO assembled by hand alike.
     */
    #[Ignore]
    public function isUserTypeProvided(): bool
    {
        return $this->userTypeProvided;
    }
}

===== Test2Types.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

/**
 * NOTE: This enum is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 *
 * From: Test2.types
 */
enum Test2Types: string
{
    case CAT = 'cat';
    case DOG = 'dog';
}

===== Test2UserType.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use Symfony\Component\Serializer\Attribute\Ignore;
use Symfony\Component\Validator\Constraints as Assert;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 *
 * From: Test2.userType
 */
final class Test2UserType
{
    private ?string $region = null;

    #[Ignore]
    private bool $regionProvided = false;

    public function __construct()
    {
    }

    public function getRegion(): ?string
    {
        return $this->region;
    }

    public function setRegion(?string $region): void
    {
        $this->region = $region;
        $this->regionProvided = true;
    }

    /**
     * Whether the payload carried this key — true even when its value was null. Set by the setter,
     * so it answers for a request the serializer built and for a DTO assembled by hand alike.
     */
    #[Ignore]
    public function isRegionProvided(): bool
    {
        return $this->regionProvided;
    }

    /**
     * This DTO as an OpenAPI-named payload: what it received, under the names the schema uses.
     * Machinery for the generated validation, not an API to call — an enclosing DTO reads a nested
     * one through here, because the properties themselves are private. For output, use the Symfony
     * serializer.
     *
     * @internal
     *
     * @return array<string, mixed>
     */
    public function toOpenApiValidationPayload(): array
    {
        $payload = [];
        if ($this->regionProvided) {
            $payload['region'] = $this->region;
        }

        return $payload;
    }
}

===== TestPatchRequest.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use Symfony\Component\Validator\Constraints as Assert;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 */
final class TestPatchRequest
{
    public function __construct(
        #[Assert\NotNull]
        private readonly int $id,
        #[Assert\NotNull]
        private readonly string $name,
        #[Assert\NotNull]
        private readonly string $surname,
    ) {
    }

    public function getId(): int
    {
        return $this->id;
    }

    public function getName(): string
    {
        return $this->name;
    }

    public function getSurname(): string
    {
        return $this->surname;
    }
}

===== TestPostRequest.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use Symfony\Component\Serializer\Attribute\Ignore;
use Symfony\Component\Validator\Constraints as Assert;
use Symfony\Component\Validator\Context\ExecutionContextInterface;

use function array_filter;
use function array_is_list;
use function array_key_exists;
use function array_keys;
use function array_pop;
use function array_unique;
use function array_values;
use function filter_var;
use function floor;
use function get_object_vars;
use function implode;
use function in_array;
use function is_array;
use function is_bool;
use function is_finite;
use function is_float;
use function is_int;
use function is_object;
use function is_scalar;
use function is_string;
use function json_encode;
use function ltrim;
use function method_exists;
use function preg_match;
use function preg_replace;
use function property_exists;
use function sprintf;
use function str_contains;
use function str_ends_with;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 */
final class TestPostRequest
{
    private const array OPENAPI_VALIDATION_CONSTRAINTS = [
        'properties' => [
            'id' => [],
            'name' => [],
            'code' => [
                'oneOf' => [
                    [
                        'type' => 'integer',
                        'minimum' => 10,
                        'maximum' => 100,
                    ],
                    [
                        'type' => 'string',
                        'format' => 'uuid',
                    ],
                ],
            ],
            'homepage' => [
                'format' => 'uri',
            ],
            'endpoint' => [
                'format' => 'uri-template',
            ],
        ],
    ];

    private const array OPENAPI_PHP_TO_NAME_MAP = [
        'id' => 'id',
        'name' => 'name',
        'code' => 'code',
        'homepage' => 'homepage',
        'endpoint' => 'endpoint',
    ];

    private const array OPENAPI_PROVIDED_FLAGS = [
        'homepage' => 'homepageProvided',
        'endpoint' => 'endpointProvided',
    ];

    private const int OPENAPI_MAX_VALIDATION_DEPTH = 256;

    private ?string $homepage = null;

    #[Ignore]
    private bool $homepageProvided = false;

    private ?string $endpoint = null;

    #[Ignore]
    private bool $endpointProvided = false;

    public function __construct(
        #[Assert\NotNull]
        private readonly int $id,
        #[Assert\NotNull]
        private readonly string $name,
        #[Assert\NotNull]
        private readonly int|string $code,
    ) {
    }

    public function getId(): int
    {
        return $this->id;
    }

    public function getName(): string
    {
        return $this->name;
    }

    public function getCode(): int|string
    {
        return $this->code;
    }

    public function getHomepage(): ?string
    {
        return $this->homepage;
    }

    public function setHomepage(?string $homepage): void
    {
        $this->homepage = $homepage;
        $this->homepageProvided = true;
    }

    /**
     * Whether the payload carried this key — true even when its value was null. Set by the setter,
     * so it answers for a request the serializer built and for a DTO assembled by hand alike.
     */
    #[Ignore]
    public function isHomepageProvided(): bool
    {
        return $this->homepageProvided;
    }

    public function getEndpoint(): ?string
    {
        return $this->endpoint;
    }

    public function setEndpoint(?string $endpoint): void
    {
        $this->endpoint = $endpoint;
        $this->endpointProvided = true;
    }

    /**
     * Whether the payload carried this key — true even when its value was null. Set by the setter,
     * so it answers for a request the serializer built and for a DTO assembled by hand alike.
     */
    #[Ignore]
    public function isEndpointProvided(): bool
    {
        return $this->endpointProvided;
    }

    #[Assert\Callback]
    public function validateOpenApiConstraints(ExecutionContextInterface $context): void
    {
        foreach ($this->validateOpenApiNode($this->toOpenApiValidationPayload(), self::OPENAPI_VALIDATION_CONSTRAINTS, 'payload', 0) as $error) {
            $context->buildViolation(str_ends_with($error, '.') ? $error : $error . '.')->addViolation();
        }
    }

    /**
     * This DTO as an OpenAPI-named payload — machinery for the generated validation, not an API to
     * call. It is public only because an enclosing DTO is a different class and needs this view of
     * a nested one. For output, use the Symfony serializer.
     *
     * @internal
     *
     * @return array<string, mixed>
     */
    public function toOpenApiValidationPayload(): array
    {
        $payload = [];
        foreach (self::OPENAPI_PHP_TO_NAME_MAP as $phpName => $openApiName) {
            // A required property always exists. An optional one belongs in the payload only when
            // it was actually provided — its presence flag says so, which is also what keeps the
            // flags themselves out of the payload (iterating the object's properties would put
            // them in and trip `additionalProperties: false`).
            $flag = self::OPENAPI_PROVIDED_FLAGS[$phpName] ?? null;
            if ($flag !== null && $this->{$flag} !== true) {
                continue;
            }
            $payload[$openApiName] = $this->{$phpName};
        }

        return $payload;
    }

    /**
     * @param array<string, mixed> $schema
     * @return array<int, string>
     */
    private function validateOpenApiNode(mixed $value, array $schema, string $path, int $depth): array
    {
        if ($depth >= self::OPENAPI_MAX_VALIDATION_DEPTH) {
            return [];
        }

        $errors = [];
        $normalizedValue = $value;
        $structuredValue = $this->normalizeOpenApiStructuralValue($value);

        if (is_array($schema['oneOf'] ?? null)) {
            $matchingBranches = 0;
            $branchErrors = [];
            foreach ($schema['oneOf'] as $branch) {
                if (!is_array($branch)) {
                    continue;
                }

                if (is_string($branchClass = $branch['x-php-instanceof'] ?? null) && $branchClass !== '') {
                    $branchFqcn = __NAMESPACE__ . '\\' . ltrim($branchClass, '\\');
                    if (is_object($value) && $value instanceof $branchFqcn) {
                        $matchingBranches++;
                    }
                    continue;
                }

                if (array_key_exists('type', $branch) && !$this->matchesOpenApiCallbackType($normalizedValue, $branch['type'])) {
                    continue;
                }
                // Keep why each branch failed: with no match at all those reasons are far more
                // useful than "does not match any oneOf branch".
                $errorsForBranch = $this->validateOpenApiNode($value, $branch, $path, $depth + 1);
                if ($errorsForBranch === []) {
                    $matchingBranches++;
                    continue;
                }

                $branchErrors = [...$branchErrors, ...$errorsForBranch];
            }

            if ($matchingBranches > 1) {
                $errors[] = sprintf('%s matches more than one allowed oneOf branch', $path);
            } elseif ($matchingBranches === 0) {
                $errors = $branchErrors === []
                    ? [...$errors, $this->describeOpenApiUnionMismatch($path, 'oneOf', $schema['oneOf'], $value)]
                    : [...$errors, ...array_values(array_unique($branchErrors))];
            }

            if ($matchingBranches === 1) {
                $discriminatorProperty = is_string($schema['x-discriminator-property'] ?? null)
                    ? $schema['x-discriminator-property']
                    : null;
                $discriminatorPhpProperty = is_string($schema['x-discriminator-php-property'] ?? null)
                    ? $schema['x-discriminator-php-property']
                    : null;
                $discriminatorMap = is_array($schema['x-discriminator-map'] ?? null)
                    ? $schema['x-discriminator-map']
                    : null;

                if ($discriminatorProperty !== null && $discriminatorMap !== null && is_object($value)) {
                    $phpProperty = $discriminatorPhpProperty ?? $discriminatorProperty;
                    // The DTO keeps its state private: read the discriminator from the payload it
                    // reports, keyed by the OpenAPI name, and fall back to a public property for a
                    // hand-written object.
                    $discriminatorPayload = $this->normalizeOpenApiStructuralValue($value);
                    $discriminatorCarrier = is_array($discriminatorPayload) ? $discriminatorPayload : [];
                    if (!array_key_exists($discriminatorProperty, $discriminatorCarrier) && !property_exists($value, $phpProperty)) {
                        $errors[] = sprintf('%s discriminator property %s is required', $path, $discriminatorProperty);
                    } else {
                        $discriminatorValue = $discriminatorCarrier[$discriminatorProperty]
                            ?? (property_exists($value, $phpProperty) ? $value->{$phpProperty} : null);
                        $mapKey = is_scalar($discriminatorValue) ? (string)$discriminatorValue : null;
                        if ($mapKey === null || !array_key_exists($mapKey, $discriminatorMap)) {
                            $errors[] = sprintf(
                                '%s discriminator %s must be one of: %s',
                                $path,
                                $discriminatorProperty,
                                implode(', ', array_keys($discriminatorMap)),
                            );
                        } elseif (is_string($expectedClass = $discriminatorMap[$mapKey]) && $expectedClass !== '') {
                            $expectedFqcn = __NAMESPACE__ . '\\' . ltrim($expectedClass, '\\');
                            if (!$value instanceof $expectedFqcn) {
                                $errors[] = sprintf('%s discriminator %s must match concrete class', $path, $discriminatorProperty);
                            }
                        }
                    }
                }
            }
        }

        if (array_key_exists('type', $schema)) {
            $typeConstraint = $schema['type'];
            if (is_string($typeConstraint)) {
                if (!$this->matchesOpenApiCallbackType($normalizedValue, $typeConstraint)) {
                    $errors[] = sprintf('%s must be of type %s', $path, $typeConstraint);
                }
            }
        }

        if ((is_int($normalizedValue) || is_float($normalizedValue)) && $this->schemaHasAnyOpenApiKey($schema, ['minimum', 'maximum'])) {
            $minimum = $this->toFloatConstraint($schema['minimum'] ?? null);
            if ($minimum !== null && !($normalizedValue >= $minimum)) {
                $errors[] = sprintf('%s must be greater than or equal to %s', $path, $this->stringifyOpenApiNumber($minimum));
            }
            $maximum = $this->toFloatConstraint($schema['maximum'] ?? null);
            if ($maximum !== null && !($normalizedValue <= $maximum)) {
                $errors[] = sprintf('%s must be less than or equal to %s', $path, $this->stringifyOpenApiNumber($maximum));
            }
        }

        if (is_string($normalizedValue) && $this->schemaHasAnyOpenApiKey($schema, ['format'])) {
            if (is_string($format = $schema['format'] ?? null) && !$this->isValidOpenApiStringFormat($normalizedValue, $format)) {
                $errors[] = sprintf('%s must match format %s', $path, $format);
            }
        }

        if (is_array($schema['properties'] ?? null) && is_array($structuredValue)) {
            foreach ($schema['properties'] as $propertyName => $propertySchema) {
                if (!is_string($propertyName) || !is_array($propertySchema)) {
                    continue;
                }
                if (!array_key_exists($propertyName, $structuredValue)) {
                    continue;
                }
                if ($this->isGeneratedOpenApiDtoObject($structuredValue[$propertyName])) {
                    continue;
                }
                $errors = [
                    ...$errors,
                    ...$this->validateOpenApiNode($structuredValue[$propertyName], $propertySchema, $this->openApiChildPath($path, $propertyName), $depth + 1),
                ];
            }
        }

        return $errors;
    }

    /**
     * @param array<string, mixed> $schema
     */
    private function matchesOpenApiNode(mixed $value, array $schema, int $depth): bool
    {
        return $this->validateOpenApiNode($value, $schema, 'payload', $depth) === [];
    }

    /**
     * Every branch was gated out by its declared type, so no branch has a reason to report. Naming the
     * types the union does accept turns an unactionable sentence into one the caller can act on. Same
     * wording as the runtime validator, so a payload reads the same whichever mode answered it.
     *
     * @param array<int, mixed> $branches
     */
    private function describeOpenApiUnionMismatch(string $path, string $kind, array $branches, mixed $value): string
    {
        $types = [];
        foreach ($branches as $branch) {
            if (!is_array($branch)) {
                continue;
            }

            $declared = is_array($branch['type'] ?? null) ? $branch['type'] : [$branch['type'] ?? null];
            foreach ($declared as $type) {
                if (is_string($type) && $type !== '' && !in_array($type, $types, true)) {
                    $types[] = $type;
                }
            }
        }

        if ($types === []) {
            return sprintf('%s does not match any %s branch', $path, $kind);
        }

        $last = array_pop($types);
        $expected = $types === [] ? $last : implode(', ', $types) . ' or ' . $last;

        return sprintf(
            '%s does not match any %s branch (expected %s, got %s)',
            $path,
            $kind,
            $expected,
            match (true) {
                $value === null => 'null',
                is_bool($value) => 'boolean',
                is_int($value) => 'integer',
                is_float($value) => 'number',
                is_string($value) => 'string',
                is_array($value) => array_is_list($value) ? 'array' : 'object',
                default => 'object',
            },
        );
    }

    private function matchesOpenApiCallbackType(mixed $value, mixed $type): bool
    {
        if (!is_string($type) || $type === '') {
            return true;
        }

        return match ($type) {
            'integer' => is_int($value) || (is_float($value) && is_finite($value) && floor($value) === $value),
            'string' => is_string($value),
            default => true,
        };
    }

    private function normalizeOpenApiStructuralValue(mixed $value): mixed
    {
        if (is_array($value) || !is_object($value)) {
            return $value;
        }

        // Generated DTOs expose their OpenAPI-named payload themselves.
        if (method_exists($value, 'toOpenApiValidationPayload')) {
            return $value->toOpenApiValidationPayload();
        }
        // Any other object: only its public state is visible, under PHP property names.
        return array_filter(
            get_object_vars($value),
            static fn(mixed $propertyValue): bool => $propertyValue !== null,
        );
    }

    private function isGeneratedOpenApiDtoObject(mixed $value): bool
    {
        // Such a DTO validates its own schema through its own #[Assert\Callback], reached by the
        // #[Assert\Valid] cascade — the enclosing DTO must not report the same errors again.
        return is_object($value)
            && method_exists($value, 'validateOpenApiConstraints')
            && method_exists($value, 'toOpenApiValidationPayload');
    }

    private function openApiChildPath(string $path, string $propertyName): string
    {
        // The first level names the field the way the runtime validator's caller does, so both
        // modes report the same subject: `field "tags" must ...`.
        return $path === 'payload'
            ? sprintf('field "%s"', $propertyName)
            : $path . '.' . $propertyName;
    }

    /**
     * @param array<int, string> $keys
     */
    private function schemaHasAnyOpenApiKey(array $schema, array $keys): bool
    {
        foreach ($keys as $key) {
            if (array_key_exists($key, $schema)) {
                return true;
            }
        }

        return false;
    }

    private function toFloatConstraint(mixed $value): ?float
    {
        return is_int($value) || is_float($value) ? (float)$value : null;
    }

    private function stringifyOpenApiNumber(float $value): string
    {
        $rendered = json_encode($value);
        return is_string($rendered) ? $rendered : (string)$value;
    }

    private function isValidOpenApiStringFormat(string $value, string $format): bool
    {
        return match ($format) {
            'uuid' => $this->isValidOpenApiUuid($value),
            'uri' => filter_var($value, FILTER_VALIDATE_URL) !== false,
            'uri-template' => $this->isValidOpenApiUriTemplate($value),
            default => true,
        };
    }

    private function isValidOpenApiUuid(string $value): bool
    {
        return preg_match('/^[0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}$/', $value) === 1;
    }

    private function isValidOpenApiUriReference(string $value): bool
    {
        return preg_match('/[\s\x00-\x1F\x7F]/u', $value) !== 1;
    }

    private function isValidOpenApiUriTemplate(string $value): bool
    {
        // RFC 6570: a URI-reference that may embed {expression} blocks. Every brace has to belong to a
        // well-formed expression — an optional operator followed by a comma-separated list of varspecs,
        // each a varname with an optional ":" prefix-length or "*" explode modifier. Strip the valid
        // expressions; a surviving brace means the template is malformed.
        if (!$this->isValidOpenApiUriReference($value)) {
            return false;
        }

        if (!str_contains($value, '{')) {
            return !str_contains($value, '}');
        }

        $expression = '\{[+#./;?&=,!@|]?(?:[\p{L}\p{N}_%]+(?::[1-9][0-9]{0,3}|\*)?)'
            . '(?:,[\p{L}\p{N}_%]+(?::[1-9][0-9]{0,3}|\*)?)*\}';
        $stripped = preg_replace('~' . $expression . '~u', '', $value);

        return $stripped !== null && !str_contains($stripped, '{') && !str_contains($stripped, '}');
    }
}

===== TreeNode.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use Symfony\Component\Validator\Constraints as Assert;
use Symfony\Component\Validator\Context\ExecutionContextInterface;

use function array_filter;
use function array_is_list;
use function array_key_exists;
use function array_keys;
use function array_pop;
use function array_unique;
use function array_values;
use function floor;
use function get_object_vars;
use function implode;
use function in_array;
use function is_array;
use function is_bool;
use function is_finite;
use function is_float;
use function is_int;
use function is_object;
use function is_scalar;
use function is_string;
use function ltrim;
use function mb_strlen;
use function method_exists;
use function property_exists;
use function sprintf;
use function str_ends_with;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 */
final class TreeNode
{
    private const array OPENAPI_VALIDATION_CONSTRAINTS = [
        'properties' => [
            'label' => [],
            'children' => [
                'additionalProperties' => [
                    'oneOf' => [
                        [
                            'type' => 'object',
                            'required' => [
                                'label',
                                'children',
                            ],
                            'properties' => [
                                'label' => [
                                    'type' => 'string',
                                    'minLength' => 1,
                                ],
                                'children' => [
                                    'type' => 'object',
                                    'additionalProperties' => [
                                        'oneOf' => [
                                            [
                                                'type' => 'object',
                                                'required' => [
                                                    'label',
                                                    'children',
                                                ],
                                                'properties' => [
                                                    'label' => [
                                                        'type' => 'string',
                                                        'minLength' => 1,
                                                    ],
                                                    'children' => [
                                                        'type' => 'object',
                                                        'additionalProperties' => [
                                                            'oneOf' => [
                                                                [
                                                                    'x-php-instanceof' => 'TreeNode',
                                                                ],
                                                                [
                                                                    'type' => 'object',
                                                                    'additionalProperties' => [
                                                                        'type' => 'object',
                                                                        'required' => [
                                                                            'id',
                                                                            'name',
                                                                        ],
                                                                        'properties' => [
                                                                            'id' => [
                                                                                'type' => 'integer',
                                                                            ],
                                                                            'name' => [
                                                                                'type' => 'string',
                                                                            ],
                                                                        ],
                                                                    ],
                                                                ],
                                                            ],
                                                        ],
                                                    ],
                                                ],
                                            ],
                                            [
                                                'type' => 'object',
                                                'additionalProperties' => [
                                                    'type' => 'object',
                                                    'required' => [
                                                        'id',
                                                        'name',
                                                    ],
                                                    'properties' => [
                                                        'id' => [
                                                            'type' => 'integer',
                                                        ],
                                                        'name' => [
                                                            'type' => 'string',
                                                        ],
                                                    ],
                                                ],
                                            ],
                                        ],
                                    ],
                                ],
                            ],
                        ],
                        [
                            'type' => 'object',
                            'additionalProperties' => [
                                'type' => 'object',
                                'required' => [
                                    'id',
                                    'name',
                                ],
                                'properties' => [
                                    'id' => [
                                        'type' => 'integer',
                                    ],
                                    'name' => [
                                        'type' => 'string',
                                    ],
                                ],
                            ],
                        ],
                    ],
                ],
            ],
        ],
    ];

    private const array OPENAPI_PHP_TO_NAME_MAP = [
        'label' => 'label',
        'children' => 'children',
    ];

    private const array OPENAPI_PROVIDED_FLAGS = [];

    private const int OPENAPI_MAX_VALIDATION_DEPTH = 256;

    /**
     * @param array<string, mixed> $children
     */
    public function __construct(
        #[Assert\NotNull]
        #[Assert\Length(min: 1)]
        private readonly string $label,
        #[Assert\NotNull]
        private readonly array $children,
    ) {
    }

    public function getLabel(): string
    {
        return $this->label;
    }

    /**
     * @return array<string, mixed>
     */
    public function getChildren(): array
    {
        return $this->children;
    }

    #[Assert\Callback]
    public function validateOpenApiConstraints(ExecutionContextInterface $context): void
    {
        foreach ($this->validateOpenApiNode($this->toOpenApiValidationPayload(), self::OPENAPI_VALIDATION_CONSTRAINTS, 'payload', 0) as $error) {
            $context->buildViolation(str_ends_with($error, '.') ? $error : $error . '.')->addViolation();
        }
    }

    /**
     * This DTO as an OpenAPI-named payload — machinery for the generated validation, not an API to
     * call. It is public only because an enclosing DTO is a different class and needs this view of
     * a nested one. For output, use the Symfony serializer.
     *
     * @internal
     *
     * @return array<string, mixed>
     */
    public function toOpenApiValidationPayload(): array
    {
        $payload = [];
        foreach (self::OPENAPI_PHP_TO_NAME_MAP as $phpName => $openApiName) {
            // A required property always exists. An optional one belongs in the payload only when
            // it was actually provided — its presence flag says so, which is also what keeps the
            // flags themselves out of the payload (iterating the object's properties would put
            // them in and trip `additionalProperties: false`).
            $flag = self::OPENAPI_PROVIDED_FLAGS[$phpName] ?? null;
            if ($flag !== null && $this->{$flag} !== true) {
                continue;
            }
            $payload[$openApiName] = $this->{$phpName};
        }

        return $payload;
    }

    /**
     * @param array<string, mixed> $schema
     * @return array<int, string>
     */
    private function validateOpenApiNode(mixed $value, array $schema, string $path, int $depth): array
    {
        if ($depth >= self::OPENAPI_MAX_VALIDATION_DEPTH) {
            return [];
        }

        $errors = [];
        $normalizedValue = $value;
        $structuredValue = $this->normalizeOpenApiStructuralValue($value);

        if (is_array($schema['oneOf'] ?? null)) {
            $matchingBranches = 0;
            $branchErrors = [];
            foreach ($schema['oneOf'] as $branch) {
                if (!is_array($branch)) {
                    continue;
                }

                if (is_string($branchClass = $branch['x-php-instanceof'] ?? null) && $branchClass !== '') {
                    $branchFqcn = __NAMESPACE__ . '\\' . ltrim($branchClass, '\\');
                    if (is_object($value) && $value instanceof $branchFqcn) {
                        $matchingBranches++;
                    }
                    continue;
                }

                if (array_key_exists('type', $branch) && !$this->matchesOpenApiCallbackType($normalizedValue, $branch['type'])) {
                    continue;
                }
                // Keep why each branch failed: with no match at all those reasons are far more
                // useful than "does not match any oneOf branch".
                $errorsForBranch = $this->validateOpenApiNode($value, $branch, $path, $depth + 1);
                if ($errorsForBranch === []) {
                    $matchingBranches++;
                    continue;
                }

                $branchErrors = [...$branchErrors, ...$errorsForBranch];
            }

            if ($matchingBranches > 1) {
                $errors[] = sprintf('%s matches more than one allowed oneOf branch', $path);
            } elseif ($matchingBranches === 0) {
                $errors = $branchErrors === []
                    ? [...$errors, $this->describeOpenApiUnionMismatch($path, 'oneOf', $schema['oneOf'], $value)]
                    : [...$errors, ...array_values(array_unique($branchErrors))];
            }

            if ($matchingBranches === 1) {
                $discriminatorProperty = is_string($schema['x-discriminator-property'] ?? null)
                    ? $schema['x-discriminator-property']
                    : null;
                $discriminatorPhpProperty = is_string($schema['x-discriminator-php-property'] ?? null)
                    ? $schema['x-discriminator-php-property']
                    : null;
                $discriminatorMap = is_array($schema['x-discriminator-map'] ?? null)
                    ? $schema['x-discriminator-map']
                    : null;

                if ($discriminatorProperty !== null && $discriminatorMap !== null && is_object($value)) {
                    $phpProperty = $discriminatorPhpProperty ?? $discriminatorProperty;
                    // The DTO keeps its state private: read the discriminator from the payload it
                    // reports, keyed by the OpenAPI name, and fall back to a public property for a
                    // hand-written object.
                    $discriminatorPayload = $this->normalizeOpenApiStructuralValue($value);
                    $discriminatorCarrier = is_array($discriminatorPayload) ? $discriminatorPayload : [];
                    if (!array_key_exists($discriminatorProperty, $discriminatorCarrier) && !property_exists($value, $phpProperty)) {
                        $errors[] = sprintf('%s discriminator property %s is required', $path, $discriminatorProperty);
                    } else {
                        $discriminatorValue = $discriminatorCarrier[$discriminatorProperty]
                            ?? (property_exists($value, $phpProperty) ? $value->{$phpProperty} : null);
                        $mapKey = is_scalar($discriminatorValue) ? (string)$discriminatorValue : null;
                        if ($mapKey === null || !array_key_exists($mapKey, $discriminatorMap)) {
                            $errors[] = sprintf(
                                '%s discriminator %s must be one of: %s',
                                $path,
                                $discriminatorProperty,
                                implode(', ', array_keys($discriminatorMap)),
                            );
                        } elseif (is_string($expectedClass = $discriminatorMap[$mapKey]) && $expectedClass !== '') {
                            $expectedFqcn = __NAMESPACE__ . '\\' . ltrim($expectedClass, '\\');
                            if (!$value instanceof $expectedFqcn) {
                                $errors[] = sprintf('%s discriminator %s must match concrete class', $path, $discriminatorProperty);
                            }
                        }
                    }
                }
            }
        }

        if (array_key_exists('type', $schema)) {
            $typeConstraint = $schema['type'];
            if (is_string($typeConstraint)) {
                if (!$this->matchesOpenApiCallbackType($normalizedValue, $typeConstraint)) {
                    $errors[] = sprintf('%s must be of type %s', $path, $typeConstraint);
                }
            }
        }

        if (is_string($normalizedValue) && $this->schemaHasAnyOpenApiKey($schema, ['minLength'])) {
            $length = mb_strlen($normalizedValue);
            $minLength = $this->toIntConstraint($schema['minLength'] ?? null);
            if ($minLength !== null && $length < $minLength) {
                $errors[] = sprintf('%s length must be at least %d characters', $path, $minLength);
            }
        }

        if (is_array($structuredValue) && is_array($schema['required'] ?? null)) {
            foreach ($schema['required'] as $requiredProp) {
                if (is_string($requiredProp) && !array_key_exists($requiredProp, $structuredValue)) {
                    $errors[] = sprintf('%s is required', $this->openApiChildPath($path, $requiredProp));
                }
            }
        }

        $definedProperties = [];
        $patternMatchedProperties = [];

        if (is_array($schema['properties'] ?? null) && is_array($structuredValue)) {
            foreach ($schema['properties'] as $propertyName => $propertySchema) {
                if (!is_string($propertyName) || !is_array($propertySchema)) {
                    continue;
                }
                $definedProperties[$propertyName] = true;
                if (!array_key_exists($propertyName, $structuredValue)) {
                    continue;
                }
                if ($this->isGeneratedOpenApiDtoObject($structuredValue[$propertyName])) {
                    continue;
                }
                $errors = [
                    ...$errors,
                    ...$this->validateOpenApiNode($structuredValue[$propertyName], $propertySchema, $this->openApiChildPath($path, $propertyName), $depth + 1),
                ];
            }
        }

        if (is_array($structuredValue)) {
            $extraPropertySchema = $schema['additionalProperties'] ?? null;
            $unevaluatedProperties = $schema['unevaluatedProperties'] ?? null;
            $evaluatedProperties = [...$definedProperties, ...$patternMatchedProperties];

            if ($extraPropertySchema === false || is_array($extraPropertySchema)) {
                foreach ($structuredValue as $propertyName => $propertyValue) {
                    if (!is_string($propertyName) || array_key_exists($propertyName, $evaluatedProperties)) {
                        continue;
                    }
                    if ($extraPropertySchema === false) {
                        $errors[] = sprintf('%s has additional property "%s" which is not allowed', $path, $propertyName);
                        continue;
                    }
                    if ($this->isGeneratedOpenApiDtoObject($propertyValue)) {
                        $evaluatedProperties[$propertyName] = true;
                        continue;
                    }
                    $errors = [
                        ...$errors,
                        ...$this->validateOpenApiNode($propertyValue, $extraPropertySchema, $this->openApiChildPath($path, $propertyName), $depth + 1),
                    ];
                    $evaluatedProperties[$propertyName] = true;
                }
            }

        }

        return $errors;
    }

    /**
     * @param array<string, mixed> $schema
     */
    private function matchesOpenApiNode(mixed $value, array $schema, int $depth): bool
    {
        return $this->validateOpenApiNode($value, $schema, 'payload', $depth) === [];
    }

    /**
     * Every branch was gated out by its declared type, so no branch has a reason to report. Naming the
     * types the union does accept turns an unactionable sentence into one the caller can act on. Same
     * wording as the runtime validator, so a payload reads the same whichever mode answered it.
     *
     * @param array<int, mixed> $branches
     */
    private function describeOpenApiUnionMismatch(string $path, string $kind, array $branches, mixed $value): string
    {
        $types = [];
        foreach ($branches as $branch) {
            if (!is_array($branch)) {
                continue;
            }

            $declared = is_array($branch['type'] ?? null) ? $branch['type'] : [$branch['type'] ?? null];
            foreach ($declared as $type) {
                if (is_string($type) && $type !== '' && !in_array($type, $types, true)) {
                    $types[] = $type;
                }
            }
        }

        if ($types === []) {
            return sprintf('%s does not match any %s branch', $path, $kind);
        }

        $last = array_pop($types);
        $expected = $types === [] ? $last : implode(', ', $types) . ' or ' . $last;

        return sprintf(
            '%s does not match any %s branch (expected %s, got %s)',
            $path,
            $kind,
            $expected,
            match (true) {
                $value === null => 'null',
                is_bool($value) => 'boolean',
                is_int($value) => 'integer',
                is_float($value) => 'number',
                is_string($value) => 'string',
                is_array($value) => array_is_list($value) ? 'array' : 'object',
                default => 'object',
            },
        );
    }

    private function matchesOpenApiCallbackType(mixed $value, mixed $type): bool
    {
        if (!is_string($type) || $type === '') {
            return true;
        }

        return match ($type) {
            'integer' => is_int($value) || (is_float($value) && is_finite($value) && floor($value) === $value),
            'string' => is_string($value),
            'object' => is_array($value) || is_object($value),
            default => true,
        };
    }

    private function normalizeOpenApiStructuralValue(mixed $value): mixed
    {
        if (is_array($value) || !is_object($value)) {
            return $value;
        }

        // Generated DTOs expose their OpenAPI-named payload themselves.
        if (method_exists($value, 'toOpenApiValidationPayload')) {
            return $value->toOpenApiValidationPayload();
        }
        // Any other object: only its public state is visible, under PHP property names.
        return array_filter(
            get_object_vars($value),
            static fn(mixed $propertyValue): bool => $propertyValue !== null,
        );
    }

    private function isGeneratedOpenApiDtoObject(mixed $value): bool
    {
        // Such a DTO validates its own schema through its own #[Assert\Callback], reached by the
        // #[Assert\Valid] cascade — the enclosing DTO must not report the same errors again.
        return is_object($value)
            && method_exists($value, 'validateOpenApiConstraints')
            && method_exists($value, 'toOpenApiValidationPayload');
    }

    private function openApiChildPath(string $path, string $propertyName): string
    {
        // The first level names the field the way the runtime validator's caller does, so both
        // modes report the same subject: `field "tags" must ...`.
        return $path === 'payload'
            ? sprintf('field "%s"', $propertyName)
            : $path . '.' . $propertyName;
    }

    /**
     * @param array<int, string> $keys
     */
    private function schemaHasAnyOpenApiKey(array $schema, array $keys): bool
    {
        foreach ($keys as $key) {
            if (array_key_exists($key, $schema)) {
                return true;
            }
        }

        return false;
    }

    private function toIntConstraint(mixed $value): ?int
    {
        if (is_int($value)) {
            return $value;
        }
        if (is_float($value) && floor($value) === $value) {
            return (int)$value;
        }

        return null;
    }
}

===== TypeEnum.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

/**
 * NOTE: This enum is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 */
enum TypeEnum: string
{
    case CAT_TYPE = 'catType';
    case DOG_TYPE = 'dogType';
}

===== UnionUnderContainer.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use Symfony\Component\Serializer\Attribute\Ignore;
use Symfony\Component\Validator\Constraints as Assert;
use Symfony\Component\Validator\Context\ExecutionContextInterface;

use function array_filter;
use function array_is_list;
use function array_key_exists;
use function array_keys;
use function array_pop;
use function array_unique;
use function array_values;
use function floor;
use function get_object_vars;
use function implode;
use function in_array;
use function is_array;
use function is_bool;
use function is_finite;
use function is_float;
use function is_int;
use function is_object;
use function is_scalar;
use function is_string;
use function ltrim;
use function mb_strlen;
use function method_exists;
use function property_exists;
use function sprintf;
use function str_ends_with;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 */
final class UnionUnderContainer
{
    private const array OPENAPI_VALIDATION_CONSTRAINTS = [
        'properties' => [
            'chain' => [
                'items' => [
                    'oneOf' => [
                        [
                            'type' => 'object',
                            'required' => [
                                'links',
                            ],
                            'properties' => [
                                'links' => [
                                    'type' => 'array',
                                    'items' => [
                                        'type' => 'object',
                                        'required' => [
                                            'code',
                                        ],
                                        'properties' => [
                                            'code' => [
                                                'type' => 'string',
                                                'minLength' => 3,
                                            ],
                                            'tags' => [
                                                'type' => 'array',
                                                'items' => [
                                                    'type' => 'object',
                                                    'required' => [
                                                        'label',
                                                    ],
                                                    'properties' => [
                                                        'label' => [
                                                            'type' => 'string',
                                                            'minLength' => 2,
                                                        ],
                                                    ],
                                                ],
                                            ],
                                        ],
                                    ],
                                ],
                            ],
                        ],
                    ],
                ],
            ],
            'listOfUnions' => [
                'items' => [
                    'anyOf' => [
                        [
                            'type' => 'object',
                            'required' => [
                                'id',
                                'name',
                            ],
                            'properties' => [
                                'id' => [
                                    'type' => 'integer',
                                ],
                                'name' => [
                                    'type' => 'string',
                                ],
                            ],
                        ],
                        [
                            'type' => 'string',
                            'minLength' => 2,
                        ],
                    ],
                ],
            ],
        ],
    ];

    private const array OPENAPI_PHP_TO_NAME_MAP = [
        'tree' => 'tree',
        'chain' => 'chain',
        'listOfUnions' => 'listOfUnions',
    ];

    private const array OPENAPI_PROVIDED_FLAGS = [
        'chain' => 'chainProvided',
        'listOfUnions' => 'listOfUnionsProvided',
    ];

    private const int OPENAPI_MAX_VALIDATION_DEPTH = 256;

    private ?array $chain = null;

    #[Ignore]
    private bool $chainProvided = false;

    private ?array $listOfUnions = null;

    #[Ignore]
    private bool $listOfUnionsProvided = false;

    public function __construct(
        #[Assert\NotNull]
        #[Assert\Valid]
        private readonly TreeNode $tree,
    ) {
    }

    public function getTree(): TreeNode
    {
        return $this->tree;
    }

    public function getChain(): ?array
    {
        return $this->chain;
    }

    public function setChain(?array $chain): void
    {
        $this->chain = $chain;
        $this->chainProvided = true;
    }

    /**
     * Whether the payload carried this key — true even when its value was null. Set by the setter,
     * so it answers for a request the serializer built and for a DTO assembled by hand alike.
     */
    #[Ignore]
    public function isChainProvided(): bool
    {
        return $this->chainProvided;
    }

    public function getListOfUnions(): ?array
    {
        return $this->listOfUnions;
    }

    public function setListOfUnions(?array $listOfUnions): void
    {
        $this->listOfUnions = $listOfUnions;
        $this->listOfUnionsProvided = true;
    }

    /**
     * Whether the payload carried this key — true even when its value was null. Set by the setter,
     * so it answers for a request the serializer built and for a DTO assembled by hand alike.
     */
    #[Ignore]
    public function isListOfUnionsProvided(): bool
    {
        return $this->listOfUnionsProvided;
    }

    #[Assert\Callback]
    public function validateOpenApiConstraints(ExecutionContextInterface $context): void
    {
        foreach ($this->validateOpenApiNode($this->toOpenApiValidationPayload(), self::OPENAPI_VALIDATION_CONSTRAINTS, 'payload', 0) as $error) {
            $context->buildViolation(str_ends_with($error, '.') ? $error : $error . '.')->addViolation();
        }
    }

    /**
     * This DTO as an OpenAPI-named payload — machinery for the generated validation, not an API to
     * call. It is public only because an enclosing DTO is a different class and needs this view of
     * a nested one. For output, use the Symfony serializer.
     *
     * @internal
     *
     * @return array<string, mixed>
     */
    public function toOpenApiValidationPayload(): array
    {
        $payload = [];
        foreach (self::OPENAPI_PHP_TO_NAME_MAP as $phpName => $openApiName) {
            // A required property always exists. An optional one belongs in the payload only when
            // it was actually provided — its presence flag says so, which is also what keeps the
            // flags themselves out of the payload (iterating the object's properties would put
            // them in and trip `additionalProperties: false`).
            $flag = self::OPENAPI_PROVIDED_FLAGS[$phpName] ?? null;
            if ($flag !== null && $this->{$flag} !== true) {
                continue;
            }
            $payload[$openApiName] = $this->{$phpName};
        }

        return $payload;
    }

    /**
     * @param array<string, mixed> $schema
     * @return array<int, string>
     */
    private function validateOpenApiNode(mixed $value, array $schema, string $path, int $depth): array
    {
        if ($depth >= self::OPENAPI_MAX_VALIDATION_DEPTH) {
            return [];
        }

        $errors = [];
        $normalizedValue = $value;
        $structuredValue = $this->normalizeOpenApiStructuralValue($value);

        if (is_array($schema['oneOf'] ?? null)) {
            $matchingBranches = 0;
            $branchErrors = [];
            foreach ($schema['oneOf'] as $branch) {
                if (!is_array($branch)) {
                    continue;
                }

                if (is_string($branchClass = $branch['x-php-instanceof'] ?? null) && $branchClass !== '') {
                    $branchFqcn = __NAMESPACE__ . '\\' . ltrim($branchClass, '\\');
                    if (is_object($value) && $value instanceof $branchFqcn) {
                        $matchingBranches++;
                    }
                    continue;
                }

                if (array_key_exists('type', $branch) && !$this->matchesOpenApiCallbackType($normalizedValue, $branch['type'])) {
                    continue;
                }
                // Keep why each branch failed: with no match at all those reasons are far more
                // useful than "does not match any oneOf branch".
                $errorsForBranch = $this->validateOpenApiNode($value, $branch, $path, $depth + 1);
                if ($errorsForBranch === []) {
                    $matchingBranches++;
                    continue;
                }

                $branchErrors = [...$branchErrors, ...$errorsForBranch];
            }

            if ($matchingBranches > 1) {
                $errors[] = sprintf('%s matches more than one allowed oneOf branch', $path);
            } elseif ($matchingBranches === 0) {
                $errors = $branchErrors === []
                    ? [...$errors, $this->describeOpenApiUnionMismatch($path, 'oneOf', $schema['oneOf'], $value)]
                    : [...$errors, ...array_values(array_unique($branchErrors))];
            }

            if ($matchingBranches === 1) {
                $discriminatorProperty = is_string($schema['x-discriminator-property'] ?? null)
                    ? $schema['x-discriminator-property']
                    : null;
                $discriminatorPhpProperty = is_string($schema['x-discriminator-php-property'] ?? null)
                    ? $schema['x-discriminator-php-property']
                    : null;
                $discriminatorMap = is_array($schema['x-discriminator-map'] ?? null)
                    ? $schema['x-discriminator-map']
                    : null;

                if ($discriminatorProperty !== null && $discriminatorMap !== null && is_object($value)) {
                    $phpProperty = $discriminatorPhpProperty ?? $discriminatorProperty;
                    // The DTO keeps its state private: read the discriminator from the payload it
                    // reports, keyed by the OpenAPI name, and fall back to a public property for a
                    // hand-written object.
                    $discriminatorPayload = $this->normalizeOpenApiStructuralValue($value);
                    $discriminatorCarrier = is_array($discriminatorPayload) ? $discriminatorPayload : [];
                    if (!array_key_exists($discriminatorProperty, $discriminatorCarrier) && !property_exists($value, $phpProperty)) {
                        $errors[] = sprintf('%s discriminator property %s is required', $path, $discriminatorProperty);
                    } else {
                        $discriminatorValue = $discriminatorCarrier[$discriminatorProperty]
                            ?? (property_exists($value, $phpProperty) ? $value->{$phpProperty} : null);
                        $mapKey = is_scalar($discriminatorValue) ? (string)$discriminatorValue : null;
                        if ($mapKey === null || !array_key_exists($mapKey, $discriminatorMap)) {
                            $errors[] = sprintf(
                                '%s discriminator %s must be one of: %s',
                                $path,
                                $discriminatorProperty,
                                implode(', ', array_keys($discriminatorMap)),
                            );
                        } elseif (is_string($expectedClass = $discriminatorMap[$mapKey]) && $expectedClass !== '') {
                            $expectedFqcn = __NAMESPACE__ . '\\' . ltrim($expectedClass, '\\');
                            if (!$value instanceof $expectedFqcn) {
                                $errors[] = sprintf('%s discriminator %s must match concrete class', $path, $discriminatorProperty);
                            }
                        }
                    }
                }
            }
        }

        if (array_key_exists('type', $schema)) {
            $typeConstraint = $schema['type'];
            if (is_string($typeConstraint)) {
                if (!$this->matchesOpenApiCallbackType($normalizedValue, $typeConstraint)) {
                    $errors[] = $typeConstraint === 'array' && is_array($normalizedValue)
                        ? sprintf('%s must be a JSON array (list with sequential keys), got an associative array', $path)
                        : sprintf('%s must be of type %s', $path, $typeConstraint);
                }
            }
        }

        if (is_string($normalizedValue) && $this->schemaHasAnyOpenApiKey($schema, ['minLength'])) {
            $length = mb_strlen($normalizedValue);
            $minLength = $this->toIntConstraint($schema['minLength'] ?? null);
            if ($minLength !== null && $length < $minLength) {
                $errors[] = sprintf('%s length must be at least %d characters', $path, $minLength);
            }
        }

        if (is_array($structuredValue) && is_array($schema['required'] ?? null)) {
            foreach ($schema['required'] as $requiredProp) {
                if (is_string($requiredProp) && !array_key_exists($requiredProp, $structuredValue)) {
                    $errors[] = sprintf('%s is required', $this->openApiChildPath($path, $requiredProp));
                }
            }
        }

        if (is_array($schema['properties'] ?? null) && is_array($structuredValue)) {
            foreach ($schema['properties'] as $propertyName => $propertySchema) {
                if (!is_string($propertyName) || !is_array($propertySchema)) {
                    continue;
                }
                if (!array_key_exists($propertyName, $structuredValue)) {
                    continue;
                }
                if ($this->isGeneratedOpenApiDtoObject($structuredValue[$propertyName])) {
                    continue;
                }
                $errors = [
                    ...$errors,
                    ...$this->validateOpenApiNode($structuredValue[$propertyName], $propertySchema, $this->openApiChildPath($path, $propertyName), $depth + 1),
                ];
            }
        }

        $listValue = is_array($value) && array_is_list($value);

        if ($listValue && is_array($schema['items'] ?? null)) {
            foreach ($value as $index => $itemValue) {
                if ($this->isGeneratedOpenApiDtoObject($itemValue)) {
                    continue;
                }
                $errors = [
                    ...$errors,
                    ...$this->validateOpenApiNode($itemValue, $schema['items'], $path . '[' . $index . ']', $depth + 1),
                ];
            }
        }

        if (is_array($schema['anyOf'] ?? null)) {
            $anyOfMatched = false;
            $anyOfErrors = [];
            foreach ($schema['anyOf'] as $branch) {
                if (!is_array($branch)) {
                    continue;
                }

                if (array_key_exists('type', $branch) && !$this->matchesOpenApiCallbackType($normalizedValue, $branch['type'])) {
                    continue;
                }
                $errorsForBranch = $this->validateOpenApiNode($value, $branch, $path, $depth + 1);
                if ($errorsForBranch === []) {
                    $anyOfMatched = true;
                    break;
                }

                $anyOfErrors = [...$anyOfErrors, ...$errorsForBranch];
            }

            if (!$anyOfMatched) {
                $errors = $anyOfErrors === []
                    ? [...$errors, $this->describeOpenApiUnionMismatch($path, 'anyOf', $schema['anyOf'], $value)]
                    : [...$errors, ...array_values(array_unique($anyOfErrors))];
            }
        }

        return $errors;
    }

    /**
     * @param array<string, mixed> $schema
     */
    private function matchesOpenApiNode(mixed $value, array $schema, int $depth): bool
    {
        return $this->validateOpenApiNode($value, $schema, 'payload', $depth) === [];
    }

    /**
     * Every branch was gated out by its declared type, so no branch has a reason to report. Naming the
     * types the union does accept turns an unactionable sentence into one the caller can act on. Same
     * wording as the runtime validator, so a payload reads the same whichever mode answered it.
     *
     * @param array<int, mixed> $branches
     */
    private function describeOpenApiUnionMismatch(string $path, string $kind, array $branches, mixed $value): string
    {
        $types = [];
        foreach ($branches as $branch) {
            if (!is_array($branch)) {
                continue;
            }

            $declared = is_array($branch['type'] ?? null) ? $branch['type'] : [$branch['type'] ?? null];
            foreach ($declared as $type) {
                if (is_string($type) && $type !== '' && !in_array($type, $types, true)) {
                    $types[] = $type;
                }
            }
        }

        if ($types === []) {
            return sprintf('%s does not match any %s branch', $path, $kind);
        }

        $last = array_pop($types);
        $expected = $types === [] ? $last : implode(', ', $types) . ' or ' . $last;

        return sprintf(
            '%s does not match any %s branch (expected %s, got %s)',
            $path,
            $kind,
            $expected,
            match (true) {
                $value === null => 'null',
                is_bool($value) => 'boolean',
                is_int($value) => 'integer',
                is_float($value) => 'number',
                is_string($value) => 'string',
                is_array($value) => array_is_list($value) ? 'array' : 'object',
                default => 'object',
            },
        );
    }

    private function matchesOpenApiCallbackType(mixed $value, mixed $type): bool
    {
        if (!is_string($type) || $type === '') {
            return true;
        }

        return match ($type) {
            'integer' => is_int($value) || (is_float($value) && is_finite($value) && floor($value) === $value),
            'string' => is_string($value),
            'array' => is_array($value) && array_is_list($value),
            'object' => is_array($value) || is_object($value),
            default => true,
        };
    }

    private function normalizeOpenApiStructuralValue(mixed $value): mixed
    {
        if (is_array($value) || !is_object($value)) {
            return $value;
        }

        // Generated DTOs expose their OpenAPI-named payload themselves.
        if (method_exists($value, 'toOpenApiValidationPayload')) {
            return $value->toOpenApiValidationPayload();
        }
        // Any other object: only its public state is visible, under PHP property names.
        return array_filter(
            get_object_vars($value),
            static fn(mixed $propertyValue): bool => $propertyValue !== null,
        );
    }

    private function isGeneratedOpenApiDtoObject(mixed $value): bool
    {
        // Such a DTO validates its own schema through its own #[Assert\Callback], reached by the
        // #[Assert\Valid] cascade — the enclosing DTO must not report the same errors again.
        return is_object($value)
            && method_exists($value, 'validateOpenApiConstraints')
            && method_exists($value, 'toOpenApiValidationPayload');
    }

    private function openApiChildPath(string $path, string $propertyName): string
    {
        // The first level names the field the way the runtime validator's caller does, so both
        // modes report the same subject: `field "tags" must ...`.
        return $path === 'payload'
            ? sprintf('field "%s"', $propertyName)
            : $path . '.' . $propertyName;
    }

    /**
     * @param array<int, string> $keys
     */
    private function schemaHasAnyOpenApiKey(array $schema, array $keys): bool
    {
        foreach ($keys as $key) {
            if (array_key_exists($key, $schema)) {
                return true;
            }
        }

        return false;
    }

    private function toIntConstraint(mixed $value): ?int
    {
        if (is_int($value)) {
            return $value;
        }
        if (is_float($value) && floor($value) === $value) {
            return (int)$value;
        }

        return null;
    }
}

===== User.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use DateTimeImmutable;
use Symfony\Component\Serializer\Attribute\Ignore;
use Symfony\Component\Validator\Constraints as Assert;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 */
final class User
{
    public function __construct(
        #[Assert\NotNull]
        private readonly int $id,
        #[Assert\NotNull]
        private readonly string $name,
        #[Assert\Valid]
        private readonly ?Animal2 $pet,
        #[Assert\NotNull]
        private readonly DateTimeImmutable $createdAt,
    ) {
    }

    public function getId(): int
    {
        return $this->id;
    }

    public function getName(): string
    {
        return $this->name;
    }

    public function getPet(): ?Animal2
    {
        return $this->pet;
    }

    /**
     * Formatted as the schema declares it — a date stays a date, and a date-time keeps the
     * sub-second precision the payload carried.
     */
    public function getCreatedAt(): string
    {
        return ($this->createdAt->format('u') === '000000'
            ? $this->createdAt->format('c')
            : $this->createdAt->format('Y-m-d\TH:i:s.uP'));
    }

    #[Ignore]
    public function getCreatedAtAsDateTime(): DateTimeImmutable
    {
        return $this->createdAt;
    }

    /**
     * This DTO as an OpenAPI-named payload: what it received, under the names the schema uses.
     * Machinery for the generated validation, not an API to call — an enclosing DTO reads a nested
     * one through here, because the properties themselves are private. For output, use the Symfony
     * serializer.
     *
     * @internal
     *
     * @return array<string, mixed>
     */
    public function toOpenApiValidationPayload(): array
    {
        $payload = [];
        $payload['id'] = $this->id;
        $payload['name'] = $this->name;
        $payload['pet'] = $this->pet;
        $payload['createdAt'] = $this->createdAt;

        return $payload;
    }
}

===== User2.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use DateTimeImmutable;
use Symfony\Component\Serializer\Attribute\Ignore;
use Symfony\Component\Validator\Constraints as Assert;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 */
final class User2
{
    public function __construct(
        #[Assert\NotNull]
        private readonly int $id,
        #[Assert\NotNull]
        private readonly string $name,
        #[Assert\Valid]
        private readonly ?User2Pet $pet,
        #[Assert\NotNull]
        private readonly DateTimeImmutable $createdAt,
    ) {
    }

    public function getId(): int
    {
        return $this->id;
    }

    public function getName(): string
    {
        return $this->name;
    }

    public function getPet(): ?User2Pet
    {
        return $this->pet;
    }

    /**
     * Formatted as the schema declares it — a date stays a date, and a date-time keeps the
     * sub-second precision the payload carried.
     */
    public function getCreatedAt(): string
    {
        return $this->createdAt->format('Y-m-d');
    }

    #[Ignore]
    public function getCreatedAtAsDateTime(): DateTimeImmutable
    {
        return $this->createdAt;
    }
}

===== User2Pet.php =====
<?php

declare(strict_types=1);

namespace GoldenCorpus;

use Symfony\Component\Validator\Constraints as Assert;

/**
 * NOTE: This DTO is auto-generated from an OpenAPI schema.
 * Do not edit this file manually.
 *
 * From: User2.pet
 */
final class User2Pet
{
    public function __construct(
        #[Assert\NotNull]
        private readonly string $name,
        #[Assert\NotNull]
        private readonly string $meow,
        #[Assert\NotNull]
        private readonly string $bark,
        #[Assert\NotNull]
        private readonly string $test,
    ) {
    }

    public function getName(): string
    {
        return $this->name;
    }

    public function getMeow(): string
    {
        return $this->meow;
    }

    public function getBark(): string
    {
        return $this->bark;
    }

    public function getTest(): string
    {
        return $this->test;
    }

    /**
     * This DTO as an OpenAPI-named payload: what it received, under the names the schema uses.
     * Machinery for the generated validation, not an API to call — an enclosing DTO reads a nested
     * one through here, because the properties themselves are private. For output, use the Symfony
     * serializer.
     *
     * @internal
     *
     * @return array<string, mixed>
     */
    public function toOpenApiValidationPayload(): array
    {
        $payload = [];
        $payload['name'] = $this->name;
        $payload['meow'] = $this->meow;
        $payload['bark'] = $this->bark;
        $payload['test'] = $this->test;

        return $payload;
    }
}
