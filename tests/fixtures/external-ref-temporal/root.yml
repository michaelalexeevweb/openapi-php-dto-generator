openapi: 3.0.0
info:
  title: Root Temporal
  version: 1.0.0
paths: { }
components:
  schemas:
    Root:
      type: object
      required:
        - event
      properties:
        event:
          $ref: './sub/event.yml#/components/schemas/Event'
