openapi: 3.0.0
info:
  title: Lib
  version: 1.0.0
paths: { }
components:
  schemas:
    Widget:
      type: object
      required:
        - id
        - name
      properties:
        id:
          type: integer
        name:
          type: string
    Unrelated:
      type: object
      required:
        - foo
      properties:
        foo:
          type: string
