openapi: 3.0.3
info:
  title: Integer formats
  version: 1.0.0
paths: { }
components:
  schemas:
    IntFormatModel:
      type: object
      required:
        - small
      properties:
        small:
          type: integer
          format: int32
        big:
          type: integer
          format: int64
