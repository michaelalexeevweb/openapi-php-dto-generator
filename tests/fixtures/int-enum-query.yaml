openapi: 3.0.3
info:
  title: Int-backed enum and bounded int
  version: 1.0.0
paths: { }
components:
  schemas:
    Filter:
      type: object
      required:
        - status
      properties:
        status:
          type: integer
          enum:
            - 1
            - 2
            - 3
        count:
          type: integer
