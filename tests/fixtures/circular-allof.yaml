openapi: 3.1.0
info:
  title: Circular allOf inheritance (invalid — must be rejected)
  version: 1.0.0
paths: { }
components:
  schemas:
    CircularA:
      allOf:
        - $ref: '#/components/schemas/CircularB'
        - type: object
          properties:
            x: { type: string }
    CircularB:
      allOf:
        - $ref: '#/components/schemas/CircularA'
        - type: object
          properties:
            y: { type: string }
