openapi: 3.0.3
info:
  title: AdditionalProperties test
  version: 1.0.0
paths: { }
components:
  schemas:
    TestMapModel:
      type: object
      required:
        - test_map
      properties:
        test_map:
          type: object
          additionalProperties:
            type: string
            example: test1
