openapi: 3.0.0
info:
  title: Union Type (property oneOf) Test
  version: 1.0.0
paths: {}
components:
  schemas:
    UnionSample:
      type: object
      required:
        - id
      properties:
        id:
          oneOf:
            - type: string
              format: uuid
            - type: integer
              minimum: 10
              maximum: 100

