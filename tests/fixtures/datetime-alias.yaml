openapi: 3.0.0
info:
  title: Datetime Alias Format Test
  version: 1.0.0
paths: {}
components:
  schemas:
    # "datetime" is an alias for "date-time" supported by this library.
    AliasDateTimeModel:
      type: object
      required:
        - createdAt
      properties:
        createdAt:
          type: string
          format: datetime

