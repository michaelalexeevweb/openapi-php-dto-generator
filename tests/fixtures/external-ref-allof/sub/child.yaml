openapi: 3.0.0
info:
  title: Child
  version: 1.0.0
paths: { }
components:
  schemas:
    Cat:
      allOf:
        - $ref: '../common/base.yaml#/components/schemas/Base'
        - type: object
          required:
            - meow
          properties:
            meow:
              type: string
