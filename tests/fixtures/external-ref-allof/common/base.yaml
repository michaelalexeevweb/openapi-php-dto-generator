openapi: 3.0.0
info:
  title: Base
  version: 1.0.0
paths: { }
components:
  schemas:
    Base:
      type: object
      required:
        - id
      properties:
        id:
          type: integer
          minimum: 1
